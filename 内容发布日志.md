# 内容发布日志（CalcBadger）

矩阵首个**工具站**：发布单元是「一个经过验证的工具」，不是一篇文章。审计/复查本站时核查重点是**公式正确性 + 组件可用性**（单元测试期望值是否真来自权威来源、浏览器实测结果是否与独立手算一致），不是文章维度。

## 2026-08-02 — 建站 + 首批 5 个工具上线

- **背景**：交接文档 `独立站/CalcBadger建站计划_20260803.md`，词库 `独立站/候选六号站-计算器工具站词库_20260802.md`（896 词，选题按发布顺序总表从上往下）。域名 calcbadger.com（Cloudflare，2026-08-03 购）。
- **技术栈**：Astro 6 + **Preact islands**（`client:load`，计算器即首屏主内容）+ Tailwind v4 + `vendor/site-toolkit` submodule。无 webfont（系统字体栈，性能优先）。GitHub `zhangwenhao66/calcbadger` → CF Pages（git 连接自动部署，**无需 deploy hook**）→ 域名绑定完成。
- **`404.astro` 第一个 commit 就带上**，线上实测 `curl https://calcbadger.com/does-not-exist/` 返回真 404 ✓。
- **Lighthouse 移动端（线上 /cd-calculator/）：Performance 98**（LCP 2.0s / TBT 0ms / CLS 0），达标（要求 90+）。

### 工具站生产管线（与内容站的根本差异，本批验证跑通）

每个工具 = 纯函数公式模块（`src/lib/`，头注权威来源）+ vitest 单元测试（`tests/`，期望值**来自权威来源人工核算**，绝不用实现输出回填）+ Preact 岛组件 + 逐页独立撰写的讲解/示例/参考表/FAQ + `/embed/<slug>/` 嵌入皮肤。**116 个测试全部通过。**

### 首批 5 个工具（跨 5 类目）

| slug | 目标词（+同页变体） | 月搜索/KD | 公式权威来源 | 测试期望值来源 |
|---|---|---|---|---|
| `cd-calculator` | cd calculator | 165,000/3 | A=P(1+r/n)^nt（investor.gov）；APY 定义 Reg DD (12 CFR 1030) Appendix A | 教科书公认值（$1,000@6%月复利10年=$1,819.40）+ Python 独立计算 |
| `square-footage-calculator` | square footage calculator（+room square footage calculator） | 201,000/9 | 初等几何；换算 NIST SP 811（1 ft=0.3048 m 精确值） | 手算 + NIST 精确换算因子 |
| `stair-calculator` | stair calculator（+stair stringer calculator） | 33,100/10 | 2021 IRC R311.7.5（riser≤7¾"、tread≥10"）；勾股定理 | IRC 限值 + Python 独立验算（108" rise→14 risers/7 11/16"/169" stringer） |
| `sat-score-calculator` | sat score calculator | 22,200/0 | College Board《Scoring Your Paper SAT Practice Test #4》官方换算表（©2023, 2324-BB-852） | **官方 PDF 下载后 pdftotext 机器提取**，测试内嵌提取原文做 67+55 行全表比对 |
| `molarity-calculator` | molarity calculator | 22,200/8 | IUPAC Gold Book c=n/V；摩尔质量 PubChem | 定义式手算（58.44 g NaCl/1 L=1.000 M 等） |

- **争议/多标准处理**：stair 页明示采用 IRC 住宅标准（IBC 商用 7"/11" 可自行输入）；SAT 页明示是纸质练习题 #4 的官方 worksheet 实现、真数字 SAT 为自适应故只能给区间、Bluebook 由 App 自行计分。
- **讲解差异化**：五页结构各不相同（CD 走 APY/APR 辨析+提前支取罚息；sqft 走测量方法+购料损耗；stair 走 code 限值+riser-first 算例+画线切割；SAT 走自适应计分原理+区间诚实解读；molarity 走单位陷阱+容量瓶原理+稀释）。FAQ 覆盖同簇变体问法。humanizer 检查过。
- **嵌入组件（本站结构性外链武器）**：每页带 "Embed this calculator" 可复制 iframe 代码（snippet 含指向工具页的 dofollow 文字链）；`/embed/<slug>/` 为同一组件的无导航无广告皮肤，页内 "Powered by CalcBadger" dofollow 链接，canonical 指向主工具页，**可抓取但排除出 sitemap**。已实测正常渲染。
- **浏览器功能实测（dev server + 线上）**：五个工具各输入 2-3 组数值，结果与 Python 独立手算全部一致（CD APR 模式 $11,442.48/4.594% APY；sqft 2m×1m 圆整回 2 m²；stair 非合规 tread=9" 正确亮红牌；SAT 越界输入正确钳制；molarity 三种 solve-for 均正确）。移动端 375px 单列无横向滚动。console 0 报错。copy 按钮实测可用。
- **Schema**：WebApplication（UtilitiesApplication）+ FAQPage + BreadcrumbList；首页 WebSite。
- **related-tools**：用 site-toolkit `pickRelatedGuides` 轮转窗口 + 跨类目补位（当前每类目仅 1 个工具，5 页互链全覆盖）。

### 基础设施接入（第十节六项，全部完成）

1. `流量站清单.md` CalcBadger 行已改「运营中」→ trafficsite-* 任务群自动纳入
2. AdSense：site 已加 + ownership 已验证（代码法）+ **Request review 已提交**（状态 Getting ready，随 8/8 `adsense-review-followup` 复查——该任务 SKILL 已更新为五站）；ads.txt 线上 200
3. contact@calcbadger.com：收信 Cloudflare Email Routing → 0009888@gmail.com **Active**；发信 Gmail「Send mail as」经 Mailjet 587 TLS，`gmail_send.py` FROM_ALIASES 已补 `calcbadger`。（建站当天误记为"发信待 Owen 配置"，2026-08-02 晚到 Gmail 设置实地核对，别名早已存在，`--from calcbadger` 实测发信成功，记录已改正。）
4. GSC：`sc-domain:calcbadger.com` 域名属性已建（DNS TXT 验证通过，记录保留勿删）+ sitemap-index.xml 已提交（提交后即时状态"Couldn't fetch"是新域名常态，待 Google 首爬后自动转 Success，后续由 trafficsite-gsc-index-requests 跟进）
5. IndexNow：密钥文件线上 200；首批 5 工具+首页提交 Bing 202/Yandex 202，记入 `indexnow-submit-log.json`
6. 定时任务 `calcbadger-tool-publishing` 已创建并启用（每天 18:00，M1 每次 1 个工具，SKILL 按交接文档第四/七/八节从头写，未抄内容站模板；提速到 2 个/天需 GSC 健康+8/28 WarCrumbs 试点结论+Owen 确认）

### 词库标记

`cd calculator`、`square footage calculator`、`stair calculator`、`molarity calculator`、`sat score calculator` 标 `[已用 2026-08-02]`；`stair stringer calculator`、`room square footage calculator` 标并入对应页。（词库为共享文件，行内替换 + 总行数断言 1530 不变。）

### 冷启动预期（写给未来复查的会话）

M1-M2 GSC 接近零是**正常曲线**（Hollowvane 实测上线 10 天仅 2 次点击/155 展示），按 M3-M6 爬坡斜率评估，不要提前改策略。

### ⚠️ 遗留：通知邮件未能发出（2026-08-02）

给 Owen 的「Gmail 发信别名待配置」通知邮件**没有发出去**——`gmail_send.py` 当天所有会话的调用都无限挂起（同环境 curl/原生 Python 走代理访问 Google API 均正常，疑似脚本 httplib2 传输层不吃代理环境变量），已开独立修复任务并在修复任务里要求补发本邮件。在补发成功前，「配 Gmail Send mail as 别名」这件事 Owen 还不知情。

## 2026-08-02 — UI 改版 + 新 logo（Owen 逐轮确认后部署）

### UI 改版（commit `ff114af`，ui-ux-pro-max 主导 + emil-design-eng 交互规则）

- **一句话概念**：页面是浅色文档，**全页唯一的深色物体就是那台计算器**。计算器组件改成深色仪器（冷炭机身 `#18181b`、凹陷 mono 输入位、结果显示条，主结果用运算橙大号 mono）。
- **交互**（emil-design-eng）：工具卡/按钮做成 keycap（底边 3px，按下下沉 1px，160ms strong ease-out）；hover 只在 `(hover:hover)` 生效；`prefers-reduced-motion` 下全部关闭；**结果数字高频更新，刻意不加动画**。
- **字体**：自托管 Rubik Variable（display）+ ui-monospace tabular（数字）。橙色只给可交互/运算元素，不做装饰。
- **与 MythCairn 的区分**（Owen 中途指出两站太像，已修正两轮）：① 页首深色横带改回浅色 chrome，深色只属于设备本体；② 机身色由暖玄武改冷炭（`#1c1917`→`#18181b`），跟 MythCairn 的暖玄武 `#262219` 拉开色温。最终四层差异：色温 / 字体（slab serif vs geometric sans）/ accent（靛紫 vs 橙）/ 深色块语义（碑 vs 机）。
- **全站 em-dash 清零**（含建站当天我写进讲解文案的那批），仅保留 SAT 换算表里表示"无此项"的占位符。
- 验证：116 单测通过、构建通过、楼梯计算器功能回归与建站当天的权威核算值一致、线上 Lighthouse 移动端 **99**（LCP 1.5s / TBT 30ms / CLS 0）。

### 新 logo（commit `529b0b2`）

- 旧的抽象獾头「既看不出獾也看不出计算器」，重做。用 ARK 的 **Seedream 5.0 Pro** 生成三个方向（gpt-image-2 / Nano Banana 2 本机无可用通道），Owen 选中"獾从橙色计算器后探头"的构图，再手工矢量重画。
- 定稿几何经四轮细调：3 列 × 2 行键盘、键间距 1.75、键盘上留白 2.10 / 下留白 2.30、键 5.17×2.62。**更密的网格在 22px 导航和 16px favicon 下会糊成一块橙**，这是选 3×2 的唯一原因。
- `BadgerLogo.astro` 与 `public/favicon.svg` 同一份图形；线上 favicon 与本地 md5 逐字节一致（`bb969f32…`，**没有靠看标签页图标判断**，浏览器 favicon 缓存不可信）。
- 生成的全身吉祥物图（獾抱计算器）留作后续 About 页 / OG 图 / 404 页素材，暂存 scratchpad 未入库。

## 2026-08-02 — 第 6 个工具：BMI Calculator（首次按每日 1 个节奏发布）

- **slug** `bmi-calculator`，**category** `Health`（新类目，跨类目混排：此前 5 个分别是 Finance/Home Improvement/Construction/Education/Science）。
- **目标词 + 同页变体**：`bmi calculator`（2,740,000/月，KD58）+ `bmi calculator for women`（246,000/月，并入本页而非另建页——BMI 公式与分级对男女完全一致，"for women"页只是数值不同）+ `bmi chart`（201,000/月，并入本页的参考表）。
- **公式权威来源**：CDC "About Adult BMI"（`cdc.gov/bmi/adult-calculator`）+ "BMI Frequently Asked Questions"（`cdc.gov/bmi/faq`）——BMI = kg÷m²，US 单位捷径 703×lb÷in²（703 是 CDC 自己说明的四舍五入系数，精确值 703.07，两条单位路径算同一人会有千分位级差异，非 bug）。CDC/WHO 成人分级：<18.5 偏瘦、18.5–24.9 健康、25–29.9 超重、≥30 肥胖（30/35/40 分 Class 1/2/3）。
- **争议标准的明示处理**：亚洲人群分级（超重≥23、肥胖≥27.5）来自 WHO 2004 专家共识（*The Lancet* 2004;363:157-63），页面默认标准 WHO/CDC 分级，用一个显式下拉框切换到亚洲分级，不做静默替换——两套标准都在计算器和讲解里说明来源和适用场景。
- **测试**：14 个新 vitest 用例（`tests/bmi.test.ts`），期望值均用 Python 独立按公式手算（70kg/175cm、203lb/69in、45kg/170cm 等边界/非边界值），不是拿实现输出回填；跑通后与 `src/lib/bmi.ts` 输出比对一致。全站 130 测试全过。
- **浏览器实测**（本地 `calcbadger-preview`，端口 4346，避开另一会话占用的 4334 dev server）：160lb/5'9" → BMI 23.6（与手算一致）；切到 metric + Asian 标准，75kg/175cm → BMI 24.5 分级正确显示为"Overweight (increased risk)"（WHO 标准下同样数值应为 Healthy，验证了切换标准真的改变分级逻辑而非摆设）；移动端 375px 单列无横向滚动；console 0 报错；`/embed/bmi-calculator/` 页正常渲染带 "Powered by CalcBadger" dofollow 链接。
- **独立 agent 审核**（怀疑视角复核）：公式/分级/亚洲标准引用、14 个测试期望值、UI 单位换算与标准切换逻辑、BMI 参考表数值、来源 URL 均通过独立验证。**抓到 2 个真问题并已修复**：① worked example 里"175cm/95kg 需减 19–35kg"算错（96.66kg 下限对应的应是减 38kg 不是 35kg），已改写为准确表述；② 全篇 em dash 密度（13个，远高于其余 5 个工具页 0–2 个的水位）+ 重复的"not X, Y"排比句式，已过 humanizer 改写降到 0 个 em dash。
- **IndexNow**：`/bmi-calculator/` 提交 Bing 200 / Yandex 202，记入 `indexnow-submit-log.json`。
- **词库标记**：`bmi calculator` 标 `[已用 2026-08-02]`；`bmi calculator for women`、`bmi chart` 标并入 bmi-calculator 页（词库为共享文件，行内替换 + 总行数断言 1627 不变）。同时补记：此前 5 个工具的词条标记在日志里声称已写但实际未落地到词库文件，本次一并补上。

### 2026-08-02 晚 — 图标补全 + 发信链路核实（commit `56ee26e`）

- **标签页图标没变的真实原因有两层**：① 浏览器对 favicon 的缓存比 HTTP 缓存顽固得多，光换文件不会立刻生效；② **`/favicon.ico` 一直是 404**——任何回落到 ico 的场景（部分爬虫、旧浏览器、Windows 快捷方式、搜索结果图标）此前根本拿不到图标。
- 补齐：`favicon.ico`（16/32/48 三尺寸，从 favicon.svg 用 qlmanage 光栅化后手写 ICO 容器）+ `apple-touch-icon.png`（180×180，**白底不透明**，iOS 会把透明合成成黑色）。所有 icon href 带 `?v=2`，**以后换图形必须同时 bump 这个版本号**。
- **发信链路：此前的"待 Owen 配置"是错的**。到 Gmail 设置实地核对，`contact@calcbadger.com`（以及 mythcairn / umberlore）的「Send mail as」别名早已配好，均走 in-v3.mailjet.com:587 TLS。当天用 `--from calcbadger` 实测发信成功（Message ID `19fc304252edae3b`）。相关错误记录已在本文件、`流量站清单.md`、记忆里一并改正。
- 另注：`gmail_send.py` 当天被独立任务修复（网络超时+显式代理），并且 **`--body` 参数已改为 `--body-file`**（传文件路径，不是正文字符串）。已核对全部定时任务的 SKILL.md，调用方均已同步，无遗漏。

## 2026-08-03 — 表单控件重做（commit `8f92089`）

Owen 指出下拉列表和数字框「太简陋」。根因不是配色：**原生控件是页面唯一改不动样式的东西**——`<select>` 弹出的是系统白色菜单，直接盖在深色仪器上；数字框的 spinner 是压在数值上的两个 8px 系统箭头。所以做了三个原语（`src/components/ui/`），六个计算器全部改用：

- **`NumberField`** — 隐藏原生 spinner，换成 30×24 的自绘步进器（过 WCAG 2.5.8 的 24px 下限）；**单位内联进输入框**（`unit="ft"`）所以标签不再写成 `Total rise (ft)`；长按连发（400ms 后每 60ms）；按 step 精度回舍，避免 0.1+0.2 显示成 0.30000000000000004
- **`Segmented`** — 2-5 个短选项（单位 / 公制英制 / APY-APR）用它，选项常驻可见、一次点击切换；radiogroup + roving tabindex + 方向键/Home/End
- **`Select`** — 选项多或标签长时用；WAI-ARIA listbox，方向键/Home/End/首字母跳转/Escape 不提交/焦点归还按钮；**空间不足时向上翻转**（嵌入页 iframe 只有 400px 高）
- 选项多到会截断时加 `wide`（占两列，仅 ≥560px 生效，窄屏不会撑出隐式列）

### 实测查出并修掉的一个真 bug（长按会跑飞）

浏览器实测时**渲染进程卡死**：只在按钮上监听 pointerup 的话，如果松手落在按钮之外（手指滑出、标签页失焦、事件被浏览器丢弃），连发定时器永远不清，数字无限自增。已改成在 **window 上监听 pointerup/pointercancel、blur、visibilitychange**，外加 `MAX_REPEATS` 兜底；`setPointerCapture` 也包了 try/catch（它对失效 pointerId 会抛异常，原来会把整个处理函数打断，导致单击都不生效）。

### 验证

130 单测通过；浏览器实测覆盖：Select 全键盘流程（开/移动/提交/Escape 不提交/首字母跳转/焦点归还）、Segmented 方向键与 roving tabindex、步进器单击与长按、**松手在按钮外也能停**、原生 spinner 确实被隐藏、移动端 375px 单列无横向溢出、嵌入 iframe 内菜单不被裁切且 Powered-by 链接仍在。

### 防回潮

- `global.css` 里 `.calc-input` / `.calc-select` 两条死规则**已删除**——留着的话以后的工具会悄悄退回原生控件
- `calcbadger-tool-publishing` 的 SKILL.md 已加硬规则：新工具的表单控件必须用这三个原语，禁止写原生 `<input type="number">` / `<select>`，并写明了单位走 `unit` 属性而不是塞进 label

## 2026-08-03 — content-quality-audit 审计更新（非新发布）

**本条为`trafficsite-content-quality-audit`定时任务审计已发布内容后的修复更新，不是新工具发布。**

- **URL**：`https://calcbadger.com/square-footage-calculator/`
- 审计对象：`square-footage-calculator`（本站首批 5 工具之一，2026-08-02 上线，此前未审计过，本次是本站第二次审计，接续 8/2 对 cd-calculator 的首次审计）
- **发现并修复 2 项**（均经独立 agent 复核确认后才修改）：
  1. sources 引用的 ANSI Z765 标准链接（`homeinnovation.com/services/accreditation/ansi_z765_square_footage_standard`）已返回 **HTTP 410 Gone**，改为该机构现有的活页 `homeinnovation.com/services/national_standards/square_footage_method_for_calculating`（200，内容确认仍讲 ANSI Z765-2021 标准）。
  2. meta description（同时也是页面首屏可见的导语段落）**207 字符**，超出 Google SERP 安全长度（~155-160）约 30-35%，有截断风险；精简为 151 字符，含义不变，未删除页面其他位置已有的形状清单细节。
- 公式正确性（矩形/L 形/圆形/三角形面积、单位换算、参考表 6 组数值）、13 个单元测试、组件功能、Schema 一致性、内链（5 个工具互链全覆盖）、合规、图标可用性等其余维度复核均未发现问题，详见 `content-audit-log.md`。
- 116→130→156 单测全过（含另一并发会话新增的 coin-flip-simulator 26 个测试），build 通过，已 push（commit `60f610c`，用 blob 级暂存隔离，未影响同仓库另一会话正在进行中的 coin-flip-simulator 未提交改动）并确认线上部署生效。

## 2026-08-03 — 第 7 个工具：Coin Flip Simulator & Probability Calculator（当天 3 次节奏第 1 次，commit `02dd394`）

- **slug** `coin-flip-simulator`，**category** `Games`（新类目，与前一天 Health 不同，跨类目混排）。
- **目标词 + 同页变体**：`flip a coin`（发布顺序总表第1行，224万/月，KD20）+ `coin flip`（第22行，150万/月，KD38）——两词语义高度重合，合并为一页；均属"Google 原生 widget 词族"红线清单，按规则做出 widget 给不了的功能深度：加权硬币模拟（1-500 枚、任意正面概率）+ 连续正反面最长streak统计 + 独立的二项分布精确概率计算器（P(恰好/至少/至多 k 次)）。
- **公式权威来源**：NIST/SEMATECH Engineering Statistics Handbook, "Binomial Distribution"（`itl.nist.gov/div898/handbook/eda/section3/eda366i.htm`）——P(x;p,n) = C(n,x)·p^x·(1-p)^(n-x)，独立 agent 审核时重新 WebFetch 该 URL 逐字核对公式与站点标注一致，非我方转述臆测。均值/标准差 np、√(np(1-p)) 为标准概率论结果。
- **测试**：`tests/coinFlip.test.ts` 26 个用例，期望值均手算/组合数独立推导（如 C(10,5)/1024=0.24609375、C(4,2)×0.09×0.49=0.2646、C(20,15..20)求和/2^20≈2.07%），不是拿实现输出回填；独立 agent 审核用 Python 重新独立算过全部数字（含两张参考表的全部 17 个百分比值），逐一比对无误。
- **⚠️ 本次 Browser pane 在浏览器实测环节不可用**（无头定时任务运行，没有实时查看者可供合成画面，`computer`/`javascript_tool`/`navigate` 全部因"pane not displayed/hidden"超时，非页面本身的错误——`read_page` 等非合成类工具仍可用，确认了首屏 DOM 结构正确）。**替代验证方案**：新增 `happy-dom` + `@testing-library/preact` 依赖，写了 `tests/CoinFlipCalculator.dom.test.tsx`（4 个用例）在真实 DOM 里跑通"点击 Flip → 状态更新 → 结果渲染"的完整链路，包括验证 100% 概率硬币产出全正面+满长度streak、切换到概率计算器模式后 n=10/k=5 精确显示"24.61%"（与手算值逐字符匹配）、k>n 时正确拒绝而非显示垃圾结果。这不是 Browser pane 的完全替代（无法测真实点击坐标、移动端视觉布局、Lighthouse），但对组件逻辑正确性的覆盖比纯单测更强；后续工具如再遇到 Browser pane 不可用的情况可复用这个模式。
- **独立 agent 审核**（怀疑视角，独立 WebFetch 源头 + Python 重算）：公式来源逐字核对通过；26+4 测试期望值独立复算通过；tools.ts 两张参考表 17 个数值、4 个 worked examples 全部复算通过；与其余 6 个工具页比对确认无"换壳复用"；`queries` 关键词与其余 6 页零重叠、无蚕食风险。**揪出 2 处非阻断问题并已修复**：① 讲解正文 em dash 密度全站最高（4 处 vs 其余页 0-2 处），改写为无 em dash 版本；② 一条 FAQ 答案用"probability calculator above"这种位置性指代，破坏 GEO 可独立引用性，改写为自包含表述。另建议统一了百分比输入字段的越界校验行为（此前 n/k 越界会显示提示文字，百分比越界却静默 clamp，不一致但从未产生错误数值），已顺带修复为与 n/k 一致的显式提示。
- **验证**：`npx vitest run` 160/160 全过（含新增 30 个测试）；`astro build` 两次成功（Segmented/tools.ts 定稿前后各一次），26 个静态页面全部生成，`/coin-flip-simulator/` 与 `/embed/coin-flip-simulator/` 均在列。
- **发布**：commit `02dd394` push origin main（只暂存本工具相关 8 个文件，未带上同仓库另一并发会话的未提交产出 `gsc-index-submit-log.json`/`外链建设进度.json`/`外链执行日志.md`）；CF Pages 自动部署，`https://calcbadger.com/coin-flip-simulator/` 与 `/embed/coin-flip-simulator/` 均实测 200；IndexNow 提交 `/coin-flip-simulator/`：Bing 200 / Yandex 202。
- **词库标记**：`flip a coin`（第1行）标 `[已用 2026-08-03 并入coin-flip-simulator页]`，`coin flip`（第22行）同样标注；改前改后总行数均为 1627，断言通过。
- IndexNow：`/square-footage-calculator/` 已重新提交（Bing 200 / Yandex 202），记入 `indexnow-submit-log.json`。

## 2026-08-03 — 内容质量审计更新：BMI Calculator（本条为 content-quality-audit 审计更新，非新发布，commit `096aeee`）

- **URL**：`https://calcbadger.com/bmi-calculator/`
- 审计对象：`bmi-calculator`（本站首批 5 工具之一，2026-08-02 上线，此前未审计过，首次审计）。健康类 YMYL-adjacent 工具，本次审计重点是公式/CDC-WHO 阈值本身的准确性，不只是页面 SEO。
- **公式与阈值独立复核（最高优先级）**：用 WebSearch 独立核对官方 `cdc.gov/bmi/adult-calculator/`、`cdc.gov/bmi/adult-calculator/bmi-categories.html`、`cdc.gov/bmi/faq/` 现行内容（非记忆），确认 BMI = kg÷m²、703×lb÷in² 两条公式路径、underweight<18.5/healthy 18.5–24.9/overweight 25–29.9/obesity≥30（Class 1/2/3 = 30–34.9/35–39.9/≥40）四档分类，均与站内 `src/lib/bmi.ts`、`tools.ts` 文案逐字一致，**未发现任何公式或阈值错误**。WHO 2004 亚裔人群下调阈值（23 超重 / 27.5 肥胖）同样独立核对无误。9 组身高体重参考表边界值、3 条 worked examples 用 Python 独立重算全部吻合。检索中出现的"CDC 2026 年上调老年人 BMI 健康区间到 23–27"说法来自第三方 BMI 计算器博客（非 cdc.gov 本身），CDC 官方成人分类页仍是统一阈值，判定为不可信的营销内容，不构成需要更新的时效性问题。
- **发现并修复 1 项**（经独立 agent 复核确认后才修改）：meta description（同时复用为 og/twitter description、JSON-LD WebApplication description、页面首屏可见导语）**220 字符**，比此前 square-footage-calculator 审计修复的 207 字符案例超出幅度更大（约超出安全长度 155-160 的 38-42%），精简为 153 字符，含义不变（BMI、美制/公制单位、CDC/WHO 分类、健康体重范围、亚裔人群阈值选项全部保留）。
- 其余维度复核均未发现问题：14 个单元测试期望值独立复算全部吻合；组件功能（Segmented 单位切换、Select 亚裔标准切换）正确；外部信源链接 cdc.gov 两条实测 200；Lancet 链接 curl 返回 403 但响应头 `cf-mitigated: challenge` 确认是 Cloudflare 人机验证网关而非真实死链（与此前 cd-calculator 审计发现的 eCFR 反爬模式相同）；Schema 三处 JSON-LD（WebApplication/FAQPage/BreadcrumbList）与页面内容一致；内链健康（"More calculators" 区块含其余 6 个工具全部链接）；合规（页面正文已声明"screening number, not a diagnosis"，全站 `/terms/` 页"No professional advice"条款覆盖 medical 场景，footer 可达）；竞品差异化（`get_serp_results` 拉取"bmi calculator"真实排名，头部为 NIH/calculator.net/CDC/Cancer.org/Harvard/Mayo Clinic，本页公式讲解+worked examples+亚裔阈值切换的深度构成真实增量，非同质化）；GEO 结构（核心摘要/FAQ schema/时效署名/AI 爬虫放行）人工核对估计等效 90/99 左右，明显超过 80 门槛。
- **验证**：`npm test` 160/160 全过，`npm run build` 26 页成功生成，无报错。
- **发布**：commit `096aeee` push origin main（只暂存 `src/data/tools.ts` 一个文件，未带上同仓库另一并发会话的未提交产出）；轮询确认线上 `/bmi-calculator/` 已反映新 meta description（多次请求跨 CDN 边缘节点连续 10 次一致命中新内容后才确认部署稳定生效，中途曾观察到边缘缓存新旧内容并存的短暂窗口）；IndexNow 提交 `/bmi-calculator/`：Bing 200 / Yandex 202，记入 `indexnow-submit-log.json`（覆盖 2026-08-02 首发时的旧记录，注明 supersede 关系）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-04 — 第 8 个工具：Temperature Converter（当天 3 次节奏第 1 次，commit `cbd52f7`）

- **slug** `temperature-converter`，**category** `Science`（与上一次发布 Games 类目不同，跨类目混排）。
- **目标词 + 同页变体**：发布顺序总表第 2 行 `celsius to fahrenheit`（122万/月）为主词，一并合并同一簇的 31 个变体词：`fahrenheit to celsius`/`celsius to kelvin`/`kelvin to celsius`/`fahrenheit to kelvin`（互逆换算）+ 13 个烤箱温度具体数值页（120–250°C，`180 c to f`/`200 c to f`等）+ 15 个体温/发烧具体数值页（35.0–42.0°C，含 36.5/36.6/37.2/37.5/37.8/38.5/39.5 等小数变体）。三张参考表（烤箱温度、体温发烧、日常参考点）直接覆盖这些具体数值查询，不做成 32 个孤立页面。
- **公式权威来源**：NIST Special Publication 811（`physics.nist.gov/cuu/pdf/sp811.pdf`）——°F=°C×9/5+32、K=°C+273.15 均为 SI 系统精确定义，非实验测量近似值。体温/发烧阈值交叉核对 CDC "Definitions of Signs, Symptoms, and Conditions of Ill Travelers"（`cdc.gov/port-health`），独立 agent 审核时重新 WebFetch 该 URL 逐字核对，确认原文为"a measured temperature of 100.4°F [38°C] or greater"，与站内文案一致。
- **测试**：`tests/temperature.test.ts` 25 个用例，期望值用 Python 独立计算后手工核对（含 0°C=32°F、100°C=212°F、37°C=98.6°F、38°C=100.4°F、-40°C=-40°F 交叉点、绝对零度 -273.15°C/-459.67°F/0K 等边界），非拿实现输出回填；独立 agent 审核用 Python 重新独立复算全部 25 个测试用例 + 3 张参考表全部 39 行数值，逐一比对无误。
- **独立 agent 审核**（怀疑视角，独立 WebFetch 源头 + Python 重算）：公式与 CDC 来源逐字核对通过；差异化检查（对比 bmi-calculator/cd-calculator 文案结构）确认无换壳复用；GEO/humanizer 检查通过（em dash 密度正常范围）。**揪出 2 处非阻断问题并已修复**：① 切换 °C/°F/K 单位时，输入框里的数字原样保留、被静默按新单位重新解读（如 38.5 从"38.5°C"变成"38.5 K"却无提示），与站内 `BmiCalculator.tsx` 为避免歧义采用的按单位制分开存储数值的既有惯例不一致——改为切换单位时自动把当前数值换算成新单位下的等价值（`switchTo` 函数，`TemperatureConverter.tsx`），而不是重新解读同一串数字；② `embedHeight` 定为 620px 但组件实测高度（移动端最高布局）只有约 380-475px，比同量级工具（molarity-calculator 560px 对应更多字段）明显偏高，会在被嵌入的第三方页面上留出大片空白，改为 420px。两处均在浏览器实测环节独立复现确认后才修改。
- **验证**：`npm test` 185/185 全过（含新增 25 个测试）；`npm run build` 28 个静态页面成功生成；Browser pane 实测 100°C→212°F/373.15K、38.5°C→101.3°F/311.65K 均与 Python 独立算值一致；Kelvin 模式下输入 -5（低于绝对零度）正确显示"不可能"提示而非崩溃或错误数值；移动端 375px 视口无横向滚动；embed 页面 `/embed/temperature-converter/` 正常渲染且带 Powered by 归属链接；console 无报错（此前一次误判的 `round2` 导出报错来自浏览器标签页遗留的 HMR 竞态缓存，开新标签页验证后确认应用本身无报错）。
- **发布**：commit `cbd52f7` push origin main（只暂存本工具相关 5 个文件，未带上同仓库另一并发会话的未提交产出 `gsc-index-submit-log.json`/`外链建设进度.json`/`外链执行日志.md`）；CF Pages 自动部署，轮询 5 次后确认 `https://calcbadger.com/temperature-converter/` 200 且含新工具标题；IndexNow 提交 `/temperature-converter/`：Bing 200 / Yandex 202。
- **词库标记**：32 个变体词（第 2/19/56/57/73/149/192/241/278/307/310/311/367/376/377/406/443/523/524/583/585/587/660/661/662/672/673/699/817/819/948/1010 行）均标 `[已用 2026-08-04 并入temperature-converter页]`，改前改后总行数均为 1627，Python 脚本内置断言通过。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-04 — 第 9 个工具：Length Converter（当天 3 次节奏第 2 次，commit 待定）

- **slug** `length-converter`，**category** `Conversion`（新类目，跨类目混排：与上一次发布 Science 不同）。
- **目标词 + 同页变体**：发布顺序总表第 3 行 `cm to inches`（122万/月）为主词，合并同一簇的 8 个变体词：`mm to inches`（第5行）、`inches to cm`（第12行）、`km to miles`（第13行）、`meters to feet`（第17行）、`inches to mm`（第33行）、`how many feet in a mile`（第34行）、`miles to km`（第44行）、`inches to feet`（第47行）——全部为 C 层单值换算词，合计 490万/月。支持 mm/cm/m/km/in/ft/yd/mi 共 8 个单位，输入一次即显示其余 7 个单位的换算结果（Select 下拉选起始单位，不用 Segmented——8 个选项超过站内 2-5 项的 Segmented 惯例上限）。`kg to lbs`/`lbs to kg`（第4/6行，重量单位）未纳入本页，留给后续单独的重量换算工具，未标记为已用。
- **公式权威来源**：1959 年 International Yard and Pound Agreement（美/英/加/澳/新西兰/南非六国协议，yard 定义为精确 0.9144 米，mile 因此精确等于 1,609.344 米）+ NIST 官网 `nist.gov/pml/owm/si-units-length`（引用该协议对应的 1959 Federal Register Notice，明确 "the inch...is exactly equivalent to 25.4 mm"）+ NIST Handbook 44（2026）Appendix C 通用计量单位表（PDF 二进制文本提取失败，但其数值与 NIST 官网原文完全一致，构成交叉验证）。米制内部（mm/cm/m/km）换算按 SI 十进制精确定义，非测量近似值。
- **测试**：`tests/length.test.ts` 24 个用例，期望值用 Python 独立按精确换算因子计算后核对（含 1in=2.54cm/1ft=0.3048m/1mi=1609.344m/1mi=5280ft/1yd=3ft 等精确定义边界、30cm→11.811023622047244in、5km→3.1068559611866697mi、42.195km 马拉松→26.218757456454306mi 等真实场景值、0/负数/极小值 1mm→0.000001km/极大值 1e9mm 往返换算等边界），非拿实现输出回填。撰写过程中发现一处 Python 手算录入错误（马拉松换算英里数误写成另一个数字），已修正为正确的独立计算值后测试通过，过程记入本条日志以便复查。
- **独立 agent 审核**（怀疑视角，独立 WebFetch NIST 源头 + 独立 WebSearch 1959 协议 + Python 重算）：公式来源逐字核对通过（WebFetch nist.gov 页面确认协议原文引用准确）；`METERS_PER_UNIT` 八个精确因子核对无误；24 个测试用例 + tools.ts 内 12 行厘米-英寸表、11 行身高对照表（ft/in→cm）、6 行赛程距离表（km vs mi）、4 个 worked examples、7 条 FAQ 全部数字独立复算通过，未发现拿实现输出回填的痕迹；差异化检查（对比 temperature-converter 文案结构）确认整体骨架相似属"单位换算页"的合理共性，但句子层面的历史叙事、举例、来源均为独立撰写，非换壳复用；正文 em dash 密度 3 处，低于 temperature-converter 页的 12 处，处于站内更保守水位；GEO/FAQ 自包含性检查通过，无"如上所述"类位置性指代。**揪出 2 处真实 UI bug 并已修复**：① 负数长度被静默接受、渲染出一整屏负数结果且无提示，与 `TemperatureConverter.tsx`（绝对零度下限校验）、`BmiCalculator.tsx`（`min={0}` 物理量惯例）不一致，已加入 `parsed`/`negative`/`valid` 三态判断，负数时展示专属提示"A length can't be negative"；② 15 位数量级的超大输入会导致结果卡片数字溢出、与相邻列重叠不可读，已在 `global.css` 给 `.calc-results > div` 加 `min-width: 0`、给 `.calc-result-value` 加 `overflow-wrap: anywhere`（属共享规则，同时加固了 BMI/CD 等其余计算器同类风险，经截图确认对 CD Calculator 正常范围输出无视觉回归）。另指出 `embedHeight` 640px 相对实测 631px 缓冲只有 9px（站内惯例约 90-100px），非阻断问题，已采纳建议改为 720px。
- **验证**：`npx vitest run` 209/209 全过（含新增 24 个测试）；`npm run build` 31 个静态页面成功生成，含 `/length-converter/` 与 `/embed/length-converter/`；Browser pane 实测 30cm→11.811in/300mm/0.3m/0.0003km/0.984252ft/0.328084yd/0.000186411mi 全部与 Python 独立算值一致；Select 下拉键盘/鼠标切换单位验证正常（无原生 `<select>` 白色菜单）；切换单位时数值正确重新表达（30cm 切到 miles 显示 0.000186411 mi，而非把"30"直接当成新单位的数字）；移动端 375px 单列布局无横向滚动；`/embed/length-converter/` 在 500px 宽度下渲染正常，"Powered by CalcBadger Length Converter" 归属链接存在；console 无报错。
- **发布**：待 commit + push origin main（只暂存本工具相关文件：`src/lib/length.ts`、`tests/length.test.ts`、`src/components/calculators/LengthConverter.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`，不带上同仓库另一并发会话的未提交产出 `wikipedia-opportunities.md`/`gsc-index-submit-log.json`/`guest-post-outreach.json`/`podcast-pitch-log.md`/`sourcebottle-callout-log.md`/`外链建设进度.json`/`外链执行日志.md`）；CF Pages 自动部署后轮询确认 `https://calcbadger.com/length-converter/` 200 且含新工具标题；IndexNow 提交 `/length-converter/`。
- **词库标记**：9 个变体词（第 3/5/12/13/17/33/34/44/47 行）均标 `[已用 2026-08-04 并入length-converter页]`，改前改后总行数均为 1627，Python 脚本内置断言通过。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-04 — content-quality-audit 审计更新（非新发布）

**本条为`trafficsite-content-quality-audit`定时任务审计已发布内容后的修复更新，不是新工具发布。**

- **URL**：`https://calcbadger.com/stair-calculator/`（另有一处站点全局代码修复影响其余 8 个工具页的"相关计算器"侧栏）
- 审计对象：`stair-calculator`（本站首批 5 工具之一，2026-08-02 上线，此前从未审计过；本次是全站第 4 次审计，按"从未审计过优先"选取）。
- **公式正确性（最高优先级）**：用 Python 独立重算（不参考实现代码）——108in 总升高/10in 踏步深度 → 14 级踢面、7.7143in/级、13 级踏步、总跑距 130in、斜梁长 169.0089in、角度 39.7188°，与 `tests/stairs.test.ts` 及页面 worked example 逐字一致；参考表 8 行（24-120in 总升高对应的级数/踢面高度/跑距）逐一复算全部吻合；分数换算（如 7.714286→7 11/16、7.5→7 1/2、48/7→6 7/8）全部核对无误；commercial IBC 场景（108in/11in/7in）复算一致。IRC 数值本身（7¾in 最大踢面、10in 最小踏步、3/8in 级差容差）与 IBC 商用数值（7in/11in）均用 WebSearch 独立核对 2021 IRC/IBC 现行条文，逐字一致。FAQ 中"Massachusetts 使用不同限值"一句原以为是可能编造的地方名细节，独立 WebSearch 核实：MA 780 CMR 确实将住宅楼梯踢面上限修订为 8¼in、踏步下限 9in，与 IRC 基准值不同，属实非编造。Blondel 法则（2×踢面+踏步≈24-25in，1600 年代法国建筑师）同样独立核实为真实历史来源，非杜撰。
- **单元测试**：`npm test` 全部 10 个测试文件、209 个测试通过（`tests/stairs.test.ts` 11 个）；期望值逐条独立 Python 复算比对全部一致。
- **组件与内嵌**：`StairCalculator.tsx` 逻辑核对无误，`npm run build` 31 页成功生成，`/stair-calculator/` 与 `/embed/stair-calculator/` 均正常。
- **引用来源**：两条 ICC 链接（`codes.iccsafe.org` IRC2021 章节页、`iccsafe.org` 产品页）curl 均 200，前者标题确认为"CHAPTER 3 BUILDING PLANNING"（正确章节，正文本身 JS 渲染故 curl 抓不到具体条文文字，但标题与目标章节吻合，非死链）。
- **发现并修复 2 项**（均经独立 fresh-context agent 复核确认后才修改）：
  1. `stair-calculator` 的 `description` 字段（同时复用为 meta description/og/twitter description/JSON-LD WebApplication.description/页面首屏可见导语）**207 字符**，与此前 square-footage-calculator 审计中被判定应修的 207 字符案例幅度相同，精简为 **157 字符**，含义保留（踢面数、踏步、斜梁长、角度、IRC 合规校验全部保留）。
  2. **站点级 bug（`src/pages/[slug].astro`）**：审计"内链健康度"维度时发现，"相关计算器"侧栏的 `crossCategory` 补位逻辑用固定 `tools` 数组顺序 + 非轮转的 `.slice(0, 6)`，是本项目 CLAUDE.md 明确记录过的那类 bug（`.slice(0,N)` 永远只取数组最前面几项）在新代码路径里的复发——`pickRelatedGuides` 本身是正确的轮转算法，没问题；问题出在同一文件里紧接着的 crossCategory 兜底填充，因为全站 9 个工具里 7 个类目只有 1 个工具（无同类目同伴可轮转），几乎全部工具都会落到这段有 bug 的兜底逻辑，导致数组里排最后的 `length-converter`（Conversion 类目，也是唯一同伴）在全站任何一个"相关计算器"侧栏里**从未出现过**（独立 agent 用真实 `pickRelatedGuides` 实现复现验证，并 curl 实测 3 个不同工具页面 HTML 全文 0 次出现 `/length-converter/`，仅首页/分类页可达，非真正孤儿页但内链权重被系统性剥夺）。修复：把 crossCategory 兜底也改成按当前工具在全局数组里的位置轮转选取（同 `pickRelatedGuides` 的手法），修复后本地模拟验证全站 9/9 工具覆盖率达标，构建产物 grep 确认 `bmi-calculator`/`coin-flip-simulator`/`molarity-calculator`/`sat-score-calculator`/`stair-calculator` 五个页面新增指向 `/length-converter/` 的链接。
- 其余维度（SEO 静态审计、GEO 人工核对、Schema 一致性、合规措辞、图标可用性）均未发现问题，详见 `content-audit-log.md`。
- **验证**：`npm test` 209/209 全过；`npm run build` 31 页成功生成，无报错。
- **发布**：commit `bdbfada` push origin main（只暂存 `src/data/tools.ts` 与 `src/pages/[slug].astro` 两个文件，未带上同仓库另一并发会话的未提交产出 `wikipedia-opportunities.md`/`gsc-index-submit-log.json`/`guest-post-outreach.json`/`podcast-pitch-log.md`/`sourcebottle-callout-log.md`/`外链建设进度.json`/`外链执行日志.md`）；轮询确认线上 `/stair-calculator/` 已反映新 meta description，`/bmi-calculator/`、`/sat-score-calculator/` 已反映新增 `/length-converter/` 链接；IndexNow 提交 `/stair-calculator/`：Bing 200 / Yandex 202（相关计算器侧栏变动属跨页轻微改动，未逐一重新提交其余 8 个页面，避免被判定为批量提交）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-04 — 第 10 个工具：Weight Converter（当天 3 次节奏第 3 次，commit `1a446f4`）

- **slug** `weight-converter`，**category** `Conversion`（与上一次发布同类目 Conversion——本次未做跨类目混排，因为发布顺序总表排名最靠前的未用词恰好也是重量单位换算，且 Conversion 类目本身只有 length-converter 一个同伴，跨类目混排的目的（避免"批量模板"表面特征）在选题层面已通过内容差异化满足）。
- **目标词 + 同页变体**：发布顺序总表第 4 行 `kg to lbs`（122万/月）为主词，合并同一簇的 86 个变体词（第 6/11/35/46/49/69/83/94/... 等行，含 `lbs to kg`/`how many ounces in a pound`/`how many grams in an ounce`/`grams to lbs`/`oz to lbs`/`lbs to oz`/`oz to grams`/`grams to oz`/`mg to g`/`mcg to mg`/`tons to lbs`/`how many grams in a kilogram`/`how many mg in a gram`，以及约 70 个"`[N] kg to lbs`"/"`[N] lbs to kg`"具体数值变体，N 从 1 到 250 不等），合计约 470万/月。支持 mcg/mg/g/kg/oz/lb/US ton 共 7 个单位，输入一次即显示其余 6 个单位的换算结果（沿用 length-converter 的 Select 下拉 + 8→7 单位收敛模式）。
- **公式权威来源**：1959 年 International Yard and Pound Agreement（与 length-converter 引用的是同一份六国协议，但这次是它对 pound 的定义条款：avoirdupois pound 精确等于 0.45359237 kg）+ NIST 官网 `nist.gov/pml/owm/si-units-mass` + NIST Handbook 44（2026）Appendix C（与 length-converter 复用同一份 PDF，因为该文件本身就是长度和质量的通用计量单位表，非偷懒复用无关来源）。ounce（1/16 lb = 28.349523125g）与 US short ton（2,000 lb = 907,184.74g）均从该固定 pound 值算术推导，非独立定义。米制内部（mcg/mg/g/kg）换算按 SI 十进制精确定义。
- **测试**：`tests/weight.test.ts` 27 个用例，期望值用 Python Decimal（30 位精度）独立按精确换算因子计算后核对（含 1lb=0.45359237kg/1oz=28.349523125g/16oz=1lb/1USton=2000lb=907.18474kg 等精确定义边界、1kg→2.2046226218487758lb、150lb→68.0388555kg、134oz(8lb6oz)→3798.83609875g 等真实场景值、0/负数/极小值 1mcg→1e-9kg/极大值 1e6mg 往返换算等边界），非拿实现输出回填。
- **独立 agent 审核**（怀疑视角，独立 WebSearch 1959 协议 + NIST 页面存在性核实 + Python 重算，因 WebFetch 在本环境已知会挂起故全程改用 WebSearch）：公式来源逐字核对通过（1959 协议对 pound 的定义、NIST 两条来源真实存在）；20+ 个测试用例 + tools.ts 内 4 张参考表（kg→lb/lb→kg/oz→g/新生儿 oz→lb·oz）、3 个 worked examples（68kg 体重/8lb6oz 新生儿/200mcg 用药剂量）全部数字独立复算通过，无拿实现输出回填痕迹；GEO/coreSummary/FAQ 自包含性检查通过。**揪出 2 处问题并已修复**：① `WeightConverter.tsx` 沿用 length-converter 的 `fmt()` 格式化函数（`toLocaleString(maximumFractionDigits:10)`），但本工具单位跨度达 mcg↔US ton 约 12 个数量级（远超 length-converter 的 mm↔mile 约 6.4 个数量级），导致 1–45 mcg 换算到 tons 列时，`roundSig` 内部正确保留 6 位有效数字，但 `toLocaleString` 的 10 位小数上限会把这类真实非零结果静默截断显示成"0"，与真正的零无法区分——已改为对绝对值小于 1e-6 的结果改用 `toFixed` 按 `roundSig` 已计算出的有效数字位数展开，浏览器实测 1e-11 kg（触发路径）正确显示为 `0.0000000000000110231 US ton` 而非 0；② "Two systems" 与 "The exact factors" 两个板块的开篇段落与 length-converter 同名板块结构性雷同（标题完全相同、"until 1959...drifted apart..."一段基本是换名词的同一套骨架），属于本项目"同模板换词"红线在页面内解说文字层面的体现（不是整页 doorway page，但两段确属实质复用）——已重写为 avoirdupois vs troy 计量系统起源（金衡盎司比常衡盎司重约 9.7%，为何贵金属用金衡制）+ 美制短吨(2,000lb) vs 英制长吨(2,240lb) vs 公制吨(1,000kg)三方混淆风险，两个角度均为质量单位独有、与长度单位无对应概念，独立核实"avoirdupois"词源(法语"aveir de pois"="goods of weight")与金衡盎司精确值 31.1034768g 均属实非编造；重写后正文 em dash 密度回落到 3 处，与 length-converter 基线持平。此项差异化问题因不确定是否等同于既有"批量模板换词"规则（该规则针对整页规模化生产，这次是同页内解说段落层面）而记入 `独立站/内容通用教训库.md` 新条目 `L-0804-11`（家族工具页解说文字容易在骨架层面复用，需要每页至少一个该主题独有的解释角度，不能只换名词）。
- **验证**：`npm test` 236/236 全过（含新增 27 个测试）；`npm run build` 33 个静态页面成功生成，含 `/weight-converter/` 与 `/embed/weight-converter/`；Browser pane 实测 70kg→154.324lb/2469.18oz/70000g/0.0771618 US ton 均与 Python 独立算值一致；kg↔lb 单位切换验证物理量正确保留（150kg 切到 lb 后再折算回 kg 精确等于 150）；移动端 375px embed 页无横向滚动，"Powered by CalcBadger Weight Converter" 归属链接存在；console 无报错。
- **发布**：commit `1a446f4` push origin main（只暂存本工具相关 5 个文件：`src/lib/weight.ts`、`tests/weight.test.ts`、`src/components/calculators/WeightConverter.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，未带上同仓库另一并发会话的未提交产出 `wikipedia-opportunities.md`/`gsc-index-submit-log.json`/`podcast-pitch-log.md`/`sourcebottle-callout-log.md`/`外链建设进度.json`/`外链执行日志.md`）；CF Pages 自动部署，轮询 3 次后确认 `https://calcbadger.com/weight-converter/` 200 且含新工具标题；IndexNow 提交 `/weight-converter/`：Bing 200 / Yandex 202。
- **词库标记**：87 个变体词均标 `[已用 2026-08-04 并入weight-converter页]`（Python 脚本按精确关键词匹配逐行标记，内置断言：目标 87 词全部命中、改前改后总行数均为 1627）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-05 — 第 11 个工具：Mortgage Calculator（commit `29218e8`）

- **slug** `mortgage-calculator`，**category** `Finance`（此前一直发布 Conversion 类目的 length-converter/weight-converter，本次跨类目混排回到 Finance，此前 Finance 只有首批的 cd-calculator 一个同伴）。
- **目标词**：发布顺序总表第 9 行 `mortgage calculator`（274万/月，KD79，$2.17），另将第 31 行 `amortization calculator`（30.1万/月，KD20）并入同一页——本工具的年度还款余额表 + M=P[r(1+r)ⁿ]/[(1+r)ⁿ−1] 公式讲解本身就是 amortization 概念的完整覆盖，`queries` 数组已含该词。**未**合并 mortgage payoff/extra payment/recast/interest-only/points/biweekly/ARM 等其余 8 个 mortgage 变体词（第 275/477/792/1042/1229/1248/1373/1472 行）——这些是数学结构真正不同的独立计算器（提前还款重算、双周供款、可调利率等），不能同页糊弄，留作后续独立工具。
- **公式权威来源**：标准等额本息公式 M=P[r(1+r)ⁿ]/[(1+r)ⁿ−1]，独立 WebSearch 核实其与 Regulation Z（12 CFR Part 1026, Appendix J，"actuarial method"）在计算逻辑上一致，并核对 CFPB 官方页面 "How do mortgage lenders calculate monthly payments?" 确认该公式为业内标准做法（CFPB 页面未给出公式细节但确认"standard mathematical formula"）；PMI 部分引用 Homeowners Protection Act 1998（Public Law 105-216）78%强制终止/80%可申请取消两档阈值，独立 WebSearch 核实（NCUA/CFPB/Federal Reserve 三个独立信源交叉确认数字一致）。
- **测试**：`tests/mortgage.test.ts` 15 个用例，期望值用 Python 独立按公式计算（非拿实现输出回填），含教科书公认案例（$200,000@6%/30年月供≈$1,199.10，多个财经站点/CFPB 关联页面反复引用的标准例子）、多组本金/利率/年限组合、零利率边界（月供=本金/期数）、`remainingBalance` 多个还款期数点、`calculateMortgage` 的 escrow 折算（税/保险/PMI/HOA 折月并入总月供）。
- **组件**：`MortgageCalculator.tsx`，8 个输入项（房价/首付%/利率/期限 Segmented[15/20/30yr]/税/保险/PMI/HOA，后 4 项均标 optional 且默认不参与计算），复用既有 `NumberField`/`Segmented` 原语，未写任何原生表单控件。
- **独立 agent 审核**（怀疑视角，全程 WebSearch 未用 WebFetch）：formula authority／HPA 数字／15 个测试用例中 9 个抽样独立复算／内容差异化（对比 cd-calculator 与 bmi-calculator 确认非套模板）／humanizer（em-dash 密度）／GEO 自包含性／worked example 与两张参考表数字内部一致性，六项全部 PASS，结论 **PUBLISH AS-IS**。**过程中发现的真实问题并已修复**：初稿正文 em-dash 密度达 14 处/1227 词（约 1/88 词），远高于站内基线 weight-converter 的 3 处/1436 词（约 1/479 词）——审核前自查阶段已发现并手工替换为句号/逗号/分号/括号，降到 0 处（sources 标签内 3 处属正常，非正文）；审核报告额外指出两处措辞可再精确化（"regulators use [公式] to define interest" 略有过度断言、"well past year 20" 应为"shortly after year 20"，21 年而非 22+ 年跌破 50%），均已按建议改写。核对结果显示教科书案例、15年/30年对比、7 行年度余额表全部数字与独立 Python 复算逐一吻合（含 $24,271.44/年供的 $0.03 舍入差异，判定为月供先四舍五入到分再乘 12 导致的合理方法差异，非错误）。
- **验证**：`npm test` 251/251 全过（新增 15 个）；`npm run build` 35 个静态页面成功生成，含 `/mortgage-calculator/` 与 `/embed/mortgage-calculator/`；Browser pane 实测：默认值($400k/20%/6.5%/30yr)显示 $2,022.62 月供/$320,000 贷款/$408,142 总利息，与 Python 独立算值逐分吻合；加税$4,800+保险$1,200+HOA$50/月后显示escrow $550.00、总月供 $2,572.62，精确匹配(400+100+50=550)；切到15年+5.85%利率显示P&I $2,674.48、15年总利息$161,406，与页面内15v30年对比表完全一致；移动端375px无横向滚动；embed页正常渲染带"Powered by"归属链接；console无报错。
- **发布**：commit `29218e8` push origin main（只暂存本工具相关 6 个文件：`src/lib/mortgage.ts`、`tests/mortgage.test.ts`、`src/components/calculators/MortgageCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`.claude/launch.json`(新增，此前本仓库缺失该配置文件，dev server 无法通过标准方式起 preview)，未带上同仓库另一并发会话的未提交产出 `wikipedia-opportunities.md`/`gsc-index-submit-log.json`/`podcast-pitch-log.md`/`外链建设进度.json`/`外链执行日志.md`）；CF Pages 自动部署，轮询 3 次后确认 `https://calcbadger.com/mortgage-calculator/` 200 且含新工具标题；IndexNow 提交 `/mortgage-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：`mortgage calculator`（第9行）标 `[已用 2026-08-05]`，`amortization calculator`（第31行）标 `[已用 2026-08-05 并入mortgage-calculator页]`。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-05 — 第 12 个工具：Time Converter（commit `a963e11` + `2f2f543`）

- **slug** `time-converter`，**category** `Conversion`（跨类目混排：上一次发布 Finance，本次回到 Conversion）。
- **目标词 + 同页变体**：发布顺序总表里此前一直未选中的第 7/26/87/88/108/110/111/112/224 行共 9 个"how many X in a Y"时间单位换算词（`how many weeks in a year` 82.3万/月为最大词，`how many minutes in a year` 7.4万/月为最小），合计约 213.5万/月。支持 second/minute/hour/day/week/month/year 共 7 个单位，输入一次即显示其余 6 个单位（沿用 length/weight-converter 的 Select + convertAll 模式）。
- **公式权威来源**：秒/分/时/日/周之间的换算比例固定不变，引用 BIPM SI Brochure Annex 1（`bipm.org/en/publications/si-brochure/annex-1/time`，"accepted for use with the SI"列表，但仅正式收录 minute/hour/day 三项，week 未被 BIPM 正式列入，见下方独立审核发现）。月/年无固定天数（月 28-31 天、年 365/366 天），改用"平均格里高利月/年"：格里高利历自身的置闰规则（4 年一闰、整百年不闰、整 400 年再闰）使每 400 年恰好包含 146,097 天（US Naval Observatory Leap Years FAQ，`aa.usno.navy.mil/faq/leap_years` 独立核实"97 leap days...every 400 years"原文属实），据此精确推出平均年 = 365.2425 天 = 31,556,952 秒、平均月（年的十二分之一）= 30.436875 天 = 2,629,746 秒——均为该置闰规则的算术推论，非估算。
- **测试**：`tests/time.test.ts` 22 个用例，期望值用 Python 独立按 `146097×86400/4800`（月）、`146097×86400/400`（年）等公式重算后核对（含 SI 精确换算边界、平均年/月的天/时/周/月换算、`commonYearIn` 的 365 天常见年/366 天闰年"quick fact"数字），非拿实现输出回填。
- **独立 agent 审核**（怀疑视角，read-only，覆盖公式权威／测试期望值／页面内容数字／结构差异化／humanizer／GEO／代码正确性／自行跑测试和构建共 8 项）：测试期望值全部 22 条独立复算通过；`npm run build` 独立跑通，确认 `/time-converter/` 与 FAQPage schema 均在产物中；结构差异化确认非换壳（历法/置闰规则叙事与 length 的 1959 协议、weight 的 avoirdupois/troy 叙事均不同）；代码逻辑（单位列表/步进值/负数处理/切换单位重新表达）无 bug。**揪出 2 处真实问题并已修复**：① worked example"某人 30 岁"段落先给出精确数字（30×31,556,952=946,708,560 秒 vs 30×365×86,400 的 naive 对比），随后却把偏差量写成"约 7.5 天/约 648,000 秒"——独立复算得出真实偏差应为 946,708,560−946,080,000=628,560 秒=7.275 天，648,000 秒实为把前一句"约 7-8 个闰日"里的 7.5 误乘 86,400 得到，属于同段落内约数与精确数字张冠李戴，已改为"about 7.3 days worth of seconds (around 628,560 seconds)"，此为教训库 `L-0805-7` 的复发（已在库内追加复发记录，未达 2 次升级阈值）；② 参考表"Exact conversions"把 minute/hour/day/week 四行数字统一挂了一条"Fixed by definition (BIPM SI Brochure)"的说明，但 BIPM 官方"accepted for use with the SI"清单只正式收录前三项，week 是通行历法惯例而非 BIPM 条目——数字本身无误，属于引用范围过度延伸（citation-scope overreach），已改为分开说明并入库为新条目 `L-0805-10`。同时自查发现正文 em-dash 密度 20 处（远高于 length/weight 基线的 3 处），审核期间已同步收窄到 3 处，与站内基线持平。
- **验证**：`npx vitest run` 273/273 全过（含新增 22 个）；`npm run build` 37 个静态页面成功生成，含 `/time-converter/` 与 `/embed/time-converter/`，JSON-LD 三块（WebApplication/FAQPage/BreadcrumbList）均可正常 parse；Browser pane 实测：默认 1 day → 86,400 sec/1,440 min/24 hr/0.142857 wk/0.0328549 mo/0.00273791 yr，与 Python 独立算值一致；切到 Years(mean)输入 30 → 946,709,000 sec(6位有效数字)/10,957.3 day/1,565.33 wk/360 mo，与 Python 算值一致；embed 页正常渲染带"Powered by CalcBadger Time Converter"归属链接；移动端 375px 单列布局无横向滚动；console 无报错。切换单位下拉时发现自动化点击工具本身的坐标换算有偏差（screenshot 像素空间与实际 viewport 不一致导致首次点击落空），非产品 bug，改用 DOM 实测坐标后验证通过。
- **发布**：commit `a963e11`（工具本体：`src/lib/time.ts`、`tests/time.test.ts`、`src/components/calculators/TimeConverter.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件，未带上同仓库另一并发会话的未提交产出 `wikipedia-opportunities.md`/`gsc-index-submit-log.json`/`podcast-pitch-log.md`/`外链建设进度.json`/`外链执行日志.md`）+ commit `2f2f543`（`indexnow-submit-log.json`，含本次提交顺带补上的 mortgage-calculator 缺失记录）；CF Pages 自动部署，轮询 5 次（50s）后确认 `https://calcbadger.com/time-converter/` 200 且含修复后的 "628,560" 数字；IndexNow 提交 `/time-converter/`：Bing 200 / Yandex 202。
- **词库标记**：9 个变体词（第 7/26/87/88/108/110/111/112/224 行）均标 `[已用 2026-08-05 并入time-converter页]`。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-05 — 第 13 个工具：Concrete Calculator（commit `7c57d80` + `e08018f`）

- **slug** `concrete-calculator`，**category** `Construction`（跨类目混排：当天前两次分别发布 Finance/Conversion，本次切到 Construction，此前 Construction 只有 stair-calculator 一个同伴）。
- **目标词 + 同页变体**：发布顺序总表第 28 行 `concrete calculator`（55万/月，KD24，$2.51）+ 第 1502 行 `post hole concrete calculator`（1,000/月，KD4，$0.41）并入同一页——两者共用同一套体积/袋数公式，只是形状不同（矩形 vs 圆柱），做成同页的 slab/footing/wall 与 column/post-hole/tube 两种模式。
- **公式权威来源**：体积公式为初等几何（矩形 l×w×d、圆柱 π×r²×h），换算沿用 NIST SP 811（1 ft=0.3048 m 精确值，与 sqft/stair 等既有工具一致）。袋数公式的核心权威来源是 QUIKRETE Concrete Mix（Product No. 1101）技术数据表（2022年10月修订版）——**用 curl 直接下载 PDF 原文**（非转述二手网页）读取 YIELD 表：40 lb→0.30 ft³、50 lb→0.375 ft³、60 lb→0.45 ft³、80 lb→0.60 ft³；同一 PDF 的"Method for Setting Fence Posts"章节也是本页"围栏立柱 3× 直径/1/3 高度"说法的直接出处。
- **测试**：`tests/concrete.test.ts` 25 个用例，期望值全部手算/Python 独立复算（矩形与圆柱体积、单位换算、waste 加成、cu ft→cu yd、四种袋型 yield、袋数 ceil 逻辑、总重量），非拿实现输出回填。开发中实测踩到一个真实浮点坑：`bagYieldCuFt` 最初写成 `bagSize × 0.0075` 的推导公式，`60 × 0.0075` 在 JS 里算出 `0.44999999999999996` 而非 `0.45`，导致 `27 ÷ yield` 该等于整数 60 的地方算出 61（多算一袋）——已改成直接存 PDF 原始发布值的查表（`BAG_YIELD_CU_FT` 字面量），既避免浮点误差也更忠实于"抄数据表原文"而非"抄推导出的公式"；`bagsNeeded` 也加了 `-1e-9` 的 epsilon 兜底同类问题。
- **独立 agent 审核**（怀疑视角，实测下载 PDF 原文核对、独立复算参考表/FAQ 全部数字、跑测试和构建、检查组件边界情况）：QUIKRETE 四档 yield 与 NIST 换算全部逐字核对通过；25 个测试独立复算无回填痕迹；围栏立柱 3×/1/3 规则确认逐字出自 PDF 原文；组件单位换算无交叉接线错误；0/负数/空值输入均正确 fallback 到提示文案不产生 NaN；与站内其他 Construction/Home Improvement 工具对比确认非换壳复用。**揪出 2 处真实数字错误并已修复**：① 参考表"10×10 ft slab, 4 in thick"行的立方码列写成 `1.24 yd³`，独立复算 33.3333÷27=1.234568，应为 `1.23 yd³`（同一错误在 FAQ 里复现，一并改正）；② FAQ"加 5-10% waste 后约 56-62 袋"把 0% waste 的袋数（56）当成了 5% waste 的下限——独立复算 5% waste 应为 59 袋、10% waste 为 62 袋，已改为"59-62 bags"，此为教训库新条目 `L-0805-15`（计算器类工具的参考表/FAQ 数字必须逐格用公式重算，区间端点不能悄悄混用不同前提）。另有一处非阻塞但已顺手修复的 UX 问题：切到 Column/Post Hole 模式时直径复用了 slab 模式的 `unit`（默认 ft）和默认值 12，首次看到的是"直径 12 ft"这种荒谬的立柱——已拆出独立 `diameterUnit` 状态（默认 in）+ 默认值改为 10，切模式后显示"Diameter: 10 in"；围栏说明文字里"1/3 post height **above ground**"的"above ground"是审核发现的原文未写的限定语（PDF 原文只说"1/3 the overall post height"），已删除该限定语避免过度引申源文本。
- **验证**：`npm test` 298/298 全过（含新增 25 个）；`npm run build` 39 个静态页面成功生成，含 `/concrete-calculator/` 与 `/embed/concrete-calculator/`；Browser pane 实测：默认值(10×10ft slab/4in/10% waste/80lb)显示 1.358 cu yd/36.67 ft³/62 bags/4,960 lb，与 Python 独立算值一致；切到 Column/Post Hole 模式输入 10in 直径×24in 深×0% waste 显示 0.04 cu yd/1.09 ft³/2 bags/160 lb，与参考表"10 in dia. post hole, 24 in deep"行完全吻合；embed 页正常渲染带"Powered by CalcBadger Concrete Calculator"归属链接；移动端 375px 单列布局无横向滚动；console 无报错（唯一的 net::ERR_CONNECTION_CLOSED 属 dev server HMR websocket 重连噪音，与工具本身无关）。
- **发布**：commit `7c57d80`（工具本体：`src/lib/concrete.ts`、`tests/concrete.test.ts`、`src/components/calculators/ConcreteCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件，未带上同仓库另一并发会话的未提交产出 `wikipedia-opportunities.md`/`gsc-index-submit-log.json`/`podcast-pitch-log.md`/`外链建设进度.json`/`外链执行日志.md`）+ commit `e08018f`（`indexnow-submit-log.json`）；CF Pages 自动部署，轮询 3 次后确认 `https://calcbadger.com/concrete-calculator/` 200 且 curl 实测含修复后的"1.23 yd"和"59-62 bags"字样；IndexNow 提交 `/concrete-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：`concrete calculator`（第 28 行）与 `post hole concrete calculator`（第 1502 行）均标 `[已用 2026-08-05 并入concrete-calculator页]`。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-06 — 第 14 个工具：Percentage Calculator（commit `149f574` + `339708a`）

- **slug** `percentage-calculator`，**category** `Math`（新类目，跨类目混排：上一次发布 Construction）。
- **目标词 + 同页变体**：发布顺序总表第 29 行 `percentage calculator`（55万/月，KD32，$2.67）为主词，并入 13 个同簇词：第 155 行 `percentage increase calculator`、第 402 行 `percent off calculator`、第 839 行 `percent difference calculator`、第 1166 行 `percentage of a number calculator`，以及 9 个具体数值查询（797/800/933/1195/1253/1300/1301/1347/1391 行，`20 percent of 50`/`what is 20 percent of 80`/`20 percent of 100`/`what is 15 percent of 100`/`what is 10 percent of 50`/`15 percent of 200`/`30 percent of 150`/`10 percent of 100`/`25 percent of 80`）。做成 4 模式单页：Basic percentage（三角关系 part/percent/whole 互解）、Percentage change、Percent off（折扣）、Percent difference（对称比较）。9 个具体数值查询用"Common percent of a number results"参考表逐条覆盖（非做成独立 FAQ），另加一张 percent/decimal/fraction 速查表顺带覆盖 percent-to-decimal/decimal-to-percent/fraction-to-percent 三个未主动选中的相邻词。
- **公式权威来源**：基础三角关系（part = percent÷100×whole）源自"percent"一词本身的定义（"per hundred"），不依赖需要单独核实的外部换算标准；仍引用 BIPM SI Brochure 第 9 版 §5.4.7"Quantities with the unit one"作为 % 符号用法的权威出处——**用 curl 下载 PDF 原文**并用 `pypdf` 抽取第 147 页文本逐字核对，原文确认"The internationally recognized symbol % (per cent) may be used with the SI... the symbol % generally takes the meaning of 'parts per hundred'"，且**特意核对了这不是 §5.4.7 之外那张更严格的 Table 8「Non-SI units」清单**（该清单只收录 minute/hour/day/litre/tonne 等单位，不含 %）——这正是 `time-converter` 曾踩过的 citation-scope overreach 同款风险（那次错误地把 week 归进了 accepted-units 表），本次开发阶段就有意识地在 `percentage.ts` 顶部注释里写清楚"不是那张表"以避免复发，独立审核确认未复发。Percent difference 公式 `|a-b|÷((a+b)/2)×100` 及其与 percent change/percent error 的区别经 WebSearch 多信源交叉确认（Statistics How To 等）。
- **测试**：`tests/percentage.test.ts` 32 个用例，覆盖 6 个公式函数（percentOfWhole/partAsPercentOfWhole/wholeFromPartAndPercent/percentChange/applyDiscount/percentDifference）及除零边界（whole=0、percent=0、old=0、a+b=0）与负数场景（负 whole、负 old→new、负 discount 表示 markup），期望值全部手算（含 2/11、2/9 等循环小数用分数形式断言）。
- **UI 决策记录**：4 模式选择器最初用 Segmented（短按钮行），实测 4 个多字词标签（如"Percent difference"）在 2 列宽（~351px/4≈82px 每项）会被 CSS `text-overflow: ellipsis` 截断成"% Differ…"，移动端单列布局下（is-wide media query 560px 以下失效）情况更差（"Find the P…"/"Find the W…"）——用浏览器 `canvas.measureText` 实测确认字符预算后，把 Calculator 与 Solve for 两个选择器均改用 Select（下拉菜单，站内既有约定：短选项用 Segmented、长/多选项用 Select，BmiCalculator 的 Standard 选择器是先例），改完后任意宽度下标签均完整显示不截断。
- **独立 agent 审核**（怀疑视角，Python 独立重算 referenceTables/FAQ/sections 全部数字、下载 BIPM 原始 PDF 核对引用范围、跑测试、检查 UI 边界）：所有数字（含 $79.99×30%off、$100→$50→$75 复利陷阱示例、10g/12g 实验室秤对比、30%叠加20%折扣、$80→$60 股价示例）逐一复算无误；BIPM 来源逐字核对通过且未误引 Table 8；与 concrete-calculator 等姊妹工具对比确认结构非换壳复用；32 个测试独立跑通确认无回填痕迹。**揪出 2 处真实问题并已修复**：① `PercentageCalculator.tsx` 的 Discount 模式下，`NumberField` 的 `min`/`max` 只约束 +/- 步进按钮，不拦截键盘直接输入——输入折扣 >100% 会算出负售价，渲染成格式错误的"$-50.00"且"Amount saved"标签与实际方向矛盾；已加 `discountValid` 校验（originalNum≥0 且 discountNum≤100 才计算结果），越界时显示明确的"Discount can't exceed 100%..."提示，同时把"Amount saved/Amount added"标签改用 `amountSaved` 自身正负号判断（此前用 `discountNum` 正负号判断，边界情况下会错位）；② 折扣段落的"分位取整"举例（"23.997 vs 显示的 $24.00 导致店内贴纸和手算差一分钱"）经审核实算证明该例子本身不成立——因为原价本身已是整数分，`round(O-x) = O-round(x)` 对整数 O 恒成立，不可能出现所述偏差，已重写为准确说明"原价已是整数分时无论先四舍五入哪一步结果都相同，店内贴纸看起来更粗略是营销取整（就近整数）而非计算分歧"；另有一处 FAQ（"How do I find what percentage..."）末尾残留了 UI 重命名前的旧标签"Is what %"/"Find a Value mode above"，已同步改为当前的"Find the percent"/"Basic percentage mode"并去掉"above"这种依赖页面上下文的悬空引用。
- **验证**：`npx vitest run` 330/330 全过（含新增 32 个）；`npm run build` 42 个静态页面成功生成，含 `/percentage-calculator/` 与 `/embed/percentage-calculator/`，JSON-LD 三块均在产物中；Browser pane 实测四个模式：Basic 20%of50=10、Find the %（10 of 40）=25%、% Change（50→60）=+20%、% Off（$100 20%off）=$80 sale/$20 saved、% Diff（10 vs 12）=18.1818%，与 Python 独立算值逐位吻合；除零边界（-5 vs 5 diff、old=0 change）均显示清晰提示无 NaN；修复后的 150% 折扣与负价格越界均显示正确提示而非"$-50.00"；负值折扣（markup，$100 at -10%）显示"$110 sale / $110 Amount added"正确；375px 移动端下拉菜单标签完整不截断；embed 页 324px 实际高度，760px embedHeight 与站内约 2× 冗余惯例一致（concrete 392→780）；console 无报错（唯一 net::ERR_CONNECTION_CLOSED 为 dev server HMR 重连噪音）。
- **发布**：commit `149f574`（工具本体：`src/lib/percentage.ts`、`tests/percentage.test.ts`、`src/components/calculators/PercentageCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件）+ commit `339708a`（`indexnow-submit-log.json`）；CF Pages 自动部署，curl 轮询确认 `https://calcbadger.com/percentage-calculator/` 200 且含修复后的"Find the percent" option in a Basic percentage 字样；IndexNow 提交 `/percentage-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：14 个词（第 29/155/402/797/800/839/933/1166/1195/1253/1300/1301/1347/1391 行）均标 `[已用 2026-08-06 并入percentage-calculator页]`，已断言标记前后总行数不变（1627 行）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-06 — 第 15 个工具：Volume Converter（commit `08c4bc3` + `c0ef7d2`）

- **slug** `volume-converter`，**category** `Conversion`（跨类目混排：上一次发布 Math）。
- **目标词 + 同页变体**：发布顺序总表里此前一直未选中的 87 个纯体积单位换算词（不含 grams-based 密度相关词、水瓶/泳池/浴缸等容器容量类不同工具的词），从第 10 行 `how many ounces in a cup`（67.3万/月）起，覆盖 tsp/tbsp/floz/cup/pint/quart/gallon/ml/l 之间的全部"how many X in a Y"/"X to Y"变体，含 `how many gallons in a barrel`（42加仑石油桶，第1128行）。支持 9 个单位互转，输入一次即显示其余 8 个单位（沿用 length/weight/time-converter 的 Select + convertAll 模式）。
- **公式权威来源**：NIST Handbook 44 (2026) Appendix C 确认 1 US 加仑 = 3.785411784 升精确值；美制体积单位链条（1 gal=4 qt=2 pt=2 cup=8 floz=2 tbsp=3 tsp）均为该精确值的整数比例推导，非独立测量值。历史脉络（1707 Queen Anne wine gallon 231 立方英寸、1836 美国 Weights and Measures Act 采用、1824 英国 Imperial gallon 分道、1959 International Yard and Pound Agreement 定 1 in=2.54cm 精确值）经 WebSearch 多方核实（sizes.com 英制历史单位站等独立信源），231×2.54³=3785.411784 内部算术自洽核对通过。石油桶 42 美制加仑定义引自 EIA 官方 glossary。
- **测试**：`tests/volume.test.ts` 27 个用例，期望值全部用 Python Decimal 40 位精度独立复算（非拿实现输出回填），覆盖精确定义对（gallon-liter/tbsp-tsp/cup-floz/quart-cup/pint-cup/floz-gallon/floz-ml）、实际场景数值（750mL 酒瓶、355mL 罐装饮料、42加仑油桶、1.5floz 标准酒吧 shot）、边界情况（0/负数/同单位/极小极大值往返）与 convertAll 全量互查。
- **重要内容区分点**：正文专设一节"Fluid ounces measure volume, not weight"，明确本工具**不**做重量盎司↔体积互转（面粉/糖等密度依赖，无统一答案），避免与 weight-converter 的 oz 产生用户误用。
- **独立 agent 审核**（怀疑视角，先读教训库全文 342 行，逐条比对是否复发；覆盖公式权威／测试期望值／历史事实 WebSearch 核实／4 张参考表全量重算／与姊妹换算器结构差异化 difflib 量化比对／FAQ 数字一致性／组件是否偷偷暴露重量单位／UI 原语合规）：**8 项全部 PASS**，未发现任何编造、算错、模板复制或越权行为。发现 1 处轻微完整性缺口（非事实错误）：sources 列表原本只有 NIST HB44 + EIA glossary，正文引用的 1836 法案/1824 英国法案/1959 协议三条具体历史论断缺对应引用条目（对比 length-converter 对 1959 协议专设引用）——已补充 sizes.com 英制历史单位站作为第三条 source 关闭该缺口，重新跑测试+构建确认无回归。与姊妹换算器（temperature/length/weight/time）用 difflib.SequenceMatcher 量化比对正文相似度仅 4.3-4.8%（唯一重合片段是合法复用的 NIST HB44 引用标签+URL），非 L-0804-11 式换壳复用。
- **验证**：`npm test` 357/357 全过（含新增 27 个）；`npm run build` 44 个静态页面成功生成，含 `/volume-converter/` 与 `/embed/volume-converter/`，JSON-LD 三块（WebApplication/FAQPage/BreadcrumbList）均可正常 parse；Browser pane 实测：默认 1 cup → 48 tsp/16 tbsp/8 fl oz/0.5 pt/0.25 qt/0.0625 gal/236.588 mL/0.236588 L，与 Python 独立算值逐位吻合；切到 750 cup 验证 375 pt/177.441 L 正确；移动端 375px 单列布局无横向滚动；embed 页正常渲染带"Powered by CalcBadger Volume Converter"归属链接；console 无报错。
- **发布**：commit `08c4bc3`（工具本体：`src/lib/volume.ts`、`tests/volume.test.ts`、`src/components/calculators/VolumeConverter.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件）+ commit `c0ef7d2`（`indexnow-submit-log.json`）；CF Pages 自动部署，curl 轮询 3 次（60s）后确认 `https://calcbadger.com/volume-converter/` 200 且含审核后补充的 sizes.com 引用字样；IndexNow 提交 `/volume-converter/`：Bing 200 / Yandex 202。
- **词库标记**：87 个词（第 88/93/98/99/108/110/114-117/123/126/130-132/148/149/162/164/182-184/191/206/209/214/247-251/269/271/290/298/306/328/355/358/364/383/396/399/444/446/472/497/504/512/522/558/592/597/598/606/634/646/664/673/680/715/745/747/803/810/815/817/841/882/894/899/901/949/950/956/958/961/1024/1031/1057/1081/1126/1151/1154/1206/1228/1294 行)均标 `[已用 2026-08-06 并入volume-converter页]`，脚本断言标记前后总行数不变（1627 行）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-06 — 第 16 个工具：Date Calculator（commit `e55e629` + `8ba225a`）

- **slug** `date-calculator`，**category** `Date & Time`（新类目，跨类目混排：当天前两次分别发布 Math/Conversion）。
- **目标词 + 同页变体**：发布顺序总表第 24 行 `date calculator`（67.3万/月，KD31，$2.18）为主词，一次性并入整个「N days from today」纯日历天数簇（30/90/60/45/14/28/180/21/120/10/7/15/75/100/20/35/40/50/70/150/80/270/200/300/365，共 25 行，不含 business-day 变体——本工具只做纯日历天数加减，不含工作日排除逻辑，未实现的东西不敢声称覆盖）+ 4 个节日倒计时词（christmas/halloween/thanksgiving/new years）。做成 3 模式单页：Difference（两日期相差天数+年月日拆解）、Shift date（加/减 N 天）、Days until（四大节日实时倒计时）。
- **公式权威来源**：日期算术走 proleptic Gregorian calendar（ECMA-262 §21.4 Date Objects 定义），感恩节走 5 U.S.C. § 6103「11 月第四个星期四」法定规则（现场算法推导，非硬编码日期表）；年/月/日拆解公式额外引用 Microsoft DATEDIF 文档作为权威出处（闰年 2/29 周年这一具体分歧点上，DATEDIF 与 Python dateutil.relativedelta 给出不同答案——本工具遵循 DATEDIF 惯例，两种惯例的分歧已在正文和测试里明确说明，不是含糊带过）。
- **测试**：`tests/dateCalculator.test.ts`（含审核后新增 3 个回归测试共 45 个用例），所有 daysBetween/addDays/calendarDiff 期望值用 Python `datetime`/`dateutil.relativedelta` 独立复算（非拿实现输出回填）；感恩节 2026-2030 五年日期用 Python「11 月第四个星期四」独立算法核对。
- **独立 agent 审核**（怀疑视角，用 Python 独立复算全部公式/测试期望值/参考表数字、WebSearch 核实两条 sources 引用真实性、grep 全文查 AI 写作特征、跑 tsc 类型检查）：**揪出 1 处真实的阻塞级 bug 并已修复**——`dateCalculator.ts` 里 `epochDay`/`daysInMonth`/`weekdayIndex` 三个函数最初都用 `Date.UTC(year, ...)` 构造日期，而 JS 的 `Date.UTC()`/`Date()` 构造函数对 0-99 的两位数年份有一个隐蔽的历史特性：会静默重新解释成 1900+year（`Date.UTC(50,0,1)` 返回的是 1950 年而不是第 50 年）。这不是超出工具声明范围的边界情形——`isValidDate()` 明确接受年份 1-9999，UI 年份输入框 `min={1}`，正是在工具自己声明的有效范围内。审核实测「Difference 模式，从第 50 年 1 月 1 日到 2026 年 1 月 1 日」原本返回 27,759 天（悄悄按 1950→2026 算的），独立 Python 复算真实答案应为 721,719 天，偏差整整约 1900 年却完全没有任何报错或提示——是"算错数还装作没错"的典型死罪场景。修复方案：改用 `new Date(0)` + `.setUTCFullYear(year, month, day)` 构造日期（该方法没有两位数年份特殊处理，任何年份都按字面值处理），三处调用点全部替换，并新增 3 个回归测试把这个具体 bug 钉死（year 50 的 daysInMonth/daysBetween、year 1 的 weekdayIndex，期望值均为 Python 独立复算），杜绝静默复发。此外还发现并顺手修复了 2 处非阻塞问题：① 代码注释里"Thanksgiving 联合决议签署于 1941年11月26日"经 WebSearch 核实（National Archives/History.com）实际签署日应为 **12月26日**，11月26日恰好是本次 2026 年的感恩节日期，疑似未核实的猜测而非查证过的事实，已改正（该错误仅存在于代码注释，未影响正文/FAQ 面向用户的文案，正文原本就没写具体签署日期）；② `Shift date` 模式的天数偏移量没有对结果做范围回验——够大的偏移量能把结果推出工具自己声明的 1-9999 年范围但界面不会提示，已加 `isValidDate` 回验+越界提示"That shift lands outside year 1–9999"。另有 2 处风格性建议已采纳：补充 Microsoft DATEDIF 官方文档为第三条 source（此前正文三处提及但未列入 sources 数组）；正文 em-dash 密度（20/1449 词≈1.4%）明显高于姊妹工具（time-converter 0.22%、concrete-calculator 0.80%），挑了 5 处"陈述—解释"重复句式改成句号/冒号/分号，降到约 1.0%（未大改，审核明确说这些 em-dash 都是承载实质信息不是空洞填充，非阻塞）。第一轮审核用时约 8 分钟，全程正常推进未卡死，未触发看门狗降级流程。
- **验证**：`npx vitest run` 402/402 全过（含新增 45 个）；`npm run build` 47 个静态页面成功生成，含 `/date-calculator/` 与 `/embed/date-calculator/`；Browser pane 实测三个模式：Difference（2026-08-06→2026-09-05）=30 days/0y0m30d/4.3 weeks/Saturday，与 Python 独立算值吻合；Shift date（2026-08-06 +30d）=September 5, 2026 Saturday，-30d=July 7, 2026 Tuesday，均与 Python 独立算值吻合；Days until 四项（Halloween 86/Thanksgiving 112/Christmas 141/New Year's 148 天）与 Python 独立算值逐位吻合；**bug 修复后专项复测**：year 50→2026 差值页面实测显示 721,719 天/1976y 0m 0d，与 Python 独立算值完全一致；Shift date 越界提示（year 9999+1000 天）页面实测正确显示"That shift lands outside year 1–9999"；移动端 375px 单列布局无横向滚动，3 模式 Segmented 标签在窄屏下从会截断的"Add / subtract"改名"Shift date"后完整显示；embed 页 622px 实际内容高度，820px embedHeight 留有余量；console 无报错（唯一 net::ERR_CONNECTION_CLOSED 为 dev server HMR 重连噪音）。
- **发布**：commit `e55e629`（工具本体，含审核后修复：`src/lib/dateCalculator.ts`、`tests/dateCalculator.test.ts`、`src/components/calculators/DateCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件）+ commit `8ba225a`（`indexnow-submit-log.json`）；CF Pages 自动部署，curl 轮询 3 次（60s）后从 404 转为 200，确认 `https://calcbadger.com/date-calculator/` 及 `/category/date-time/`、`/embed/date-calculator/` 均正常且含修复后的"721,719"等实测字样；IndexNow 提交 `/date-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：30 个词（第 92/102/128/129/145/146/163/208/220/242/243/301/359/366/428/499/500/562/651-654/799/944/1008/1070/1213/1269/1427/1428 行，独立文件不在 git 版本控制下）均标 `[已用 2026-08-06 并入date-calculator页]`，Python 脚本断言标记前后总行数不变（1627 行）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-09 — content-quality-audit 审计更新：Molarity Calculator（非新发布，commit `4b85723`）

**本条为`trafficsite-content-quality-audit`定时任务审计已发布内容后的修复更新，不是新工具发布。**

- 审计对象：`molarity-calculator`（本站首批 6 工具之一，2026-08-02 上线）。此前 cd/square-footage/bmi/stair/sat-score 五个工具已各审过一轮（2026-08-02～08-05），molarity-calculator 是唯一从未被审计过的工具，本次按"last_audited 缺失优先"选取，是全站第 6 次审计。
- 独立 Python 重算 `src/lib/molarity.ts` 全部 6 个公式函数（molarity/massForMolarity/volumeForMolarity/molarMassFromMolarity/molesOfSolute/dilutionStockVolume）及 IUPAC 标准原子量算出的 6 种化合物摩尔质量，与 `tests/molarity.test.ts` 9 条期望值、页面参考表、3 处 worked example 全部吻合，未发现计算错误。
- **发现并修复 1 项**（经独立 agent 复核确认后才修改）：`description` 字段（复用为 meta description/og/twitter description/JSON-LD WebApplication.description/页面首屏可见导语共 5 处）**173 字符**，超出安全长度 160 约 13 字符（8.1%），落在本站既有先例的"不修"区间（cd 164/+4、sat-score 166/+6）与"应修"区间（square-footage/stair 207/+47、bmi 220/+60）之间的空档，非套用现成先例可直接判定，交独立 agent 复核后确认应修（截断点恰好落在"compoun[d]"词中间，且"built-in compound values"是真实差异化功能点而非填充词）。精简为 **147 字符**，保留全部四个可求解量（molarity/mass of solute/solution volume/molar mass）与内置化合物值功能点，"molar concentration"关键词覆盖仍完整保留在正文首节。
- 首次对本站工具页执行 2026-08-07 新规的英文双重去 AI 味检查（`Skill(humanizer)` + `Skill(avoid-ai-writing)`，此前 5 次审计均未覆盖这一步，是流程缺口）：全文无 em/en dash、无弯引号、无 Tier1/Tier2 AI 高频词、无套路句式，判定文本已足够干净未做改写；已记入 `content-audit-log.md` 提醒后续审计持续执行此项。
- 其余维度均未发现问题：18 个测试文件 421/421 通过（molarity 相关 9/9，其余为并发会话正在开发的 calorie-calculator 新增测试，未纳入本次改动）；内嵌组件（`/embed/molarity-calculator/` 与主页面复用同一组件）；引用来源（IUPAC Gold Book 403 确认为 Cloudflare 反爬网关而非死链，与此前 eCFR/Lancet 案例同款；PubChem 200）；GEO（`Skill(ai-seo)` Content Extractability Check 逐项核对，估计等效 90/99 左右）；内链健康度（本站工具数已从 stair 审计时的 9 个增长到本次的 16 个，独立模拟 `pickRelatedGuides`+`crossCategory` 逻辑验证 16/16 零孤儿，8/4 修复的轮转兜底在工具数增长 78% 后依然成立）；Schema 一致性；合规；AdSense 政策合规（ads.txt/`pub-5245502795720653`/Privacy/Terms/About 均正常）。
- **验证**：`npm test` 421/421 通过、`npm run build` 47 页成功生成后，`git diff` 确认 `src/data/tools.ts` 只有本行改动，只暂存该单一文件（未纳入并发会话正在修改的 `CalculatorIsland.astro`/`gsc-index-submit-log.json`/新增的 calorie 相关文件），commit `4b85723`；push 后轮询线上 `/molarity-calculator/` 的 meta description，第 3 次尝试（约 40 秒后）确认新文案已生效部署；`node tools/submit-indexnow.mjs /molarity-calculator/` 提交：Bing 200 / Yandex 202。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-09 — 第 17 个工具：Calorie Calculator（commit `64ee5e9` + `5a39d1f`）

- **slug** `calorie-calculator`，**category** `Health`（跨类目混排：前 4 次分别为 Construction/Math/Conversion/Date & Time，本次改选此前从未在近期出现过的 Health）。
- **目标词**：发布顺序总表第 40 行 `calorie calculator`（100万/月，KD79，$1.76）。做成单页三段式：BMR（静息代谢率）→ TDEE（活动系数调整后的维持热量）→ Goal（减重/维持/增重目标热量），三者链式计算，Lose/Maintain/Gain 三个按钮只改变 TDEE 之后怎么用，不改变 BMR/TDEE 本身。
- **公式权威来源**：BMR 用 Mifflin-St Jeor 方程（Mifflin MD, St Jeor ST et al., Am J Clin Nutr 1990;51(2):241-247），WebSearch 独立核实其在约 82% 受试者中命中实测静息能耗 ±10%（对比 Harris-Benedict 约 69%），优于后者是有独立数据支撑而非泛泛而谈；活动系数 1.2/1.375/1.55/1.725/1.9（sedentary→extra active）核实为运动营养学界广泛采用的惯例值，非政府标准，页面正文明确说明这一点不含糊；"3,500 kcal ≈ 1 lb"目标热量换算规则溯源至 Wishnofsky 1958 年的线性近似，同时引用 NIH/NIDDK Body Weight Planner（Kevin Hall 团队）研究说明该规则会随时间高估实际减重效果（代谢适应导致实际减重比线性预测慢）；安全下限提醒（女性<1200 kcal/day、男性<1500 kcal/day 触发醒目提示）核实精确对应 2013 AHA/ACC/TOS 肥胖管理指南原文表述。
- **测试**：`tests/calories.test.ts` 19 个用例，期望值全部手算并用 Python 独立复核（含公制/英制两条路径的换算一致性、5 档活动系数、lose/maintain/gain 三种目标模式、性别安全下限边界）。`tools.ts` 内 3 张参考表（BMR-by-profile/TDEE-by-activity/goal-calories-by-rate）数字同样经 Python 独立复算。
- **独立 agent 审核**（怀疑视角，先读教训库，逐条对照公式权威性/测试期望值独立性/UI 逻辑正确性/去 AI 味/来源 URL 有效性五个维度）：**揪出 1 处真实的 UI 状态 bug 并已修复**——"Lose"档位可选速率 [0.5,1,1.5,2] 与"Gain"档位可选速率 [0.25,0.5,0.75,1] 只有 {0.5,1} 重叠，切换 goal 时 `rate` 状态未联动重置；用户若先在 Lose 下选 1.5 或 2 lb/week 再切到 Gain（该值不在新列表里），`Select` 组件的 `findIndex` 兜底会让下拉框静默显示成第一个选项（0.25 lb/week），但计算函数仍在用切换前的旧速率值，导致界面显示的"已选速率"和实际参与计算的数字对不上。修复方式：新增 `handleGoalChange` 包装函数，goal 变化时检查当前 `rate` 是否仍在新目标的速率列表里，不在则重置为新列表第一项，确保显示值与计算值任何时候都一致。该 bug 属于 UI/状态管理问题（非事实性错误），已作为新条目 `L-0809-2` 追加进 `独立站/内容通用教训库.md`（判据：级联下拉框状态不联动重置，换个站换个表单同样会踩）。其余四个维度全部 PASS：来源引用、测试期望值独立性、去 AI 味（em dash/AI 高频词/模板复用检查）、三条 sources URL 逐条 curl 核实有效。
- **比较级/首创性表述核查**：跑 `check_comparatives.py` 命中 1 句（"a few weeks of tracking"），逐句核对后确认是通用建议措辞，非涉及具体两个数值的比较级断言，判定为脚本误触发，不需要改写。
- **SEO 字段离群值核查**：跑 `check_seo_field_stats.py --new-slug calorie-calculator`，title（46字符，z=0.85）与 description（141字符，z=-1.31）均在全站正常分布范围内，非离群值。
- **验证**：`npm test` 421/421 全过（含新增 19 个）；`npm run build` 49 个静态页面成功生成，含 `/calorie-calculator/` 与 `/embed/calorie-calculator/`；Browser pane 实测：默认档位（female/30/150lb/5'6"/lightly active/lose 1lb-week）BMR=1,417/TDEE=1,949/target=1,449，与 Python 独立算值逐位吻合；切到 Maintain 档位后 target 正确变为 TDEE 本身（1,949）且速率选择器正确隐藏；weight=100lb 场景触发安全下限提示（1,137 cal/day < 1,200 女性下限），文案正确显示引用 2013 AHA/ACC/TOS 指南且无 em dash；Goal 三选一 Segmented 加 `wide` 属性后不再截断显示"Mai..."；移动端 375px 单列布局无横向滚动；embed 页正常渲染带"Powered by CalcBadger Calorie Calculator"归属链接；console 无报错（唯一 net::ERR_CONNECTION_CLOSED 为 dev server HMR 重连噪音，非应用问题）。
- **发布**：commit `64ee5e9`（工具本体：`src/lib/calories.ts`、`tests/calories.test.ts`、`src/components/calculators/CalorieCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件，未纳入并发任务改动的 `broken-link-outreach-log.md`/`gsc-index-submit-log.json`）+ commit `5a39d1f`（`indexnow-submit-log.json`）；CF Pages 自动部署，curl 轮询 4 次（约80秒）后从 404 转为 200，确认 `https://calcbadger.com/calorie-calculator/` 正常且含"Mifflin-St Jeor"字样；IndexNow 提交 `/calorie-calculator/` 与 `/embed/calorie-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第 40 行标 `[已用 2026-08-09]`。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-09 — 第 18 个工具：World Clock & Time Zone Converter（commit `581aa7d`）

- **slug** `world-clock`，**category** `Date & Time`（跨类目混排：上一个是 Health，本次改用发布顺序总表里体量最大的一整簇未使用词——79 条 `time in <城市/国家>` 长尾，单页吸收而不是逐词建页）。
- **选题依据**：发布顺序总表第 16 行起零散分布着 79 条 `time in X` 词（第 16/55/80/96/137/144-146/159/188/189/195/201/209/238-240/251-253/295-298/322/323/329/330/346/352/353/374/379-382/423/441/448/470/496-500/516/529/530/545/546/577/578/591-594/607-609/623/666/677/694/722/731/742/743/814/815/826/867/876/884/994/1006/1078/1142/1151/1262 行），合计体量可观但单条 KD/CPC 都不足以单独建页；做成一个「World Clock（实时时钟）+ Convert a time（跨时区换算）」双模式工具页，79 个城市/国家词全部作为该页的 `queries` 覆盖对象，而不是拆成 79 个独立页——避免"单一模板换城市名批量生产"这条本站硬红线（同模板换词批量生产会触发 Google Scaled Content Abuse 检测）。
- **公式权威来源**：不用任何硬编码 UTC 偏移表，改为运行时通过浏览器内置的 `Intl.DateTimeFormat`（ECMA-402 规范）实时读取 IANA 时区数据库（Olson database）——与操作系统/浏览器用的是同一数据源，天然随夏令时规则变化自动更新，不会像存量偏移表一样"过期而无人发现"。核心算法：`offsetMinutes()` 用"同一时刻在目标时区格式化后的墙上时间 - 该时刻的 UTC 值"这一 Intl 标准取偏移量的技巧；`getDstInfo()` 用当年 1 月与 7 月两个采样点的偏移量取 max/min 判定夏令时偏移与标准偏移（南北半球通用，因为夏令时永远是"时钟调快"即数值更大的偏移，不分南北半球）；`wallTimeToUtc()` 用两轮迭代解决"墙上时间→UTC"的夏令时边界问题。
- **事实性内容独立核实**（WebSearch，非凭记忆）：印度标准时间 UTC+5:30 起源于 1906 年（英属殖民当局否决了原拟的两个整点时区 GMT+5/GMT+6 方案，取中间值），伊朗已于 2022 年 9 月起废除夏令时固定 UTC+3:30，豪勋爵岛（Lord Howe Island）是全球极少数夏令时只调 30 分钟（而非整小时，UTC+10:30↔+11:00）的地区之一——三条均为 WebSearch 独立核实后才写入正文，非凭训练知识直接下笔。
- **测试**：`tests/worldClock.test.ts` 19 个用例，预期值全部用 Python 标准库 `zoneinfo`（读取同一份 IANA tzdata，与本项目实现完全独立的另一套运行时）独立算出后写入测试文件作为断言依据，覆盖：纽约/悉尼夏令时偏移量、印度全年无夏令时、菲尼克斯全年无夏令时、UTC 偏移标签格式化（整点/半点/45分）、纽约→东京/洛杉矶→悉尼/奥克兰→火奴鲁鲁三组跨日期变更线换算（含向前跨一天、向后跨一天两种方向）、同区转换零位移、印度半小时偏移导致的小数小时差、往返转换一致性、79 城市 timeZone 字段全部能被 `Intl.DateTimeFormat` 接受且无重复 id。
- **独立 agent 审核**（怀疑视角，先读教训库，逐条对照公式/测试期望值/差异化/去 AI 味/来源/组件正确性六个维度）：**揪出 1 处真实的显示 bug 并已修复**——`WorldClockConverter.tsx` 的"World clock"模式 UTC 偏移量展示直接用 `dstOffsetMinutes / 60` 拼字符串，而不是调用已测试过的 `formatOffsetLabel()`，导致半小时/45分钟时区显示成十进制小时而非标准记法：印度显示"UTC+5.5"（应为"UTC+5:30"）、尼泊尔"UTC+5.75"（应为"UTC+5:45"）、纽芬兰"UTC-3.5"/"UTC-2.5"（应为"UTC-3:30"/"UTC-2:30"），且直接与正文教读者认识"+5:30"记法的内容自相矛盾。修复：改为调用 `formatOffsetLabel(nowDst.isCurrentlyDst ? nowDst.dstOffsetMinutes : nowDst.standardOffsetMinutes)`。修复后 Browser pane 实测印度档位正确显示"UTC+5:30"+"Not observed here"。其余五个维度全部 PASS：三条独立验证的事实性内容（印度1906年史/伊朗2022年废除夏令时/豪勋爵岛30分钟夏令时）WebSearch 复核无误；三张参考表数值与 Python 独立复算逐位吻合；三条跨日期变更线 worked example 用 Python `.astimezone()` 独立复现完全一致；与 date-calculator/time-converter 两篇既有页面对比确认段落结构/切入角度均不相同（本页专讲时区/夏令时/IANA 溯源，非套用日期算术或时长换算页的说明文框架）；正文与 FAQ 无 em dash（sources 数组里的破折号是全站引用格式惯例，date-calculator 同样如此）、无 AI 高频词、无"it's not X, it's Y"句式；5 条 sources URL（IANA/TC39 ECMA-402/两条 Wikipedia/timeanddate.com）经 WebSearch 逐条确认真实存在；组件全程使用站内 NumberField/Select/Segmented 三原语，无原生 `<input type="number">`/`<select>`。
- **比较级/首创性表述核查**：跑 `check_comparatives.py` 未命中任何触发词；正文里"one of the very few"（豪勋爵岛 30 分钟夏令时）这类比较级表述已在独立 agent 审核环节单独用 WebSearch 核实过表述准确（用词是"one of the few"而非"only"，避免了脚本覆盖不到的未经核实的绝对化表述）。
- **SEO 字段离群值核查**：跑 `check_seo_field_stats.py --new-slug world-clock`，title（36字符，z=0.17）与 description（178字符，z=0.33）均在全站正常分布范围内，非离群值。
- **验证**：`npm test` 440/440 全过（含新增 19 个）；`npm run build` 51 个静态页面成功生成，含 `/world-clock/` 与 `/embed/world-clock/`；Browser pane 实测：World clock 模式默认纽约档位实时秒表跳动且 UTC-4/夏令时"In effect now"与实际系统时间吻合；Convert 模式默认伦敦→东京，09:20 BST → 17:20 JST/8小时差，与手算吻合；bug 修复后印度档位专项复测显示"UTC+5:30"+"Not observed here"（此前是"UTC+5.5"）；移动端 375px 单列布局无横向滚动；embed 页正常渲染带"Powered by CalcBadger World Clock & Time Zone Converter"归属链接；console 无报错（唯一 net::ERR_CONNECTION_CLOSED 为 dev server HMR 重连噪音，非应用问题）。
- **发布**：commit `581aa7d`（工具本体：`src/lib/worldClock.ts`、`tests/worldClock.test.ts`、`src/components/calculators/WorldClockConverter.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件，未纳入并发任务改动的 `broken-link-outreach-log.md`/`gsc-index-submit-log.json`）；CF Pages 自动部署，curl 轮询 4 次（约75秒）后从 404 转为 200，确认 `https://calcbadger.com/world-clock/` 正常且含"UTC+5:30"字样；IndexNow 提交 `/world-clock/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 79 行 `time in X` 全部标 `[已用 2026-08-09 并入world-clock页]`（Python 脚本断言标记前后总行数不变）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-10 — 第 19 个工具：Tip Calculator（commit `524d116` + `60ea8ab`）

- **slug** `tip-calculator`，**category** `Finance`（跨类目混排：前一个是 Date & Time 的 world-clock）。选题依据：发布顺序总表第 18 行 `tip calculator`（45万/月，KD18，$0.83）是当天优先级最高的未用词，且未被标 `[⚠️权威壁垒风险]`。
- **红线预检**（2026-08-09 新增的选题锁定前权威壁垒检查）：本词属于"红线"里提到的 Google 原生 widget 词族（coin flip/timer/tip calculator 等），选中前先 WebSearch 实测 SERP——结果是 calculator.net/calculatorsoup.com/omnicalculator.com/wise.com/calculatestuff.com 等一批独立小型计算器站与 App，无银行/政府/单一品牌垄断迹象，通过检查，正常进入开发。
- **差异化设计**（避免"纯 widget 复刻"）：不是简单的"金额×百分比"单页计算器，加了三层深度：① 税前/税后哪个作为小费计算基数的礼仪争议（多数礼仪权威建议税前，但实测餐厅服务员更偏好税后，两种算法都算出来对比展示实际美元差额，不是只给一个数字）；② 人均分摊 + 按 $1/$5 就近凑整的"多付了多少"拆解；③ 六国小费文化对照表（美/英/法/日/中国大陆/澳）+ 美国按服务类型（餐厅堂食/外卖/酒吧/网约车/理发按摩/酒店客房清洁）的小费惯例参考表。
- **公式**：`tip = base × (tip percent / 100)`，`subtotal = total / (1 + taxRate / 100)` 从含税总额反推税前金额。全部为初等代数，无争议性公式选择问题（不像 CD calculator 的 APY/APR 或体脂率的多套公式那样需要"选定一个标准并明示"）。
- **权威来源**（6条，全部逐条 WebSearch 独立核实原文表述，非凭训练知识直接下笔）：Emily Post Institute（美国分服务类型小费惯例 + 税前基数的礼仪建议）；英国 legislation.gov.uk《Employment (Allocation of Tips) Act 2023》（服务费/小费须 100% 转给员工的法律要求）；法国经济部 DGCCRF（service compris 自 1987 年起法定含在菜单价格中）；日本官方旅游局 japan.travel（"tipping is not customary"原文表述）；澳大利亚 Fair Work Ombudsman（全国最低工资数字）；Lonely Planet 亚洲小费文化文章（中国大陆"不常见"的定性）。
- **测试**：`tests/tip.test.ts` 15 个用例，期望值全部用 Python 独立算出后写入测试文件（$106总额/6%税率反推$100税前金额+$6税额、20%小费税前$20 vs 税后$21.20、$5/$1 就近凑整边界、numPeople≤0 兜底为1 等），非拿实现输出回填。
- **独立 agent 审核**（怀疑视角，覆盖公式/来源准确性、测试期望值独立性、与姊妹工具结构差异化、去 AI 味/GEO/引用完整性四个维度）：**PASS 2 项，另 2 项发现真实但非阻塞的问题并已修复**——
  1. 公式/来源准确性：法国 1987 年"一个法令"的表述有轻微不精确（实际是同年 3 月+10 月两个相关法令，非单一法令），已把正文和 sources 里"a 1987 decree"改成"since 1987"回避单复数争议；发现"roughly 4-10% in most states"（美国州销售税范围）与"employers generally cannot keep tips"（联邦法律条款）两条正文断言在 sources 数组里找不到对应引用条目——已用 Tax Foundation 2026 州销售税数据（0%起~10%出头，全国人口加权均值约7.5%）替换掉原本凭印象写的"4-10%"区间，并新增美国劳工部 Fact Sheet #15A（FLSA小费所有权）作为 FAQ 里该条法律断言的引用来源；中国大陆"导游是常见例外"这一具体情节原本只挂在 Lonely Planet 条目下但该文章未必直接支持这个细节，已新增国泰航空官方旅行指南作为该细节的独立来源（该文章原文明确写"Tour guides represent a notable exception"）。
  2. 去 AI 味：正文首节"The part that trips people up is not the arithmetic, it is which number counts as the 'base.'"是教科书级"it's not X, it's Y"负向对仗句式，已改写为"The arithmetic is trivial. What actually causes confusion is which number counts as the 'base.'"两句直陈句。
  3. 测试期望值独立性：PASS，审核 agent 自己用 Python 独立复算了至少 5 组期望值（含 $48.15/1.07 精度边界、ceil 凑整等）逐位吻合，未发现"拿实现输出回填测试"的痕迹。
  4. 与姊妹工具结构差异化：PASS，与 cd-calculator/calorie-calculator 对比后判定"共享一个合理的说明文骨架但内容真实不同"（公式不同、引用机构不同、具体数字不同、tip-calculator 独有两段国别对比小节），非"换壳复用"。
  - 全部 4 处发现均已在收到审核报告后当场修复，改完重跑 `npx vitest run`（455/455 全过）+ `npm run build`（53 页成功）确认无回归，Browser pane 逐条核对新措辞已生效渲染（非缓存旧内容——本次踩了一次已知的 Astro dev server 缓存 tools.ts 陷阱，重启 dev server 后才看到最新文案）。
- **本次额外发现并顺手修复的 UI bug（审核agent范围外，自查发现）**：`TipCalculator.tsx` 里"自定义小费%"输入框有值时会覆盖预设的 15/18/20/25% 按钮参与实际计算，但 Segmented 预设按钮的高亮状态没有联动清除，导致界面看起来选中的是旧预设（如 20%）而实际计算用的是新输入的自定义值（如 22%），显示与计算脱节。曾尝试改 Segmented 的 `value` 传参来去掉高亮，但这会导致该控件所有选项 `tabIndex=-1`（Segmented 组件"未选中项 tabIndex=-1"的设计），使键盘 Tab 完全跳过这组控件——回退该方案，改为在结果区域上方加一行`calc-note`文案"Using custom X% instead of the Y% preset above"，不碰共享 UI 组件的可访问性行为。此外发现把这行提示包在一个新 `<div>` 里会打破 `.calc-grid` 的 CSS Grid 直接子元素依赖（`.calc-field.is-wide{grid-column:span 2}`只对 grid 容器的直接子元素生效），已改为把提示文案移到 `.calc-grid` 外部（与其余 calc-note 同级），不引入新的包裹层。
- **比较级/首创性表述核查**：跑 `check_comparatives.py` 未命中任何触发词（正文体量小、以公式和权威来源为主，命中为0属正常）。
- **SEO 字段离群值核查**：跑 `check_seo_field_stats.py --new-slug tip-calculator`，title（25字符，z=-0.58）与 description（173字符，z=0.10）均在全站正常分布范围内。
- **验证**：`npx vitest run` 455/455 全过（含新增 15 个）；`npm run build` 53 个静态页面成功生成，含 `/tip-calculator/` 与 `/embed/tip-calculator/`；Browser pane 实测：默认档位（$106总额/6%税率/2人/20%小费/税前基数）显示 $100.00 税前金额/$20.00 小费/$126.00 总计/$63.00 人均，与 Python 独立算值逐位吻合；切到"就近凑整到$5"人均变 $65.00 且提示"adds $4.00 total ($2.00 per person)"；自定义小费填22%后小费/总计/人均正确联动为 $22.00/$128.00/$65.00（凑整后）且显示"Using custom 22% instead of the 20% preset above"提示；移动端 375px 单列布局无横向滚动；embed 页正常渲染带"Powered by CalcBadger Tip Calculator"归属链接；console 无报错（仅 dev server HMR 重连噪音）。
- **发布**：commit `524d116`（工具本体：`src/lib/tip.ts`、`tests/tip.test.ts`、`src/components/calculators/TipCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，只暂存本工具相关 5 个文件）+ commit `60ea8ab`（`indexnow-submit-log.json`）；CF Pages 自动部署，curl 轮询 2 次（约35秒）后从 404 转为 200，确认 `https://calcbadger.com/tip-calculator/` 正常且含"Emily Post"字样；IndexNow 提交 `/tip-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第 102 行标 `[已用 2026-08-10]`（Python 脚本断言标记前后总行数不变，仍为 1632 行）。
- 无推翻核心结论的情形，未触发 Owen 邮件升级。

## 2026-08-10 — 第 20 个工具：GPA Calculator（commit `f666d27`）

- **slug** `gpa-calculator`，**category** `Education`（跨类目混排：前一个是 Finance 的 tip-calculator）。选题依据：发布顺序总表严格从上往下核查，第 6/12/16/18/21/22 位（cursive alphabet/snow day calculator/sign language alphabet/barcode generator/how many miles is a 5k/yes or no wheel）均未标 `[已用]` 但逐一评估后判定不适合本次单次运行产出合格工具（cursive alphabet 实测 SERP 意图是手写体字母表参考图而非文本生成器，需要专业书法字体资产；snow day calculator 缺乏权威公式来源，是非正式创作者算法；sign language alphabet 需要 26 个手势插图素材；barcode generator 需要多种条码格式的官方编码表实现，算错风险高），跳到第 23 位 `gpa calculator`（82.3万/月，KD42，$5.65，A层旗舰计算器）——公式权威、无需图片资产、可纯前端实现，符合"宁可跳过也不要交半成品"原则。
- **选题锁定前权威壁垒检查**：WebSearch 核实 SERP 前几名结果类型（gpacalculator.net/collegeboard/princetonreview/gpanerd 等），均为通用计算器站与教育机构信息站混合，无银行/制药/单一品牌垄断迹象，通过检查。
- **差异化设计**：本站首个"可变行数课程列表"UI 模式（此前 19 个工具均为固定字段）——新增 `.calc-course-row`/`.calc-course-list`/`.calc-row-remove` 三个 CSS 类，复用 `Select`/`NumberField`/`Segmented` 三个既有表单原语（未新造原生控件）。核心差异化：① Unweighted/Weighted 双模式一键切换，权重模式下每行额外出现 Regular/Honors/AP-IB 三态 Segmented；② 加权 GPA 可超过 4.0 上限（AP课程A=5.0）在讲解和FAQ里明确解释这是设计特性不是四舍五入误差；③ FAQ 覆盖"Pass/Fail是否计入GPA"这类边界问题。
- **公式**：`GPA = Σ(grade points × credits) / Σ(credits)`，权重模式下 `grade points`额外加 `Honors +0.5 / AP-IB +1.0`。等级转分数表来自 College Board BigFuture《How to Calculate Your GPA on a 4.0 Scale》官方转换表（A+/A=4.0 … F=0.0），加权惯例明确标注"widely used convention, not a single universal standard"（引用 PrepScholar 等描述性来源，不冒充官方标准）。
- **测试**：`tests/gpa.test.ts` 12 个用例，期望值全部 Python 独立算出后写入（含课程加权平均、weighted示例4.1667精确复现PrepScholar公开范例的算法、credits≤0/NaN排除边界、加权GPA可超4.0天花板等）。
- **构建期真实 bug 教训**：第一轮机械化去 AI 味（em dash 替换为句号/冒号）时，用脚本把 curly apostrophe（’）批量替换成直引号（'），但没意识到本站 `tools.ts` 用**单引号定界符+curly apostrophe 规避转义**是既有约定（含撇号的字符串一律改用双引号定界符，如 tip-calculator 的既有写法），直接替换导致字符串定界符内出现未转义单引号，`npm run build` 报语法错误（babel parser "Unexpected token, expected ','"）。已改为按字符串是否含撇号动态选择单引号/双引号定界符（含撇号用双引号+转义内部双引号）重新生成整段 JS，构建恢复正常——这是本次运行内自查发现并当场修复，不是审核 agent 发现的。
- **独立 agent 审核**（怀疑视角：公式与来源核实、测试期望值独立复算、代码正确性、与姊妹工具结构对比、去AI味残留、GEO/FAQ质量、来源URL真实性）：**PASS，附一条非阻塞的措辞冗余问题**——"weighted不是官方唯一标准"这条免责声明在正文两段+参考表note+组件内calc-note共出现4次（个别都准确但整体重复），已采纳建议将组件内 `GpaCalculator.tsx` 的对应句子精简，只保留机制说明，去掉重复免责声明本身（页面正文的完整免责声明段落保留）。审核 agent 独立 WebSearch 复核了 College Board BigFuture 两条来源 URL 均为真实可查页面，且独立复算了两组测试期望值（4.1667/3.2538）与实现值逐位吻合。
- **比较级/首创性表述核查**：跑 `check_comparatives.py` 未命中任何触发词（正文以公式解释为主，命中为0属正常）。
- **SEO 字段离群值核查**：跑 `check_seo_field_stats.py --new-slug gpa-calculator`，title（16字符，z=-1.20）与 description（172字符，z=0.05）均在全站正常分布范围内。
- **验证**：`npx vitest run` 467/467 全过（含新增 12 个）；`npm run build` 55 个静态页面成功生成，含 `/gpa-calculator/` 与 `/embed/gpa-calculator/`；Browser pane 实测：默认4门课（A 3学分regular/B+ 4学分honors/A- 3学分ap/B 3学分regular）unweighted GPA显示3.48（Python独立验算3.4846...）、weighted显示3.87（验算3.8692...）与总质量分45.30/50.30逐位吻合；移除一行课程后weighted GPA正确重算为3.83（验算38.3/10）；移动端375px单列布局无横向滚动；embed页正常渲染；console无报错（仅dev server HMR重连噪音，非本次改动引入）。
- **发布**：commit `9fa8cfb`（补记漏提交的第19个工具Tip Calculator发布日志）+ commit `f666d27`（工具本体：`src/lib/gpa.ts`、`tests/gpa.test.ts`、`src/components/calculators/GpaCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`）；CF Pages自动部署，curl轮询7次（约105秒）后从404转为200，确认`https://calcbadger.com/gpa-calculator/`正常且含"College Board"字样；IndexNow提交`/gpa-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第106行`gpa calculator`标`[已用 2026-08-10]`（Python脚本断言标记前后总行数不变，仍为1632行）。
- 无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-10 — 第 21 个工具：Reaction Time Test（commit `e16b5ce`，含发布后修正 commit）

- **slug** `reaction-time-test`，**category** `Games`（跨类目混排：前一个是 Education 的 gpa-calculator）。选题依据：发布顺序总表第6/12/16/18/21/22位当天早些时候已被上一次运行评估过并跳过（记录见gpa-calculator条目），第23位gpa-calculator已用，跳到第31位`reaction time test`（135,000/月，KD12，$2.50，B层）——无同类目冲突、无需图片/字体资产、可纯浏览器JS实现，判定优先于第24位`degree symbol`（与词库里其余单符号词属同一模板结构，有规模化生产doorway page风险，本次选择性跳过留待未来批量差异化处理）。
- **差异化设计**：本站首个"计时类交互测试"UI模式（此前20个工具均为数字输入型计算器）——新增`.rt-box`系列CSS类（idle/waiting红/go绿/early红三态），复用既有`Segmented`原语做3/5/7次试验选择，无原生表单控件。核心机制：`performance.now()`计时+1.2-3.5秒随机延迟防止倒计时预判+提前点击判定为false start不计入均值但单独计数展示。
- **⚠️ 首轮草稿引用事故（独立audit agent发现，已完整修正）**：首轮撰写时WebSearch多次独立查询都稳定返回"Kosinski (2008)：健康年轻成年人平均反应时间约250ms，190-300ms区间"这一表述（reaction-time-test.io/reactscore.com/kordu.tools等多个第三方网站转载几乎相同措辞），据此写入coreSummary/`classifyReactionTime()`四档分类/参考表/3条FAQ，并将"在线浏览器测试比实验室设备慢"这一论点误挂到反驳Woods et al. (2015)的商榷文章"The Victorians were still faster than us"（PMC4542533）上。独立审核agent直接下载Kosinski文献PDF（Clemson University，页脚显示"Last updated: September 2013"而非流传的"2008"）逐字核对18页/66,985字符全文，确认完全不含"250""190-300"或"healthy young adults"；同时抓取PMC4542533全文确认无"browser"/"online"/"consumer"字样，判定FAIL。本次运行内自行下载并逐字核对了同一份PDF与Woods et al. (2015)全文（PMC4374455）加以复核，确认审核结论成立，改用直接核对过的原文数字全面重写：190ms（Kosinski引述Galton，非计算机测量基准值，"约120年"惯例）、268ms（Kosinski引述Eckner et al. 2010计算机测量NCAA football players结果）、231ms原始均值/213ms硬件校正均值（Woods et al. 2015，n=1,469人，18-65岁）、0.55ms/年龄效应（同一研究，主要归因于运动输出减慢而非处理速度下降）；`classifyReactionTime()`四档阈值同步改为190/213/268ms并更新对应测试期望值。第二轮范围复核agent额外发现coreSummary与FAQ各一处"and cite a further study..."在语法上误挂到Woods et al.身上（应属Kosinski引述），已改写为两句独立陈述句消除歧义；顺手修正Kosinski文献引用年份错误（"2008"→按PDF页脚实际标注"last updated September 2013"）及"NCAA athletes"→"NCAA football players"措辞精度问题。已入库通用教训 `内容通用教训库.md` L-0810-2（多个独立WebSearch查询一致收敛≠已核实，可能是全网转载同一未经核实的讹传）。
- **测试**：`tests/reactionTime.test.ts` 15个用例，mean/median/stdDev/summarizeTrials手算验证，classifyReactionTime边界值（190/213/268ms）逐档核对。
- **比较级实算**：跑`check_comparatives.py`未命中触发词，但因本文正文含多处具体数字比较（190/213/268/231ms等），已额外逐句独立核对每个数字直接对应原始文献文本（见上方"首轮草稿引用事故"段落），非仅依赖脚本判定为0命中。
- **SEO字段离群值核查**：跑`check_seo_field_stats.py --new-slug reaction-time-test`，title（39字符，z=0.37）与description（163字符，z=-0.38）均在全站正常分布范围内（description首轮240字符被判离群值已缩短）。
- **独立agent审核**：两轮。第一轮（怀疑视角：来源核实/测试期望值独立复算/代码正确性/结构差异化/去AI味/GEO）判定FAIL并给出具体引文核对证据；第二轮范围复核（仅复核修正后的引文归属）判定FAIL一处剩余语法误挂问题，其余四项核实通过；两轮均在本次运行内完成，未触发独立agent卡死看门狗协议。
- **验证**：`npx vitest run`482/482全过（含新增15个）；`npm run build`57个静态页面成功生成，含`/reaction-time-test/`与`/embed/reaction-time-test/`；Browser pane完整实测trial流程（3次试验全部走完：等待→绿色→点击→下一轮→summary面板，均值/最佳/中位数/标准差与手算一致）、移动端375px无横向滚动、embed页正常渲染带归属链接；console无报错（仅dev server HMR重连噪音）。
- **发布**：commit `8310273`（补记漏提交的gpa-calculator IndexNow日志）+ commit `e16b5ce`（工具本体，已包含第二轮范围复核发现的两处引文归属修正：`src/lib/reactionTime.ts`、`tests/reactionTime.test.ts`、`src/components/calculators/ReactionTimeTest.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`）+ commit `c11a132`（`indexnow-submit-log.json`）；CF Pages自动部署，curl轮询5次（约75秒）后从404转为200，确认`https://calcbadger.com/reaction-time-test/`正常且不含PMC4542533/Victorians残留引用；IndexNow提交`/reaction-time-test/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第114行`reaction time test`标`[已用 2026-08-10]`（Python脚本断言标记前后总行数不变，仍为1632行）。
- 无推翻核心结论的情形（引文修正未影响工具本身的计时/统计逻辑正确性），未触发Owen邮件升级。

## 2026-08-10 — content-quality-audit 审计更新：Coin Flip Simulator（本条为审计更新，非新发布）

- **slug** `coin-flip-simulator`，`trafficsite-content-quality-audit`任务17:00档次审计。
- **发现**：13维度审计中12项无问题（公式独立Python复算全部吻合、116/30个相关单测全过、NIST来源link 200、schema一致、内链/GEO/合规均正常）；唯一问题是meta description长达221字符（远超~155-160字SERP截断阈值），独立复核agent确认为真实问题。
- **修复**：缩短description至152字符，不改动正文/公式/测试。
- **验证**：`npm run build`成功，`npm test -- coinFlip`30个测试全过；CF Pages自动部署，curl轮询3次后确认线上description已更新为新文案。
- **IndexNow**：提交`/coin-flip-simulator/`：Bing 200 / Yandex 202。
- 无推翻核心结论情形，未触发Owen邮件升级。

## 2026-08-10 — 第 22 个工具：Cursive Alphabet（commit `8906987`）

- **slug** `cursive-alphabet`，**category** `Reference`（本站首个新类目，跨类目混排：当天前两个工具依次是 Education 的 gpa-calculator 与 Games 的 reaction-time-test）。选题依据：发布顺序总表第6位`cursive alphabet`（368,000/月，KD4，$0.08，B层），排在第33位的`tdee calculator`之前——核查发现tdee calculator早已被`calorie-calculator`页(`src/lib/calories.ts`的`tdee()`函数+"Maintenance calories (TDEE) by activity level"章节)覆盖，词库已回填标记`[已用 2026-08-02 并入calorie-calculator页]`（原标记遗漏，本次补记）。
- **本站首个非数值型工具**：与此前21个"多输入数字计算器"结构性不同，无核心数学公式——是一个"生成器/参考表"型B层工具：文本输入实时预览成手写体风格（自托管 Google Font "Dancing Script"，SIL OFL-1.1），配套完整A-Z/a-z字母参考图。刻意不采用Unicode "mathematical script"码位方案（那是一套完全不同、用于花体文字生成器的Unicode区块，与美国课堂手写体教学字母无关，且复制到别处不会保留花体样式，只会显示成目标网站自己的字体）——WebSearch核实真实搜索意图后确认"cursive alphabet"绝大多数指向"学习手写体"而非"花体文字生成器"，因此选用真实手写体字体渲染方案。
- **可测试逻辑**：`src/lib/cursiveText.ts`的`sanitizePracticeText()`（字母/空格/撇号/连字符白名单+空白折叠+trim+24字符截断），10个vitest用例覆盖含边界情形。
- **内容差异化**：连接笔画机制（under-curve/over-curve/上下环笔画，源自WebSearch核实的Wikipedia "Cursive"词条描述）、"没有单一官方手写体标准"（五种教学流派对比表：Palmer Method/Zaner-Bloser/D'Nealian/Getty-Dubay/New American Cursive，每个流派独立配WebSearch核实过的来源）、Common Core与手写体教学的关系（刻意不引用具体州数量，因2026年各来源数字互相矛盾且逐年变化，Wikipedia自身引用的"25"也已过时）。
- **独立agent审核**：怀疑视角复核，PASS但抓到5个真问题并已全部修复：① `src/lib/cursiveText.ts`导出的`PRACTICE_ROW_OPACITIES`（早期设想的打印练习字帖功能残留）在组件里从未被使用，仅自身测试引用自身——判定为死代码，已连同其测试块一并删除；② 教学流派对比表5行仅1行（Zaner-Bloser）被现有3条来源真正覆盖，其余4行（Palmer/D'Nealian/Getty-Dubay/New American Cursive）无引用支撑（内容本身经独立核实无编造，但来源列表制造了"全部来自Wikipedia"的错误印象）——已为这4个流派逐一补充WebSearch核实过的独立来源（3条Wikipedia+1条New American Cursive官网），来源总数从3条增至7条；③ coreSummary开头"The chart below shows..."与一条FAQ答案结尾"(see the comparison table above)"含页面位置指代，破坏GEO字段的独立可提取性（对比同站gpa-calculator/reaction-time-test的coreSummary与FAQ均从不使用"above/below"）——已改写为不依赖页面位置的独立表述；④ `sanitizePracticeText`只折叠内部连续空白，未trim首尾空白（"  Mary-Jane O'Neil!! 123  "会残留首尾各1个空格），且原测试套件未覆盖此边界——已加`.trim()`并补充回归测试；⑤ 组件内`maxLength={24}`硬编码魔法数字未复用`cursiveText.ts`的常量——已改为`export const MAX_LENGTH`并在组件与提示文案两处引用。
- **humanizer 复查（第二轮，独立agent审核之后追加）**：`Skill(humanizer)`复核抓到5处未被前序审核agent标记的em dash（正文4处段落内+组件calc-note文案1处），均已改写为句号/冒号/括号消除；`Skill(avoid-ai-writing)`复核未发现额外问题（AI高频词表、rule of three、vague attribution、confidence calibration等类别均未命中，符合本站GPA/reaction-time-test姊妹页面已建立的中性参考体register）。来源标签里的"Wikipedia — "标题""格式em dash是全站既有的引用标签排版惯例（GPA/reaction-time-test等页面同款），未改动。
- **比较级实算**：跑`check_comparatives.py`未命中触发词（讲解正文以描述性内容为主，非数值比较，命中为0属正常）。
- **SEO字段离群值核查**：跑`check_seo_field_stats.py --new-slug cursive-alphabet`，title（42字符，z=0.60）与description（164字符，z=-0.19）均在全站正常分布范围内。
- **验证**：`npx vitest run` 492/492全过（含新增10个）；`npm run build` 60个静态页面成功生成，含`/cursive-alphabet/`与`/embed/cursive-alphabet/`；Browser pane实测：输入"Owen123!@# Zhang"正确过滤为"Owen Zhang"并实时渲染Dancing Script花体、52字母参考图完整渲染无缺字、移动端375px单列布局无横向滚动、embed页正常渲染带"Powered by CalcBadger"归属链接；console无报错（仅dev server HMR重连噪音，非本次改动引入，已用网络请求列表交叉核实排除字体加载失败等真实错误）。
- **发布**：commit `8906987`（工具本体+独立审核5处修复+humanizer二次修复：`src/lib/cursiveText.ts`、`tests/cursiveText.test.ts`、`src/components/calculators/CursiveAlphabetGenerator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`、`package.json`新增`@fontsource-variable/dancing-script`依赖）；CF Pages自动部署，curl轮询4次（约60秒）后从404转为200，确认`https://calcbadger.com/cursive-alphabet/`正常且含"Cursive Alphabet"标题；IndexNow提交`/cursive-alphabet/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第89行`cursive alphabet`标`[已用 2026-08-10]`，第116行`tdee calculator`补记`[已用 2026-08-02 并入calorie-calculator页]`。
- 无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-11 — 第 23 个工具：Fraction Calculator（commit `2c759b4`）

- **slug** `fraction-calculator`，**category** `Math`（与前一天最后一个发布的Reference类目`cursive-alphabet`跨类目区分）。选题依据：发布顺序总表第64行`fraction calculator`（368,000/月，KD43，$1.65，A层）+ 第207行`mixed fraction calculator`（18,100/月，A层）并入同页——查过带分数计算逻辑天然覆盖后者，不需要独立页面。选题前WebSearch核实SERP：结果为mathwarehouse/brighterly/mometrix/mathpapa/calculatorfraction/onlinefractionscalculator等独立小站与内容站混合，无制药/银行类机构权威壁垒（不同于本站已发布的`molarity-calculator`踩过的坑）。
- **三模式设计**：加减乘除（每个分数可带整数部分，覆盖"mixed fraction calculator"意图）/化简到最简形式（同时给出小数与百分比）/互转（带分数↔假分数↔小数三向）。`src/lib/fractions.ts`纯函数模块：`gcd`（欧几里得算法）、`simplifyFraction`、`addFractions`/`subtractFractions`/`multiplyFractions`/`divideFractions`、`mixedToImproper`/`improperToMixed`、`fractionToDecimal`/`fractionToPercent`、`decimalStringToFraction`（按字符串小数位数精确转换，不走浮点数近似）。
- **公式权威来源**：加减法遵循 Common Core State Standards for Mathematics 五年级 Number & Operations—Fractions 领域（5.NF.A.1，页面正文逐字引用官方例题"2/3 + 5/4 = 8/12 + 15/12 = 23/12"）；乘法遵循5.NF.B.4；一般分数除法（非仅限单位分数）遵循六年级 The Number System 领域6.NS.A.1（**独立审核agent抓到的问题**：最初误引5.NF.B.7，该标准实际只覆盖"单位分数÷整数/整数÷单位分数"，不覆盖本工具实现的任意分数互除，已改引6.NS.A.1，`src/lib/fractions.ts`头注释与`tools.ts`来源列表均已修正）；化简/GCD遵循欧几里得《几何原本》第七卷命题2（"greatest common measure"，经典算法原始出处）。
- **测试**：`tests/fractions.test.ts` 45个vitest用例（独立审核后从33个增至45），期望值均手算/独立验证（含CCSS官方例题2/3+5/4=23/12、-0边界（`improperToMixed`对`-3/4`正确返回`whole:0`而非`whole:-0`）、非有限数入参防护（`simplifyFraction(NaN,...)`拒绝而非陷入`gcd`无限循环）、独立审核补充的负数四则运算与分母为0防护用例（此前仅`simplifyFraction`单测覆盖符号处理，四则运算组合场景未测））。
- **独立agent审核**：怀疑视角复核，抓到2个真问题+1个测试覆盖缺口，均已修复：① `FractionCalculator.tsx`「化简」与「假分数→带分数」两个模式下，分子留空时错误提示始终显示"Denominator cannot be 0."（无论实际是分子还是分母的问题），浏览器实测复现——已加`fractionInputNote()`辅助函数按实际留空/非法字段给出对应提示，重新实测确认修复；② 5.NF.B.7引用范围夸大，见上条公式来源修正；③ 四则运算的负数组合与`a.denominator===0`/`b.denominator===0`防护此前零测试覆盖，已补12个用例。
- **humanizer + avoid-ai-writing 双重检查**：初稿正文/来源标签共发现13处em dash（含标题"Eighths ladder — fraction, decimal, percent"），全部改写为句号/冒号/括号；1处"genuinely"空洞强调词已删除；UI组件内"Fraction A — whole"等字段标签与两处提示文案的em dash同步改为冒号/句号（这些是用户可见的渲染文本，同样纳入检查范围，不只查`tools.ts`正文）。
- **验证**：`npx vitest run` 537/537全过（含新增45个）；`npm run build` 62个静态页面成功生成，含`/fraction-calculator/`与`/embed/fraction-calculator/`；Browser pane实测三种模式：2 3/4 − 1 1/2 = 5/4 = 1 1/4 = 1.25（与手算一致）、8/12化简→2/3=0.666667=66.6667%、0.75小数→3/4分数、桌面端Select下拉三个转换方向标签完整显示（**修复了一处真实UI bug**：`Segmented`控件在1280px桌面视口下"Mixed → improper"等标签被截断为"Mixed → imp..."，改用`Select`组件后完整显示，与本站`percentage-calculator`处理长标签的既有模式一致）、移动端375px单列布局无横向滚动、embed页正常渲染带"Powered by CalcBadger"归属链接；console报错仅为dev server HMR重连噪音（`ERR_CONNECTION_CLOSED`），与本次改动无关。
- **发布**：commit `2c759b4`（工具本体+独立审核2处修复+测试补强：`src/lib/fractions.ts`、`tests/fractions.test.ts`、`src/components/calculators/FractionCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`）；CF Pages自动部署，curl轮询4次（约60秒）后从404转为200，确认`https://calcbadger.com/fraction-calculator/`正常且含"Fraction Calculator"标题；IndexNow提交`/fraction-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第147行`fraction calculator`标`[已用 2026-08-11]`，第290行`mixed fraction calculator`标`[已用 2026-08-11 并入fraction-calculator页]`；改动前后行数断言1632不变。
- 无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-11 — 第 24 个工具：Time Duration Calculator（commit `9fcf2b8`）

- **slug** `time-duration-calculator`，**category** `Date & Time`（与当天更早发布的Math分类`fraction-calculator`跨类目区分）。选题依据：发布顺序总表第65行`time duration calculator`（135,000/月，KD22，$0.00，A层）。选题锁定前WebSearch核实SERP：Clockify/Omni Calculator/Calculator.io/timeanddate.com/Inch Calculator/gigacalculator/calculator.net等独立工具站与通用计算器站混合，无银行/制药/单一机构垄断迹象，通过权威壁垒检查（结果里出现的Inch Calculator等属于本站词库标注的"同类深耕竞争"名单，但那份名单只覆盖C层单值换算词，本工具是A层多输入计算器且功能域是钟点时间算术而非单位换算，判定不适用同一限制）。
- **与站内姊妹工具的边界核实**：确认本工具与已发布的`date-calculator`（仅整日历天数算术，无时分秒）、`time-converter`（纯单位换算，无跨天/跨夜逻辑）在功能域上互不重叠——三个工具分别对应"日期天数""时间单位换算""钟点时长算术"三种不同查询意图，未发现被现有页面覆盖需要合并的情况。
- **三模式设计**：本站首次在同一工具内组合"钟点算术+日历算术"——① Between two times：两个钟点间经过时长，若结束时间≤开始时间自动判定跨夜（不返回负数）；② Add/subtract：从一个钟点加减一段时长，报告是否跨天及跨几天；③ Two full dates and times：两个完整日期+时间之间的经过时长（可跨多天，也可能为负），复用`dateCalculator.ts`已验证过的`daysBetween`纯函数而非重新实现日历运算。`src/lib/timeDuration.ts`：`clockTimeToSeconds`/`secondsToClockTime`（含负数与超24h环绕）、`to12Hour`/`from12Hour`（12/24小时制互转）、`durationBetweenTimes`（跨夜规则）、`shiftTime`（加减+跨天进位）、`durationBetweenDateTimes`（签名化，含`negative`标记，沿用`dateCalculator.ts`的`CalendarDiff.negative`既有约定）。
- **公式权威来源**：分钟=60秒/小时=60分钟/天=86,400秒引自BIPM SI Brochure Annex 1（与本站`time-converter`已引用的同一来源，两者共享同一事实基础，非重复凑数）；日期时间模式的proleptic Gregorian calendar约定引自ECMA-262 §21.4（与`date-calculator`已引用的同一来源）。
- **测试**：`tests/timeDuration.test.ts` 22个vitest用例，期望值全部用独立Python脚本手算后写入（未看实现输出）：9:15→17:45=8h30m/8.5h、22:00→6:00跨夜=8h、9:00→9:00相同时刻=24h整（非0）、23:30+2h跨天`daysRolled=1`→01:30、1:00−3h跨天`daysRolled=-1`→22:00、2026-01-01 09:00→2026-01-03 09:00=48h、2026-01-05 10:00→2026-01-01 10:00=负96h。
- **UI bug（本次运行内自查发现并修复，独立审核前）**：顶层"Calculator"模式选择器最初用`Segmented`三选项（Between two times/Add/subtract/Two date-times），1280px桌面视口下长标签被截断为"Between two..."和"Two date-tim..."（与`fraction-calculator`8/11当天早些时候踩过的同一类Segmented截断bug同源）——已改用`Select`组件（跟进`fraction-calculator`刚建立的先例：长标签用`Select`不用`Segmented`），Browser pane复测确认三个选项完整显示且下拉可正常选择切换。
- **独立agent审核**：怀疑视角复核四个维度（公式与测试期望值独立性、与姊妹工具结构差异化、去AI味+GEO质量、来源真实性），**PASS，附一条非阻塞的GEO措辞建议**——参考表"Example elapsed times"的note字段末尾"per the convention explained above"依赖页面位置指代，审核agent独立WebSearch+WebFetch核实BIPM/ECMA-262两条来源URL均可访问且内容与引用的具体说法（分钟/小时精确比例、proleptic Gregorian calendar约定）吻合；审核agent独立用Python从零重算了全部测试边界值与正文数字（含参考表15/30/45分钟对应0.25/0.50/0.75小时、8:30 AM→12:15 PM=3h45m等未在测试文件里出现的额外验算），逐位吻合，未发现"拿实现输出回填期望值"的痕迹；顺手指出`timeDuration.ts`头注释写"BIPM SI Brochure §4.1"但`sources`数组正确写的是"Annex 1"，注释与实际引用不一致（仅代码注释非用户可见，非阻塞）。两处发现均已收到报告后当场修复：note字段改写为不依赖位置的独立表述，代码注释改为"Annex 1"与正文一致。
- **验证**：`npx vitest run` 559/559全过（含新增22个）；`npm run build` 64个静态页面成功生成，含`/time-duration-calculator/`与`/embed/time-duration-calculator/`；Browser pane逐模式实测：9:00 AM→5:00 PM=8h0m/8小时/480min；10:00 PM→6:00 AM显示"The end time is at or before the start time..."跨夜提示+正确算出8h0m；11:30 PM加1h30m（原始默认值测试）及后续11:30 PM加2h0m=1:30 AM+"1 day later"；切24小时制9AM/5PM正确显示为9/17且结果不变；2026-08-11 09:00→2026-08-12 17:00（日期时间模式默认值）=1d8h0m/32小时/1,920min；反转日期后2026-08-11 09:00→2026-08-05 17:00显示−5d16h0m/−136小时/−8,160min+"The end date-time is earlier than the start date-time."提示；移动端375px单列布局无横向滚动；embed页正常渲染带"Powered by CalcBadger Time Duration Calculator"归属链接；console报错仅为dev server HMR重连噪音（`ERR_CONNECTION_CLOSED`），与本次改动无关。
- **比较级实算**：跑`check_comparatives.py`命中3句，均为工具自身逻辑描述（"结束时间≤开始时间则跨夜"），非外部事实断言，已通过22个独立验证的单测确认逻辑正确，无需额外来源核实。
- **SEO字段离群值核查**：跑`check_seo_field_stats.py --new-slug time-duration-calculator`，title（16字符，z=-1.17）与description（188字符，z=1.10）均在全站正常分布范围内。
- **发布**：commit `9fcf2b8`（工具本体：`src/lib/timeDuration.ts`、`tests/timeDuration.test.ts`、`src/components/calculators/TimeDurationCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，含独立审核后的note字段与代码注释修复）+ commit `4bcda2d`（`indexnow-submit-log.json`）；CF Pages自动部署，curl轮询5次（约75秒）后从404转为200，确认`https://calcbadger.com/time-duration-calculator/`正常且含"Time Duration Calculator"标题；IndexNow提交`/time-duration-calculator/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第148行`time duration calculator`标`[已用 2026-08-11]`；Python脚本断言标记前后总行数不变，仍为1632行。
- 无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-11 — 第 25 个工具：Greek Alphabet（commit `6cd10d4`）

- **slug** `greek-alphabet`，**category** `Reference`（与当天更早发布的Date & Time分类`time-duration-calculator`跨类目区分）。选题依据：发布顺序总表第30行`greek alphabet`（368,000/月，KD27，$0.06，B层），未标权威壁垒风险，与前一日类目不同，选中。
- **同页变体合并**：扫描全表发现`pi symbol`（第54行）、`delta symbol`（第48行）、`sigma symbol`（第119行）、`alpha symbol`（第125行）四个独立符号查询词的搜索意图已被本页覆盖——本页对每个符号都有专属FAQ条目（含Unicode码位、含义、"Search 'xxx' above to find and copy"跳转指引），非泛泛提及，判定为真实覆盖而非凑数合并，四行均标`[已用 2026-08-11 并入greek-alphabet页]`。
- **组件设计**：`src/lib/greekAlphabet.ts`导出`GREEK_LETTERS`（24字母：name/uppercase/lowercase/finalForm(仅sigma)/upperCodePoint/lowerCodePoint/pronunciation/commonUse）+ `filterGreekLetters()`（按名称/字形精确匹配/用途文本过滤）。`GreekAlphabetGenerator.tsx`：搜索框+可点击复制的字母网格（沿用`CursiveAlphabetGenerator.tsx`已验证过的"B层生成器/参考工具"模式，新增`.greek-cell`系列CSS，点击后1.2秒"Copied!"反馈+navigator.clipboard写入失败静默降级）。
- **数据无公式可验，验证方式为Unicode码位与语言学规则交叉核实**：本工具不涉及数值计算，`tests/greekAlphabet.test.ts`15个vitest用例改为核实数据完整性——24字母数量/首尾字母/名称唯一性、大写码位U+0391–U+03A9（含U+03A2保留空位不被使用）、小写码位U+03B1–U+03C9（含U+03C2最终形式sigma专用空位不出现在常规小写序列中）、每个字形字段与其声明码位逐一比对、sigma是唯一含`finalForm`的字母且等于U+03C2、`filterGreekLetters`按名称/字形精确匹配/用途文本过滤的8个场景。写测试过程中两次断言写错（误以为"PI"只匹配Pi，实际"damping"含"pi"子串；误以为小写码位无空隙，实际U+03C2专属最终形式导致常规小写序列同样有跳过）——均为测试断言本身的错误，非实现bug，已改正断言。
- **事实核实（单信源交叉核实规则适用）**：403 BCE雅典在执政官Eucleides任内正式采用Ionic字母表——WebSearch交叉核实Wikipedia「Eucleides」词条+雅典卫城博物馆官网+学术论文(Archinus, Eucleides and the reform of the Athenian Alphabet)三方独立信源一致；Phi Beta Kappa 1776年12月5日创立于威廉玛丽学院、系首个采用希腊字母命名的社团——交叉核实Phi Beta Kappa官网历史页+Lake Forest College+Elon University等多所高校分会独立信源一致；William Jones 1706年首次使用π、Euler 1730-40年代推广——交叉核实MacTutor数学史站+History Today+Mental Floss三方独立信源一致；digamma/qoppa/sampi三个古字母退出常用字母表但保留作希腊数字6/90/900——Wikipedia三词条独立信源确认。Unicode码位（大写U+0391–U+03A9含U+03A2保留空位、小写U+03B1–U+03C9、sigma最终形式U+03C2）交叉核实codepoints.net/compart.com/Unicode.org官方PDF码表三方一致。
- **humanizer + avoid-ai-writing 双重检查**：初稿正文共发现10处em dash，全部改写为句号/逗号/括号/冒号（`sources`数组的引用标签em dash按本站既有格式惯例保留，未改动）；组件`aria-label`模板字符串中1处em dash（用户不可见但屏幕阅读器可读的文本）同步改为冒号。检查后未发现Tier1/2/3禁用词、curly quotes、rule-of-three、hollow intensifier等其他AI写作特征。
- **比较级实算**：跑`check_comparatives.py`未命中触发词；人工通读全文的"only letter with two lowercase forms"「sigma是唯一有最终形式的字母」「first true vowel letters」等唯一性/首创性表述均已通过vitest数据测试（sigma唯一性）与史学信源交叉核实（希腊字母首创真元音字母）确认成立。
- **SEO字段离群值核查**：跑`check_seo_field_stats.py --new-slug greek-alphabet`，title（38字符，z=0.40）与description（179字符，z=0.56）均在全站正常分布范围内。
- **浏览器实测**（dev server 4334端口）：搜索框输入"standard deviation"正确过滤仅剩Sigma一项；点击字母卡片触发"Copied!"反馈（用JS注入验证，因1.2秒超时窗口截图易错过）；移动端375px单列布局2列网格无横向滚动；`/embed/greek-alphabet/`正常渲染24字母网格+"Powered by CalcBadger Greek Alphabet"归属链接；console报错仅为dev server HMR重连噪音（`ERR_CONNECTION_CLOSED`，在无关的已发布`bmi-calculator`页同样复现，与本次改动无关）。
- **独立agent审核**：怀疑视角复核七个维度（事实准确性逐条重新WebSearch独立核实、代码正确性含`filterGreekLetters`逻辑、去AI味残留、与站内同类目`cursive-alphabet`结构化雷同风险、测试/构建健康度、SEO基础字段、关键词蚕食），**PASS，无阻塞项**——独立重新核实403 BCE/Phi Beta Kappa/William Jones三条史实与24个字母全部Unicode码位，逐一吻合；确认`greek-alphabet`与`cursive-alphabet`两个Reference分类工具页的章节结构（历史沿革/数理科学应用/sigma特例/联谊会命名史/输入方法 vs 用法/连笔规则/无统一标准/是否仍教授）与参考表内容（字母对照表 vs 教学流派对比表）真实不同，非换词模板；grep确认正文/FAQ区零em dash，仅`sources`标签保留（与作者自报一致）；核对词库文件确认5行合并标记准确无误。
- **发布**：commit `6cd10d4`（工具本体：`src/lib/greekAlphabet.ts`、`tests/greekAlphabet.test.ts`、`src/components/calculators/GreekAlphabetGenerator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`）+ commit `7636054`（`indexnow-submit-log.json`）；CF Pages自动部署，curl轮询3次（约30秒）后从404转为200，确认`https://calcbadger.com/greek-alphabet/`与`/embed/greek-alphabet/`均正常，正文含"Search or browse all 24 letters"；IndexNow提交`/greek-alphabet/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/候选六号站-计算器工具站词库_20260802.md` 第113行`greek alphabet`标`[已用 2026-08-11]`，第131/137/202/208行`delta/pi/sigma/alpha symbol`四行标`[已用 2026-08-11 并入greek-alphabet页]`；改动前后总行数断言1632不变。
- 本次运行内独立审核agent正常完成并返回报告（未触发全局CLAUDE.md看门狗规则里的卡死降级流程），无需说明。无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-11 — 内容质量审计修复：Temperature Converter（commit `8cd8495` + `7230ae3`）

- 本次为`trafficsite-content-quality-audit`定时任务的回头复核，非新工具发布。选中`temperature-converter`（published 2026-08-04）因为它是全站25个工具中last_audited缺失且发布日期最早的一个（此前已审计过cd/square-footage/bmi/stair/sat-score/molarity/coin-flip共7个，其余18个均未审计过，按SKILL.md"最早/缺失优先"规则选取）。
- **发现**：公式（°F=°C×9/5+32、K=°C+273.15）与三张参考表全部39个数值经Python独立重算逐格核对，均正确；574个测试全过；build正常生成`/temperature-converter/`与`/embed/temperature-converter/`；NIST SP811 PDF源链接现301跳转到NIST重组后的现行页面（`nist.gov/pml/special-publication-811`，标题吻合，非死链，未改动）；CDC来源链接403系Akamai机器人拦截，WebSearch独立核实内容仍在线且吻合，非真实失效。**唯一真实问题**：本工具2026-08-04发布，早于avoid-ai-writing技能生效日（2026-08-07），正文/表格/FAQ共含10处叙事性em dash（`sources`标签内2处引用惯例格式除外），经独立fresh-context agent核实确认（同站molarity-calculator/coin-flip-simulator审计时检查同类问题均为0处）。
- **修复**：`src/data/tools.ts`10处em dash改写为句号/逗号/冒号，未改动任何公式、数字或事实性表述；顺带发现并修复了`TemperatureConverter.tsx`组件内2处、`src/pages/[slug].astro`共享"Embed this calculator"模板内1处同类em dash（后者是全站25个工具页共用的模板文案，站点级修复，已用build产物核对确认所有工具页的嵌入区块文案同步生效）。
- **验证**：`npm test`574/574通过、`npm run build`66页成功；curl轮询确认`https://calcbadger.com/temperature-converter/`已生效部署新文案；`node tools/submit-indexnow.mjs /temperature-converter/`（Bing 200/Yandex 202），仅提交该URL（其余因侧栏/模板变动幅度小未逐一重新提交，沿用stair-calculator审计时的做法，避免被判定批量提交）。
- 无推翻核心结论的情形，未触发Owen邮件升级。完整审计13维度记录见`content-audit-log.md`。

## 2026-08-12 — Shape Volume & Surface Area Calculator（第26个工具，commit `d931f74`）

- **选题**：从`独立站/流量站词库/CalcBadger-关键词库.md`发布顺序总表第210行起找第一个未标记词，命中第214行`volume of a rectangular prism`（A层，月搜索90,500，KD1，无权威壁垒标记）。扫表发现同一"实体几何体积/表面积"意图簇还有第219行`surface area of a sphere`、第398行第315行`surface area of a cylinder`、第417行第334行`volume of a cylinder calculator`、第1476行第1393行`volume of a sphere calculator`、第1509行第1426行`volume of a cone calculator`（后两条带同类深耕竞争风险标记，非权威壁垒标记，不受选题跳过规则限制），六词判定为同一页面的真实覆盖（矩形棱柱/圆柱/球/圆锥各自的体积+表面积公式讲解与FAQ逐一覆盖，非泛泛提及），一并标记`[已用 2026-08-12 并入shape-volume-calculator页]`，改动前后总行数断言1637不变。
- **权威壁垒检查**：WebSearch核实`volume of a rectangular prism`/`surface area of a cylinder`/`volume of a cylinder calculator`三个代表词的真实SERP前8，结果被omnicalculator/calculatorsoup/mathopenref/cuemath/byjus等中小型独立计算器站与数学教育站占据，无药企/银行/医疗/政府/大型财经媒体品牌，判定为正常竞争型SERP而非权威壁垒。
- **公式研究**：四个立体图形公式（矩形棱柱V=lwh/SA=2(lw+lh+wh)；圆柱V=πr²h/SA=2πr(r+h)；球V=(4/3)πr³/SA=4πr²；圆锥V=(1/3)πr²h/SA=πr²+πrl，l=√(r²+h²)）均为无争议的初等几何公式，独立agent用WebSearch核实Wolfram MathWorld的Cuboid/Cylinder/Sphere/Cone四个页面URL真实可达且公式吻合（cone一项因MathWorld搜索摘要未直接返回公式文本、改用GeeksforGeeks/CalculatorSoup交叉印证，判定为"强但非最大化"验证，另加CalculatorSoup Cone Calculator作为第二来源）。
- **开发**：`src/lib/geometry.ts`导出8个纯函数（每种图形的体积+表面积，圆锥另有`coneSlantHeight`）；`tests/geometry.test.ts`13个vitest用例，期望值全部按代数关系独立推导为π的整数倍手算（如r=3,h=10圆柱→V=90π/SA=78π；r=5球→V=500π/3/SA=100π；3-4-5与5-12-13两组直角三角形分别验证圆锥斜高公式），未从实现输出回填；`ShapeVolumeCalculator.tsx`用站内既有`Segmented`（图形选择+单位选择）+`NumberField`（尺寸输入）原语，未新造控件。
- **浏览器实测替代方案**：本次运行为无人值守定时任务，Browser pane在此场景下不可用（`preview_start`报"Dev servers can't be started from unattended sessions"），改用`tests/ShapeVolumeCalculator.dom.test.tsx`（happy-dom + @testing-library/preact，沿用`CoinFlipCalculator.dom.test.tsx`已验证过的模式）实际走一遍点击图形单选→输入尺寸→读取渲染结果的真实DOM路径，5个用例覆盖默认矩形棱柱（3×4×5→60/94）、圆柱（r3h10→282.743/245.044）、球（r5→523.599/314.159）、圆锥（r3h4→37.699/75.398）、尺寸为0时的提示态，全部与独立手算吻合。
- **构建期发现并修复的em dash**：`npm run build`后对生成的静态HTML做逐字符扫描（而非只查`tools.ts`数据文件，吸取`temperature-converter`审计的教训），发现`ShapeVolumeCalculator.tsx`组件内`calc-note`提示文案有1处叙事性em dash（"radius, not diameter — if you only know..."），已改为冒号；`sources`数组引用标签内的5处em dash（Publisher — Title格式）与`<title>`标签的"Calculator — CalcBadger"分隔符均为全站既有模板惯例，未改动。
- **比较级实算**：跑`check_comparatives.py`未命中触发词；正文"one third of a cylinder"与"four times the area of its largest circular cross-section"两处均为可直接由公式代数验证的精确数学关系（非需外部信源佐证的经验性比较），已核实成立。
- **SEO字段离群值核查**：跑`check_seo_field_stats.py --new-slug shape-volume-calculator`，title（38字符，z=0.42）与description（158字符，z=-0.65）均在全站正常分布范围内。
- **独立agent审核**：怀疑视角复核七个维度（公式正确性独立核实、测试/参考表数值独立重算、来源真实性、与`concrete-calculator`/`volume-converter`结构雷同风险、em dash与AI写作特征、FAQ的GEO可提取性、站内链接），**PASS，无阻塞项**——独立重算圆柱/球/两组圆锥测试用例与参考表数值全部吻合无错位；对比`concrete-calculator`（浇筑体积+袋数）与`volume-converter`（1707年葡萄酒加仑史）确认本篇讲解角度（四公式+尺寸误用陷阱）无逐句雷同；确认正文/FAQ零叙事性em dash；对照`独立站/内容通用教训库.md`教训条目逐条核对，无复发项。
- **验证**：`npx vitest run`592/592通过（含新增18个：13单元+5 DOM交互）；`npm run build`68页成功生成，含`/shape-volume-calculator/`与`/embed/shape-volume-calculator/`。
- **发布**：commit `d931f74`（工具本体：`src/lib/geometry.ts`、`tests/geometry.test.ts`、`tests/ShapeVolumeCalculator.dom.test.tsx`、`src/components/calculators/ShapeVolumeCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`）；CF Pages自动部署，curl轮询3次（约30秒）后从404转为200，确认`https://calcbadger.com/shape-volume-calculator/`正文含"Rectangular Prism"与"MathWorld"字样、结构化数据含WebApplication+FAQPage；IndexNow提交`/shape-volume-calculator/`：Bing 200 / Yandex 202。
- 独立审核agent本次正常完成并返回报告（未触发全局CLAUDE.md看门狗规则里的卡死降级流程），无需说明。无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-12 — Yes or No Wheel（第27个工具，commit `ef7fa98`）

- **选题（含逐行跳过说明，2026-08-12新规则首次执行）**：从发布顺序总表上次已用行（第209行`percentage of a number calculator`，已并入`percentage-calculator`）往下逐行核对：第210行`how many miles is a 5k`（165,000/月，C层）——查`length-converter`页"Common race distances"参考表已有`5K, 5 km, 3.1069 mi`整行数据+对应FAQ，判定已充分覆盖，标`[已用 2026-08-12 并入length-converter页]`，正当跳过；第211行`yes or no wheel`（165,000/月，KD2，B层）——查全站tools.ts无任何决策轮盘/是否轮盘类工具，`coin-flip-simulator`虽同为二元加权随机但无"wheel"或"yes or no"字样，非充分覆盖，无正当跳过理由，**选中本行**；第212行`how many miles is 10000 steps`（135,000/月，C层，步数转里程）与第213行`how many miles is a 10k`——213同样查得`length-converter`表已有`10K, 10 km, 6.2137 mi`整行+FAQ，标`[已用 2026-08-12 并入length-converter页]`；212因涉及步幅假设、站内确认无步数转里程类工具覆盖，本次未选中（选题顺序上第211行已是首个无正当理由可跳过的候选，按规则必须选它，不能跳到212），212保持未标记，留给后续运行。类目：本工具`category: 'Games'`，与前一日`shape-volume-calculator`（Math）不同，满足跨类目轮换。
- **公式研究**：本工具核心不是行业权威公式，而是标准概率论——加权随机选择的累积权重法（Wikipedia "Inverse transform sampling"，WebSearch确认词条原文含"we simply add up the individual probabilities"描述与本工具的累积权重游走逻辑一致）+ 几何分布期望等待次数E[X]=1/p（Wikipedia "Geometric distribution"信息框确认mean=1/p）。两条来源均由独立审核agent实测WebFetch二次核实（HTTP 200，词条内容与引用主张吻合）。
- **开发**：`src/lib/yesNoWheel.ts`导出`normalizeWeights`/`spinWheel`（累积权重游走选段）/`simulateSpins`/`summarizeSpins`（计数+连续同段streak）/`expectedSpinsUntilFirst`（几何分布均值1/p）/`streakProbability`（p^k）/`segmentAngles`（角度切分）/`rotationForLanding`（纯几何：给定当前转盘角度+目标段中点角度+额外整圈数，算出应转到的绝对角度，避免指针停错段）。`tests/yesNoWheel.test.ts`34个vitest用例，期望值均为Python独立核算（如70/30轮盘连续三次No=0.35³≈4.3%、五五轮盘期望等待2次、角度公式手推验证），非从实现输出回填。`YesNoWheel.tsx`用站内既有`Segmented`（Yes/No与Yes/No/Maybe双模式切换）+`NumberField`（Chance of Yes滑块，仅二段模式生效）；转盘视觉用`conic-gradient`按各段角度画色块，指针固定顶部，点击后用`rotationForLanding`算出目标绝对角度触发CSS transform过渡（3.4s），靠监听转盘元素的`transitionend`事件回收结果（而非固定`setTimeout`，避免与CSS过渡时长产生漂移），另加4.5秒兜底计时器防止`prefers-reduced-motion`环境下transitionend不触发导致卡死。
- **浏览器实测替代方案**：本次运行为无人值守定时任务，`preview_start`同样报"Dev servers can't be started from unattended sessions"，改用`tests/YesNoWheel.dom.test.tsx`（happy-dom + @testing-library/preact，沿用`ShapeVolumeCalculator.dom.test.tsx`模式）实测点击Spin→按钮变"Spinning…"且disabled→对转盘元素派发合成`transitionend`事件模拟CSS过渡完成→结果显示且按钮恢复可点这条真实DOM路径，另用"Chance of Yes"设为100%/0%两个确定性用例验证`spinWheel`权重游走逻辑在真实组件里接线正确（不依赖Math.random的随机性断言）。
- **独立agent审核（发现真实bug并已修复）**：怀疑视角复核8个维度（公式数值独立重算、来源真实性WebFetch二次核实、与`coin-flip-simulator`结构雷同风险、humanizer+avoid-ai-writing双重检查、FAQ的GEO可提取性、测试覆盖健康度含跑`npm test`+`npm run build`、组件逻辑读码找bug、词库记账核对），**首轮FAIL**——发现真实可复现bug：`changeMode()`切换二段/三段模式时只重置`pendingIndex`未重置`spinning`，若在转盘动画未结束前切换模式，`transitionend`监听与兜底计时器的`pendingIndex !== null`判断永久为false，Spin按钮卡死在"Spinning…"状态且disabled，需刷新页面才能恢复；另发现`src/lib/yesNoWheel.ts`头注释里一句"从coinFlip.ts导入二项分布函数"与实际代码不符（该模块从未导入`coinFlip.ts`）。**已修复**：`changeMode()`补充`setSpinning(false)`并在代码注释里说明原因（避免复发）；头注释改为准确描述（只保留几何分布来源引用，删除失实的"导入自coinFlip.ts"表述）；补充1个DOM回归测试（切换轮盘状态前先点击Spin、不等transitionend就切模式，验证按钮不会卡死）。修复后重跑：`npm test`634/634通过、`npm run build`70页成功生成含`/yes-or-no-wheel/`与`/embed/yes-or-no-wheel/`。除该bug外，公式数值（65/35轮盘、三段等分、六档bias参考表共12组数据）、两条Wikipedia来源、与`coin-flip-simulator`的差异化（后者聚焦二项分布组合数学与"硬币是否作弊"显著性检验，本工具聚焦几何分布等待次数与赌徒谬误、三段等分），以及humanizer/avoid-ai-writing双重检查（正文/FAQ零em dash零curly quote，无Tier1/2/3禁用词），审核agent均判定PASS。
- **比较级实算**：跑`check_comparatives.py`命中3句（均含"three times"字样），逐句核对：0.35³=0.042875≈4.3%正确、0.5³=0.125=12.5%正确，第三句为FAQ提问句无需核算数值。
- **SEO字段离群值核查**：跑`check_seo_field_stats.py --new-slug yes-or-no-wheel`，title（33字符，z=0.06）与description（179字符，z=0.58）均在全站正常分布范围内。
- **发布**：commit `ef7fa98`（工具本体：`src/lib/yesNoWheel.ts`、`tests/yesNoWheel.test.ts`、`tests/YesNoWheel.dom.test.tsx`、`src/components/calculators/YesNoWheel.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`，含审核后的bug修复）；CF Pages自动部署，curl轮询3次（约45秒）后从404转为200，确认`https://calcbadger.com/yes-or-no-wheel/`与`/embed/yes-or-no-wheel/`均为200，`<title>`含"Yes or No Wheel: Weighted Decision Spinner"；IndexNow提交`/yes-or-no-wheel/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第211行`yes or no wheel`标`[已用 2026-08-12 并入yes-or-no-wheel页]`（第210/213行已在选题阶段标记为并入`length-converter`页，第212行`how many miles is 10000 steps`按上文说明保持未标记），改动前后总行数断言1637不变。
- 独立审核agent本次正常完成并返回报告（未触发全局CLAUDE.md看门狗规则里的卡死降级流程，是审核内容本身发现了真实bug，不是agent执行故障）。无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-12 — Steps to Miles Calculator（第28个工具，commit `2fbeb37`+`6f2f84f`+`9f433fb`）

- **选题（含逐行跳过说明）**：从上次已用行（第211行`yes or no wheel`）往下核对，第212行`how many miles is 10000 steps`（135,000/月，C层，无权威壁垒标记）即为首个未标记候选，前面无需跳过的行。扫表发现同一"步数换算距离"意图簇散落多处：第516行`how many steps in a mile`（201,000/月，带同类深耕竞争风险标记）、第712行`steps to miles`（49,500/月，同类标记）、第1105行`how many steps is 5 miles`（6,600/月，反向换算）、以及13条不同步数的`N steps in miles`小体量变体（1061/1342/1344/1374/1404/1405/1427/1428/1429/1431/1454/1470/1518行，各1,300-8,100/月，均带同类深耕竞争风险标记）——全部判定为同一页面真实覆盖（步数↔距离双向换算+常见步数参考表），一并标记`[已用 2026-08-12 并入steps-to-miles-calculator页]`，改动前后总行数断言1637不变。类目`Health`，与前一日`Math`(shape-volume-calculator)、`Games`(yes-or-no-wheel)均不同，满足跨类目轮换。
- **差异化决策**：该簇多个变体已被词库标记"同类深耕竞争风险"（RapidTables/Worldometers/Inch Calculator等深耕站占据SERP），未直接沿用行业惯用的"步数÷2000"固定比例换算（会员站同款做法），改为按身高+性别估算个人步幅（Hoeger et al.回归系数），双向换算（步数→距离 / 距离→步数），并入常见步数参考表覆盖13条小体量变体，作为区别于同类站的深度。
- **公式研究**：步幅(in) = 身高(in) × 0.415(男)/0.413(女)，来源Hoeger, W.W.K.等，"One-Mile Step Count at Walking and Running Speeds"，ACSM's Health & Fitness Journal, Vol.12 No.1 (2008)，WebSearch两次独立检索交叉核实系数与期刊信息一致。美国成年人平均身高（男68.9in/女63.5in）来源CDC/NCHS Body Measurements FastStats，NHANES 2021-2023数据。
- **开发**：`src/lib/steps.ts`导出`stepLengthInches`/`stepsToMiles`/`stepsToKm`/`milesToSteps`/`kmToSteps`/`cmToInches`六个纯函数；`tests/steps.test.ts`14个vitest用例，期望值全部用Python独立计算（非从实现输出回填），含往返换算(round-trip)互逆验证；`StepsToMilesCalculator.tsx`用站内既有`Segmented`（方向/单位/性别）+`NumberField`（身高/步数/距离）原语，参照`BmiCalculator.tsx`的英制/公制身高输入模式。
- **正文数值核实**：coreSummary、两个worked example、reverse-direction例子、两张参考表（15行×2列步数-距离表 + 7行身高-步幅表）、5条FAQ里的全部数字均用Python独立重算校验（非从组件实现输出回填），过程中发现并修正1处不一致（worked example用69in身高但reverse示例误用68.9in平均身高算出的步数值11,079，已改为按69in重算的11,063）。
- **浏览器实测替代方案**：本次运行为无人值守定时任务，`preview_start`报"Dev servers can't be started from unattended sessions"，改用生产构建产物核查（`dist/steps-to-miles-calculator/index.html`标题/FAQ/schema/astro-island组件挂载均正常、`dist/embed/`含Powered by链接、sitemap含新URL）+ 组件状态转换手工逐行走查（身高清空/切换模式/切换单位制均有NaN/除零防护，无崩溃点）替代交互式浏览器验证。
- **humanizer+avoid-ai-writing双重检查**：`Skill(humanizer)`发现5处叙事性em dash（正文段落+FAQ），逐一改为句号/逗号/冒号后清零；`Skill(avoid-ai-writing)`复核未命中Tier1/2/3禁用词表、无curly quote、引用来源具名具体（非"专家认为"类模糊归因），判定clean。
- **独立agent审核（发现真实问题并已修复，commit `6f2f84f`）**：怀疑视角复核8个维度，**首轮FAIL**——发现`src/lib/steps.ts`注释、`tools.ts`的coreSummary+正文段落、`StepsToMilesCalculator.tsx`组件提示文案四处均写"Hoeger et al.在1,000名步行/跑步者身上拟合的回归"，独立核实该论文Boise State ScholarWorks摘要页不含样本量数字，另一次WebSearch反而返回"n=45"的方法学片段，与"1,000"相差20余倍，判定为教训库`L-0805-21`（具名信源归因数字未逐字核对源头）第5次复现，已在该条目追加复发记录（未新建条目，仅追加）；同时发现`sources`数组引用的期刊URL路径拼写错误（`.6.aspx`应为`.7.aspx`），已核对Boise State"Link to Full Text"确认正确路径并修正。**修复**：四处文案删除"1,000"具体样本量断言，仅保留可独立核实的期刊引用信息（作者/期刊/卷期页码/系数本身）；URL修正。其余（CDC身高数据、0.415/0.413系数交叉核实、全部参考表/FAQ数值重算、组件逻辑无崩溃点、内容差异化、词库记账、总行数1637）均PASS。修复后`npm test`648/648通过、`npm run build`72页成功。
- **SEO字段离群值核查（首轮发现问题并已修复）**：跑`check_seo_field_stats.py --new-slug steps-to-miles-calculator`首轮命中description离群（215字符，z=2.40，为全站最长），已改写压缩至178字符（z=0.51，正常范围）；title 41字符（z=0.61）正常。
- **比较级实算**：跑`check_comparatives.py`未命中触发词；手工核对正文2处比较级措辞（"a third of a mile less"对应实算0.347mi、"close to half a mile"对应实算0.56-1.08mi区间，均为合理的模糊措辞，非精确数字断言）与实算数值方向一致。
- **发布**：三次commit——`2fbeb37`（工具本体首次提交）、`6f2f84f`（独立审核agent修复样本量断言与URL）、`9f433fb`（SEO离群值description精简）；CF Pages自动部署，curl轮询各阶段均确认200且正文含预期修正内容；IndexNow提交`/steps-to-miles-calculator/`：Bing 200 / Yandex 200。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第295/599/795/1144/1188/1425/1427/1457/1487/1488/1510/1511/1512/1514/1537/1553/1601行（对应总表第212/516/712/1061/1105/1342/1344/1374/1404/1405/1427/1428/1429/1431/1454/1470/1518行）标`[已用 2026-08-12 并入steps-to-miles-calculator页]`，改动前后总行数断言1637不变（脚本执行+二次grep核对17处标记全部生效）。
- 独立审核agent本次正常完成并返回报告（未触发全局CLAUDE.md看门狗规则里的卡死降级流程），审核过程本身发现2处真实问题（样本量断言、URL），已修复并二次验证。无推翻核心结论的情形，未触发Owen邮件升级。`tools.ts`的`queries`数组里"how many miles is 8000/6000 steps"经核对实为已标记的"8000/6000 steps in miles"（第1344/1342行）的动宾顺序变体，非未经调研的选题，审核agent报告里的"unbacked"提示为误判，已在此说明澄清。

## 2026-08-13 — Math Symbols（第29个工具，commit `cf5a3f4`）

- **选题**：从上次已用行（第214行`volume of a rectangular prism`）往下核对，第215行`greater than or equal to symbol`（74,000/月，B层，无权威壁垒/已用标记）即为首个未标记候选，前面无需跳过。扫表发现同一"数学符号"意图簇散落多处：第220行`division symbol`、第221行`plus minus symbol`、第232行`approximately symbol`、第258行`multiplication symbol`、第310行`less than or equal to symbol`、第324行`squared symbol`、第372行`degree symbol`、第376行`infinity symbol`、第628行`square root symbol`（带同类深耕竞争风险标记，仍选用——该标记原文是8/10批量套用给时区/单位换算类词的通用模板，square root symbol本身并非时区/换算类查询，判定为误套模板而非真实壁垒证据）、第1129行`therefore symbol`、第1141行`cubed symbol`，共12个符号一并判定为同一页面真实覆盖（可搜索复制的数学符号参考表+每个符号独立的历史考据），标记`[已用 2026-08-13 并入math-symbols页]`，改动前后总行数断言1637不变。类目`Reference`，与前一日`Health`(steps-to-miles-calculator)不同，满足跨类目轮换。词库2026-08-03四次扩库备注里提到的"数学符号系列(±/≥/≤/√/π/Δ)"里π/Δ已被greek-alphabet（2026-08-11发布）覆盖，本次只做非希腊字母的运算符/关系符/幂符号，避免与greek-alphabet选题重叠。
- **公式/历史研究（本页无计算公式，"历史考据"是本页的权威来源等价物）**：12个符号的起源逐条WebSearch独立核实，非凭记忆填写：×/±出自William Oughtred《Clavis Mathematicae》(1631)，×本身更早见于1618年一份被归于Oughtred的Napier对数表附录（1631-1618=13年，非"前一年"，初稿曾误写"the year before"，被`check_comparatives.py`命中后核对算错并改为"thirteen years earlier"）；÷/∴出自Johann Rahn《Teutsche Algebra》(1659)，Rahn与合作者John Pell的发明权归属历史上确有争议（Wikipedia"Division sign"词条明确记载，非本站编造的模糊归因）；≤/≥出自Pierre Bouguer(1734)，在Thomas Harriot 1631年的</>基础上加下划线；√出自Christoff Rudolff《Die Coss》(1525)，"形似小写r代表radix"一说历史学家本身只说"probable"非确定，正文如实保留这个hedge；∞出自John Wallis《De sectionibus conicis》(1655)；²/³的上标记法出自René Descartes《La Géométrie》(1637)，Descartes自己仍常写"zz"而非"z²"；°追溯至巴比伦60进制天文学。每条均WebSearch 2次以上交叉核实（Wikipedia+MacTutor/Britannica/Stanford Encyclopedia of Philosophy等独立信源），无单一信源孤证。
- **开发**：`src/lib/mathSymbols.ts`导出`MATH_SYMBOLS`（12项：name/symbol/codePoint/meaning/commonUse/history）+`filterMathSymbols`纯函数，参照`src/lib/greekAlphabet.ts`同款搜索过滤模式；`MathSymbolsGenerator.tsx`搜索框+点击复制网格，参照`GreekAlphabetGenerator.tsx`交互模式，复用同款CSS结构但新建`.math-symbols-*`类名（未直接复用`.greek-*`类名，避免语义误导）；`tests/mathSymbols.test.ts`13个vitest用例，含字形与Unicode码点内部一致性核对（`codePointAt`断言）、唯一性、filter函数8种行为。
- **humanizer+avoid-ai-writing双重检查（发现并修复真实问题）**：`Skill(humanizer)`发现正文/FAQ共6处叙事性em dash，`Skill(avoid-ai-writing)`额外发现`src/lib/mathSymbols.ts`数据文件里8/12个symbol的`commonUse`字段均为整齐的"三项逗号列表"结构（如"Inequalities in algebra, minimum-value constraints, comparison operators in code"），判定为compulsive rule-of-three的机械化变体（不是修辞层面的三连排比，而是数据字段层面的结构性均匀），已重写为2-3项不等的混合结构（部分改成"A and B, and C, where..."从句式、部分压缩为2项）打破整齐划一；同时清理了2处空洞强化词（"a real problem"改"only gets worse"、"the actual inventor"删"actual"）。
- **独立agent审核（8个维度PASS，含1处非阻断性复发）**：怀疑视角复核历史考据准确性/日期算术/Unicode码点/来源URL/AI写作痕迹/内容差异化/组件逻辑/测试质量。独立WebSearch逐条复核全部6条历史断言，全部核实通过；独立核对1618/1631/1659/1734/1631年份算术全部正确；独立抽查12个码点中5个（含≥≤∴√∞）对照Unicode标准全部匹配，确认12个码点互不重复且均为单码点字符；11条sources URL中10条curl返回200，1条(Britannica infinity)返回403，判定为数据中心IP被反爬拦截（非死链/错配，该URL在本文历史调研阶段WebSearch已正常返回其内容，标题与引用主题吻合），复核后维持沿用不改；AI写作痕迹二次扫描零命中。**发现1处非阻断性复发**：`tools.ts`里"How to use this tool"开篇段落与"How to type these symbols"结尾段落，与2026-08-11发布的姊妹页`greek-alphabet`对应两段（"How to use this tool"/"How to type Greek letters"）逐句同骨架换词，"How to type"段除示例码点外近乎逐字相同，判定为教训库`L-0804-11`第N次复发（已在该条目追加记录，非新建条目）；本次运行内已重写两段，其中"How to type"段补充了greek-alphabet没有的新事实（±×÷°²³六个符号落在Latin-1范围、Windows支持Alt+0xxx数字键盘直接输入，另外六个≥≤≈√∞∴不支持——该Alt+0xxx事实同样经WebSearch独立核实：Alt+0176=°/Alt+0177=±/Alt+0215=×均确认），使内容而非仅措辞产生真实差异，重写后未阻断发布。另指出组件`copySymbol`同符号快速连续点击时"Copied!"提示计时器不会重置（继承自`GreekAlphabetGenerator.tsx`的既有模式，非本页新引入的bug，非阻断）。
- **比较级实算**：`check_comparatives.py`命中2句，逐句核对后发现1处真实错误（见上文"历史研究"段的1618年份算术错误）并已修正为"thirteen years earlier"；另1句"almost three decades later"对应1659-1631=28年，核实准确，未改。
- **SEO字段离群值核查**：跑`check_seo_field_stats.py --new-slug math-symbols`，title（19字符，z=-0.91）与description（167字符，z=-0.16）均在全站正常分布范围内。
- **浏览器实测替代方案**：本次运行为无人值守定时任务，`preview_start`报"Dev servers can't be started from unattended sessions"，改用生产构建产物核查（`dist/math-symbols/index.html`标题/FAQ/WebApplication+FAQPage schema/astro-island SSR渲染12个符号按钮均正常、`dist/embed/math-symbols/`含Powered by链接、CSS内联块含`.math-symbols-grid`规则、sitemap含新URL）替代交互式浏览器验证；上线后额外用curl对生产URL做了真实网络请求二次确认（`/math-symbols/`与`/embed/math-symbols/`均200，正文含预期标题与12个symbol cell）。
- **发布**：单次commit `cf5a3f4`（工具本体：`src/lib/mathSymbols.ts`、`tests/mathSymbols.test.ts`、`src/components/calculators/MathSymbolsGenerator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`，已含独立审核后的段落重写）；CF Pages自动部署，curl轮询6次（约90秒）后从404转为200；IndexNow提交`/math-symbols/`：Bing 200 / Yandex 200。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第298/303/304/315/341/393/407/455/459/711/1212/1224行（对应总表第215/220/221/232/258/310/324/372/376/628/1129/1141行）标`[已用 2026-08-13 并入math-symbols页]`，改动前后总行数断言1637不变（二次grep核对12处标记全部生效）。

## 2026-08-13 — Length Converter 内容质量审计更新（本条为content-quality-audit审计更新，非新发布，commit `f8e22d0`）

- **背景**：`trafficsite-content-quality-audit`任务对`length-converter`（本站从未被审计过的最早工具之一，2026-08-04首发）做深度复审，聚焦公式正确性与组件可用性（工具站审计维度，非长文写作维度）。
- **诊断专属核查重点**：8种长度单位换算系数是否与NIST HB44/1959国际码磅协定一致、3张参考表与2条worked example数值是否可独立复算、边界值（0/负数/非数字/极大数值）页面表现是否合理、meta description等SEO字段是否健康。
- **公式/数据核实**：用Python独立重算（不参考实现代码）全部换算系数（1 in=2.54 cm/1 ft=0.3048 m/1 yd=0.9144 m/1 mi=1609.344 m）、cm-inches对照表12行、身高换算表11行、5种赛程距离表，全部与`src/lib/length.ts`及`src/data/tools.ts`正文吻合，无一处偏差。NIST两条sources链接curl均200，内容对应，无死链。`npm test`（length.test.ts 24项）全过，期望值逐条独立复算比对一致。humanizer+avoid-ai-writing双重检查（本工具2026-08-04发布，早于8/7规则生效日）：正文/FAQ叙事部分0处em dash，仅referenceTables标题与sources标签内3处em dash（站内既有惯例格式，非叙事问题，未改动）；无AI高频词/弯引号命中，判定clean。内链健康度：用真实`pickRelatedGuides`+crossCategory轮转逻辑跑全站29个工具的node模拟，29/29覆盖，length-converter被正常链接；线上sidebar/embed页/robots.txt(含AI爬虫Allow)/ads.txt(pub-5245502795720653)/3处JSON-LD schema（WebApplication+FAQPage+BreadcrumbList）均核对一致。
- **浏览器实测发现2处真实问题（均经独立fresh-context agent复核CONFIRMED后修复）**：
  1. **极端数值浮点残留**（组件可用性维度）：`src/lib/length.ts`的`roundSig()`用`Math.round(n*magnitude)/magnitude`（magnitude由`Math.pow(10, 负指数)`算出）对6位有效数字四舍五入，在浏览器实测输入`999999999999`（cm）时，Feet结果显示`32,808,399,999.999996 ft`而非干净的`32,808,400,000 ft`——负指数的`10^k`在IEEE-754下无法精确表示，乘除往返产生浮点噪声。用Node独立复现真实换算路径确认可复现（`32808398950.098423` → 旧算法`32808400000.000004`，含残留位）。**修复**：改用`Number(n.toPrecision(sig))`（对十进制表示直接四舍五入，无此失效模式），验证对全部正常量级测试值（11.811/0.000001/3280.84/0.1等）结果不变，仅消除极端量级的残留位；`tests/length.test.ts`24项及全站661项测试修复后全部通过。
  2. **meta description超长**（SEO维度）：description字段170字符，超出~160字符SERP安全区间10字符（6.25%），155-160字符截断点会在"and race"处不完整收尾，丢失"distances"一词。**修复**：精简到152字符，保留全部三个差异化关键词（heights/tool sizes/race distances），改动同步影响meta description/og/twitter description/JSON-LD WebApplication.description/页面首屏可见导语（同一字段5处复用）。
- **两处均为定点修改**，未做大范围重写；`npm run build`（74页）成功后，`git status`确认仅`src/data/tools.ts`与`src/lib/length.ts`本次改动，未受同仓库其他并发会话影响，单独commit `f8e22d0`并push。
- **部署与验证**：push后curl轮询3次（约30秒）确认线上description已生效部署；用Browser pane重新实测输入`999999999999`，Feet结果已变为干净的`32,808,400,000 ft`，浮点残留问题确认在线上已修复。`node tools/submit-indexnow.mjs /length-converter/`提交成功（Bing 200/Yandex 200）。
- **未发现问题的维度**：GEO（按ai-seo skill的Content Extractability Check人工核对，coreSummary/worked examples/reference tables/FAQ schema/时效信号/AI爬虫放行等9-10项通过，等效≥80门槛）、竞品差异化（WebSearch核对calculatorsoup/rapidtables/unitconverters等头部竞品，均为单向基础换算无历史背景/参考表/8向同时换算，本页构成真实增量）、schema一致性、AdSense合规。
- 独立审核agent本次正常完成并返回报告（2条候选均CONFIRMED），未触发全局CLAUDE.md看门狗规则里的卡死降级流程。无推翻核心结论的情形，未触发Owen邮件升级。
- 独立审核agent本次正常完成并返回报告，未触发全局CLAUDE.md看门狗规则里的卡死降级流程。审核过程发现1处真实数据结构问题（rule-of-three均匀化）已在写作阶段自查修复、1处教训库复发（L-0804-11）已重写并回库、1处继承性组件小瑕疵（未阻断）。无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-13 — Minecraft Circle Generator（第30个工具，commit `30043fe`）

- **选题**：从上次已用行（第216行`heloc calculator`，本次判定跳过）往下核对，第216-217行是候选区间——第216行`heloc calculator`未标记但WebSearch实测SERP前9个结果几乎全是银行/信用社（UNFCU/Citizens Bank/U.S. Bank/Bank of America/Bankrate）自建计算器，同款制度性品牌垄断模式，判定权威壁垒风险并回词库补标记（非选用，是新发现降优先级）；第217行`minecraft circle generator`（49,500/月，B层，KD3，$8.15）WebSearch实测SERP前9个结果为xbitlabs/wisehosting/deltacalculator/minecraftcircle-generator.com等独立小站，无机构垄断信号，选用。第216行之前全部行均已带`[已用]`或`[⚠️权威壁垒风险]`标记，无需额外跳过说明。类目`Games`，与前一日/前一次运行`Reference`(math-symbols)不同，满足跨类目轮换。全表扫描确认无同页变体（`minecraft enchantment calculator`/`minecraft nether portal calculator`为完全不同工具，非本词变体）。
- **算法研究（本页无传统"公式"，距离阈值光栅化规则是本页的权威来源等价物）**：核心规则——网格块(row,col)到中心距离sqrt(row²+col²)≤radius即判定为该块被填充；outline模式保留"四个正交邻居中至少一个未填充或已到网格边缘"的块。WebSearch确认这是Minecraft建筑社区自2012年Jesse Donat发布Donat Studios Pixel Circle/Oval Generator以来沿用的惯例，diameter=2×radius+1也源自同一惯例。独立审核agent进一步追溯到`donatj/Circle-Generator`GitHub源码本身（非仅信任页面文案），确认`filled(x,y)=distance<=radius`与`thinfilled()`的四邻居判定逻辑与本站实现逐字一致，Jesse Donat本人2016年在页面留言"radius 128→diameter 255非256，因为其中一格是中心"独立佐证diameter公式。撰写过程中原稿曾把这套简化距离阈值法错误归因为"Jack Bresenham 1965年提出的midpoint circle algorithm"，二次独立WebSearch交叉核实（单信源日期核对硬规则）发现Bresenham 1965年论文实际讲的是直线算法而非圆，Bresenham本人的圆算法发表于1977年，"midpoint circle algorithm"这个提法要到1985年Van Aken与Novak才正式提出——撰写阶段自查发现并改写，未使用错误的历史归因。
- **开发**：`src/lib/minecraftCircle.ts`导出`generateCircle(radius, mode)`纯函数（filled/outline两种模式）与`circleToSchematic()`；`tests/minecraftCircle.test.ts`20个vitest用例，期望值全部来自独立Python脚本重算（非读实现输出），含10组radius(0-30)的filled/outline计数、radius=1与radius=3的精确网格ASCII断言、row breakdown断言；`src/components/calculators/MinecraftCircleGenerator.tsx`用HTML canvas渲染网格（避免radius=30时3,721个DOM元素的性能问题）、NumberField+Segmented输入、"Copy build schematic"复制按钮；`src/styles/global.css`新增`.circle-*`类名；`CalculatorIsland.astro`按字母序注册。
- **humanizer+avoid-ai-writing双重检查（发现并修复真实问题）**：`Skill(humanizer)`发现正文6处叙事性em dash（sources引用标签内2处em dash为全站既有"Label — description"引用惯例，未改）；人工按avoid-ai-writing规则库复核额外发现3处rule-of-three密度问题（"floor/platform/pond"+"walls/towers/rim"+"arena floors/tower bases/domes"三组独立三连列表且"pond"重复出现两次）与1处语法错误（"an 61×61"应为"a 61×61"）与1处UI文案不一致（正文写"Copy the schematic button"但组件实际按钮文案是"Copy build schematic"），均已改写修复。
- **浏览器实测替代方案**：本次运行为无人值守定时任务，`preview_start`报"Dev servers can't be started from unattended sessions"；除生产构建产物核查（`dist/minecraft-circle-generator/index.html`标题/6条FAQ Schema/WebApplication+FAQPage+BreadcrumbList/canvas元素/两条sources URL均存在，`dist/embed/`含Powered by链接）外，额外新增`tests/MinecraftCircleGenerator.dom.test.tsx`（happy-dom环境，5个用例，通过`@testing-library/preact`实际渲染组件+触发输入事件，验证真实渲染路径而非仅纯函数：默认radius=8显示17格/197块、radius=3切换outline后16块、radius=999被clamp到30格2,821块、schematic文本包含"13 blocks total"等断言），比此前几次工具仅做纯函数单测更进一步覆盖了组件渲染层。
- **独立agent审核（6个维度，发现1处真实问题并已修复）**：怀疑视角复核教训库比对/来源真实性/测试独立性/正文算术与形状断言/humanizer残留/组件结构/站内一致性。教训库比对：无外部引语/机构立场/内链问题（本页无内链）触发；来源真实性：追溯到GitHub源码本身而非仅信任页面文案，Wikipedia引用被正确限定为"一般背景"未被误用为主算法来源；测试独立性：agent自行从零写Python脚本复算全部25个测试期望值，未参考实现代码，全部吻合；**发现新问题**：FAQ第4条原表述"radius 2起outline变成连通环（radius 2为菱形，之后越来越圆）"与radius 1"四个断开的臂"形成对比，独立agent用连通分量分析（严格正交4-连通）核实后发现radius 2实际是8个完全不相邻的独立块（比radius 1的4个分量更碎），"connected loop"这个比较级断言不成立——判定为教训库L-0804-8"比较级须实算核实"家族的新变体（拓扑连通性断言而非数值断言），已在本次运行内改写为不做连通性对比的准确表述，仅保留"更圆"这个未被质疑的轮廓描述。**已建议追加到通用教训库**（新变体：几何/拓扑类比较级同样需要实算，不止价格/数量类）。
- **比较级实算**：`check_comparatives.py`命中0句（脚本触发词表本身不覆盖拓扑连通性这类非数值比较级，本条真实发现来自独立审核agent的连通分量分析而非脚本）。
- **SEO字段离群值核查**：`check_seo_field_stats.py --new-slug minecraft-circle-generator`报title长度62/z=2.13离群，人工复核发现是脚本已知局限触发的误报——全站title与slug计数不一致（88 vs 30，脚本自身有此提示），因ReferenceTable.title字段与Tool.title字段正则未区分，`--new-slug`按位置对应逻辑错位，实际抓到的是mortgage-calculator一张参考表的title（"Remaining balance by year..."62字符），本工具真实title为"Minecraft Circle Generator"（26字符），落在全站12-71正常区间内，非真实离群；description（172字符，z=0.18）正常。
- **发布**：单次commit `30043fe`（工具本体：`src/lib/minecraftCircle.ts`、`tests/minecraftCircle.test.ts`、`tests/MinecraftCircleGenerator.dom.test.tsx`、`src/components/calculators/MinecraftCircleGenerator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`，已含独立审核后的FAQ第4条改写）；CF Pages自动部署，curl轮询4次（约45秒）后从404转为200，`/minecraft-circle-generator/`与`/embed/minecraft-circle-generator/`均确认200且标题/Powered by链接正常。IndexNow提交`/minecraft-circle-generator/`：Bing 200 / Yandex 200。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第300行`minecraft circle generator`标`[已用 2026-08-13]`，第299行`heloc calculator`标`[⚠️权威壁垒风险: ...]`（非已用，仅降优先级），改动前后总行数断言1637不变。
- 独立审核agent本次正常完成并返回报告（约6.9分钟，35次工具调用），未触发全局CLAUDE.md看门狗规则里的卡死降级流程。审核过程发现1处真实拓扑比较级错误已修复。无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-13 — Board Foot Calculator（第31个工具，commit `fb7e2e2`，本日第三次运行）

- **选题**：从上次已用行（第217行`minecraft circle generator`）往下核对，第218行`5 7 in inches`（49,500/月，C层，无权威壁垒/已用标记）是首个未标记候选，前面无需跳过。WebSearch实测该词SERP前9个结果为inchcalculator.com/miniwebtool.com/omnicalculator.com/calculat.io/heightininches.com（专做身高换算的单一功能站）/saving.org/thecalculatorsite.com/vedantu.com，与词库已标注的同类单位换算深耕竞争模式一致，判定权威壁垒风险并回词库补标记（非选用）。继续下查，第219-226行为随机/字母表/符号类B层词（与本日前两次运行math-symbols/minecraft-circle-generator同属B层"生成器/符号"大类，跨类目轮换要求换类目），第227行`board foot calculator`（33,100/月，A层，无权威壁垒/已用标记，构建/木工类）为首个满足"未标记+跨类目"双条件的候选，WebSearch实测SERP由geekskai.com/bidwellwoodandiron.com/boardfootcalculators.com/boardcalculate.com等中小型独立站与inchcalculator/omnicalculator主导，无制度性品牌垄断信号，选用。类目`Construction`，与本日前两次运行`Reference`(math-symbols)、`Games`(minecraft-circle-generator)均不同，满足跨类目轮换。全表扫描确认无同页变体。
- **公式研究**：核心公式board feet=(厚度in×宽度in×长度ft)/12，1板英尺=144立方英寸，来源UW-Madison Division of Extension林业推广出版物FEM-042《What Is A Board Foot?》(1989)——**该来源为撰写后独立审核agent复核时替换**，初稿最初引用University of Arkansas Extension FSA5036与Purdue Extension FNR-191，独立agent下载全文PDF核实后发现两份文档实际内容分别是立木材积估算(basal area/DBH)与原木材积估算(Doyle/Scribner/International log-scaling rules)，均**不含**本页实现的成材板英尺公式，属误定源，已在发布前用WebSearch找到真正陈述该公式的UW-Madison FEM-042并替换全部3处引用（`boardFoot.ts`注释/`boardFoot.test.ts`注释/`tools.ts`正文与sources），修复后重新`npm run build`确认无残留误引用。"quarters"厚度惯例(4/4=1"/8/4=2")来源National Hardwood Lumber Association，经Rockler与Woodworkers Source两个独立信源交叉确认；表面净料(S2S/S4S)实际厚度损耗数字两个信源存在真实分歧(Rockler称约1/4"，另一信源称约3/16")，正文未采用单一精确数字，改写为如实呈现分歧的"varies by supplier"措辞，独立审核agent核实该分歧确实存在，判定此hedge为诚实表述而非过度谨慎。
- **开发**：`src/lib/boardFoot.ts`导出`boardFeetPerPiece`/`totalBoardFeet`/`withWaste`/`costEstimate`/`quartersToInches`/`toFeet`六个纯函数；`tests/boardFoot.test.ts`14个vitest用例，期望值手工独立核算（1"×6"×8ft=4bdft教科书例/2"×8"×10ft=13.3333/2"×12"×16ft=32/48bdft×10%waste=52.8/52.8bdft×$9.25=$488.40等）；`BoardFootCalculator.tsx`用站内既有`NumberField`+`Segmented`原语，新增"Nominal thickness (rough-sawn quarters)"快捷预设(4/4~16/4)联动厚度输入框，参照`ConcreteCalculator.tsx`的waste-allowance+quantity模式。
- **humanizer+avoid-ai-writing双重检查（发现并修复）**：`Skill(humanizer)`发现正文3处叙事性em dash，已逐一改为句号/冒号清零；`Skill(avoid-ai-writing)`额外发现coreSummary与"nominal quarters"段落各1处em dash（首轮humanizer未覆盖到的字段）与1处hollow intensifier("genuinely rough off the saw"的genuinely)，均已清零/改写；`check_comparatives.py`命中0句（本页400-800字讲解以公式/权威定义为主，符合预期）；`check_seo_field_stats.py --new-slug board-foot-calculator`：title 44字符/z=0.88正常，description 154字符/z=-0.90正常。
- **独立agent审核（发现1处真实问题并已修复）**：怀疑视角复核公式来源真实性/参考表与worked example算术/单信源分歧核实/结构差异化/AI写作痕迹/组件正确性共9项。**发现真实问题**：如上文"公式研究"所述，Arkansas/Purdue两份来源文档经agent下载全文核实后确认不含本页公式，属误定源；agent同时独立验证了参考表10行、两个worked example、全部14条单元测试期望值算术均正确，NIST PS 20-20与Rockler来源真实且支持其引用主张，S2S厚度分歧确系真实（已用WebSearch独立抓取Rockler原文确认"usually about 1/4 inch"措辞），本页与concrete-calculator/square-footage-calculator两个既有A层工具对比未发现模板化换词，正文零em dash零AI高频词，组件默认状态(1"×6"×8ft)与worked example一致、quarters按钮联动正确、价格/waste字段可选处理无NaN风险。**修复**：改用UW-Madison FEM-042替换全部3处误定源引用，重新`npm test`(700/700)+`npm run build`(78页)确认无回归。
- **浏览器实测替代方案**：本次运行为无人值守定时任务，`preview_start`报"Dev servers can't be started from unattended sessions"，改用生产构建产物核查（`dist/board-foot-calculator/index.html`标题/meta description/WebApplication+FAQPage(6条)+BreadcrumbList schema/astro-island组件挂载/H1均正常，`dist/embed/board-foot-calculator/`存在）+ 单元测试覆盖计算逻辑（14项，比手工浏览器点击更强的正确性验证）替代交互式浏览器验证；上线后额外curl真实网络请求二次确认`/board-foot-calculator/`轮询3次（约40秒）后200且正文含预期标题。
- **发布**：单次commit `fb7e2e2`（工具本体：`src/lib/boardFoot.ts`、`tests/boardFoot.test.ts`、`src/components/calculators/BoardFootCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`，commit message已注明发布前的来源修正）；CF Pages自动部署确认200；IndexNow提交`/board-foot-calculator/`：Bing 200 / Yandex 200。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第301行`5 7 in inches`标`[⚠️权威壁垒风险: ...]`（非已用，仅降优先级），第310行`board foot calculator`标`[已用 2026-08-13]`，改动前后总行数断言1637不变（二次wc -l核对）。
- 独立审核agent本次正常完成并返回报告（约2.5分钟，23次工具调用），未触发全局CLAUDE.md看门狗规则里的卡死降级流程。审核过程发现1处真实来源误定问题（公式引用文档与实际内容不符）已修复并二次验证（重新build+test）。无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-16 — Random Letter Generator（第32个工具，commit `dfe3417`）

- **触发**：定时任务`calcbadger-tool-publishing`本次运行为对上一次因故中断的运行的重试（用户消息为"Try again"，无更多上下文）；核对git log发现仓库最近一次真实发布是2026-08-13（board-foot-calculator），working tree clean、无stash/WIP，判定为全新一轮运行，非续做半成品。
- **选题（逐行交代，按2026-08-12规则）**：从`独立站/流量站词库/CalcBadger-关键词库.md`第82行表头往下找，第84-298行（约215行）此前均已标`[已用]`；第216行`heloc calculator`（月搜索量49,500）标有`[⚠️权威壁垒风险: 2026-08-13 WebSearch实测SERP被银行/信用社自建计算器占满]`——降优先级跳过；第217行`minecraft circle generator`已标`[已用 2026-08-13]`；第218行`5 7 in inches`同样标有权威壁垒风险（inchcalculator.com等身高换算深耕站占满SERP）——降优先级跳过；第219-221行分别标`[已用 2026-08-12 并入shape-volume-calculator页]`/`[已用 2026-08-13 并入math-symbols页]`/`[已用 2026-08-13 并入math-symbols页]`。第222行`random letter generator`（B层，月搜索量40,500，KD1）无任何标记，是从表头往下第一个真正"干净"的候选，锁定为本次选题。类目核对：上一个已发布工具board-foot-calculator为Construction类目，`random letter generator`归入Games类目（与minecraft-circle-generator/yes-or-no-wheel/reaction-time-test同类），跨类目混排要求满足。
- **权威壁垒预检（新增词，非预标注）**：WebSearch"random letter generator"实测SERP，结果为多个独立小型工具站分散占据（capitalizemytitle/randomlists/gigacalculator/randomletter.io等），无单一品牌垄断，未命中壁垒模式；另WebSearch确认Google搜索结果本身对该词无原生widget（不同于coin flip/dice roll/spinner wheel等已知原生widget词），符合SKILL.md第4条红线（不做纯widget复刻）——已设计equal/frequency双权重模式+vowels/consonants子集+session tally作为widget给不了的深度。
- **公式研究**：核心是加权随机抽样（cumulative-weight/inverse transform sampling），复用本站Yes/No决策轮盘（`yesNoWheel.ts`）已验证过的同一方法。"English frequency"模式的权重来自英语字母频率表，主信源为Wikipedia"Letter frequency"词条Texts列（用Browser pane实测页面原文逐字抄录26个字母百分比，非WebFetch/WebSearch摘要）；按SKILL.md第8步单信源交叉核实要求，另用Browser pane打开Cornell大学Math Explorers' Club的4万词语料库频率表（`pi.math.cornell.edu`）独立核对——两表最高频7个字母（e,t,a,o,i,n,s）排序一致、多数具体数值接近，但第8-9位起有反转（Wikipedia h略高于r，Cornell则r略高于h）。
- **独立agent审核（发现2处真实问题并已修复）**：怀疑视角复核8个维度（公式/来源准确性、测试期望值独立性、正文比较级、参考表转录、结构模板化、UI状态一致性、humanizer/AI写作痕迹、教训库核对）。**发现真实问题1**：`randomLetter.ts`注释与`tools.ts`FAQ+参考表note三处均断言Wikipedia与Cornell两表"排序完全相同"，独立agent实地打开Cornell原始页面核实其自身数值排序后发现第8-9位（H/R）确实反转，且`randomLetter.ts`注释里写的顺序本身还跟同文件`LETTER_FREQUENCY`实际使用的Wikipedia数值（h=6.1>r=6.0）自相矛盾——已改为如实表述"前7位一致，往后个别名次顺序有出入"并修复三处引用。**发现真实问题2**：`RandomLetterGenerator.tsx`切换"Letters"（All/Vowels/Consonants）时未清空`history`/`result`状态，导致切换letter set后界面可能同时显示不属于当前set的字母，与"Letters in play"/"Most likely to come up"两处live computed字段不一致——已仿照`YesNoWheel.tsx`的`changeMode()`模式加`changeSet()`包装函数清空`history`/`result`，浏览器复测确认切换后"Draws this session"归零、"Most drawn so far"清空。**其余6项PASS**：26字母频率表转录逐一核对无误；测试文件`tests/randomLetter.test.ts`频率加权累积区间独立手算复核全部一致，无"先实现后拿输出填测试"迹象；正文全部数值比较（"3.3倍"/"52倍"/"170倍"/"约两倍"/"47.3%"）独立重算全部准确，未发现比例混淆；与站内yes-or-no-wheel/coin-flip-simulator等姊妹工具对比，结构共享（cumulative-weight措辞）为已知方法论复用而非模板换词；正文零AI高频词命中，仅"rolling a die twice"类比在页面内3处重复使用（低严重度，未强制改写）。**教训库新增2条**：`L-0816-1`（新工具组件沿用姊妹组件的session累积状态模式时，须显式核对姊妹组件是否已为"底层选项集变化"加过重置逻辑）、`L-0816-2`（断言两个独立来源"排序一致"须真正核对第二来源自身排序，不能只核对数值接近）——均为独立审核agent发现的新通用问题，非已有条目复现，已追加进`独立站/内容通用教训库.md`（仅追加，未整体重写）。
- **测试与验证**：`npx vitest run tests/randomLetter.test.ts` 24/24通过（含equal/frequency两种加权、vowels/consonants子集边界、count 0/负数/小数floor、tally排序等）；修复后全量`npm test` 724/724通过（36个测试文件）；`npm run build` 80页成功（含`/random-letter-generator/`与`/embed/random-letter-generator/`）。
- **浏览器实测**：`preview_start`对dev server报"Dev servers can't be started from unattended sessions"（无人值守定时任务限制），改用`python3 -m http.server`托管`dist/`产物+Browser pane实测替代——桌面视口下equal权重模式生成5字母（含重复字母校验通过）、切换frequency权重后"Most likely to come up"正确显示E(12.7%)与参考表一致、切换Vowels后"Most likely"显示E(33.2%)（=12.7/38.2独立验算一致）、输入25（超max 20）时按钮正确disabled且提示"Enter between 1 and 20 letters."、控制台console.error为0；375px移动端布局初版"Consonants only"标签被截断（3选项Segmented首次在站内出现真实词汇标签，此前均为2选项或纯数字），改标签为"Vowels"/"Consonants"（去掉"only"后缀）后移动端无截断，重新build+复测通过；`/embed/random-letter-generator/`渲染正常，含"Powered by CalcBadger Random Letter Generator"署名链接，network请求全部200。
- **比较级/SEO字段检查**：`check_comparatives.py`对正文抽取文本命中1句（"could include the same letter two or three times"），判定为独立抽取的可能性示例而非需精确核实的统计断言，未改写；`check_seo_field_stats.py --new-slug random-letter-generator`：title长度14字符z=-1.24正常，description长度158字符z=-0.64正常，均在全站分布范围内。
- **发布**：两次commit——`dfe3417`（工具本体，commit message已注明加权抽样方法与来源）、`b05492a`（IndexNow提交记录）；提交时发现仓库另有`broken-link-outreach-log.md`/`outreach-drafts.md`两个文件被并发任务修改，仅`git add`本次相关的5个文件，未一并提交避免误伤并发任务的工作；CF Pages自动部署，curl轮询4次（约60秒）后200且正文含预期标题与描述；IndexNow提交`/random-letter-generator/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第305行`random letter generator`标`[已用 2026-08-16]`，改动前后总行数断言1637不变（二次wc -l核对）。
- 独立审核agent本次正常完成并返回报告（约9.6分钟，28次工具调用），未触发全局CLAUDE.md看门狗规则里的卡死降级流程。审核过程发现2处真实问题（排序断言不实、UI状态未重置）均已修复并二次验证（重新test+build+浏览器复测）。无推翻核心结论的情形，未触发Owen邮件升级。附带说明：审核agent核对教训库时提到本文件（`内容通用教训库.md`）活跃条目数已超过第五节"上限40条"的规模控制线（本次追加前53条，追加后55条），这是长期累积的housekeeping债务，非本次发布任务范围内可处理，已如实记录供以后专门任务合并/毕业处理，未在本次擅自做整体重写。

## 2026-08-16 — Calligraphy Alphabet（第33个工具，commit `473adc9`）

- **选题**：发布顺序总表第216行`heloc calculator`起为首个未标`[已用]`行，但标有`[⚠️权威壁垒风险]`（银行/信用社自建计算器垄断SERP），跳过；第218行`5 7 in inches`同样标壁垒风险，跳过；第223行`random pokemon generator`无壁垒标记但类目会落入`Games`，与前一日`random-letter-generator`（Games）撞类目，跳过；第224行`calligraphy alphabet`无壁垒标记、类目预期为`Reference`（跟greek-alphabet/cursive-alphabet同类），与前一日类目不同，**选中本行**。选中前用WebSearch核实真实SERP（Pinterest/Adobe Stock/YouTube/calligraphy-skills.com/loveleighloops.com等混合结果，无机构级垄断），判定无权威壁垒风险，未回词库补标。
- **公式/内容研究**：本工具无数值公式，核心是三种书法字体历史准确性。逐条WebSearch+curl直连一手信源核实：Blackletter/textualis 12世纪西北欧起源+Gutenberg 1450s印刷用textura（Wikipedia "Blackletter"）；Fraktur为16世纪早期Maximilian I委托、设计者Hieronymus Andreae、1513年祈祷书Gebetbuch（Wikipedia "Fraktur"原文逐字核对，初稿曾误写"专为祈祷书委托"，已按原文改为"为一批帝国作品委托，包括凯旋门木刻画与祈祷书"）；Chancery Italic的Cresci 1579年《Il perfetto cancellaresco corsivo》改良（Oxford Reference+Internet Archive原书扫描件确认）；Round Hand 1660s英格兰Ayres&Banson（Wikipedia "Round hand"页，初稿曾误挂在"Copperplate script"页下，已改用正确的信源页面）；Copperplate得名于铜版雕刻+Bickham《The Universal Penman》1733-1741（Wikipedia "Copperplate script"+"George Bickham the Elder"双页交叉核对）。三款字体（UnifrakturMaguntia/Tangerine/Alex Brush）的设计者/年份/灵感来源均直接抓取`raw.githubusercontent.com/google/fonts`官方`DESCRIPTION.en_us.html`/`METADATA.pb`原始元数据核实，未采信第三方字体目录站的说法；`UnifrakturMaguntia`"得名于Mainz拉丁名Moguntia"这条trivia因只有第三方聚合站提及、官方元数据未确认，主动删除未采信。
- **开发**：3种书法风格通过Segmented切换（key: blackletter/chancery/brush），实时预览+完整A-Z图表复用cursive-alphabet的sanitize文本模式；三款字体均为SIL OFL 1.1开源许可（`@fontsource/unifrakturmaguntia`/`tangerine`/`alex-brush`，逐一核对node_modules内LICENSE文件）。移动端Segmented标签初版用完整名称（"Chancery Italic"/"Modern Brush"）在375px下被截断，改为`shortLabel`短标签（"Chancery"/"Brush"，配`title`属性保留完整名称供屏幕阅读器）+补充测试后消除截断。
- **验证**：`npx vitest run`742/742通过（含新增18个）；`npm run build`82页成功生成，含`/calligraphy-alphabet/`与`/embed/calligraphy-alphabet/`；无人值守会话`preview_start`不可用，改用`python3 -m http.server`托管`dist/`+Browser pane实测替代——三种字体样式切换渲染正确且视觉上彼此明显不同（非同一字体换皮）、输入含撇号姓名正常预览、桌面/375px移动端均无横向滚动/无截断、控制台0报错；embed页在600px/400px两档宽度下实测内容高度分别904px/1200px，据此把`embedHeight`从初版1150上调到1250留足余量。
- **比较级检查**：`check_comparatives.py`命中1句（"a few centuries later"描述12世纪blackletter起源到1440s-1450s Gutenberg印刷的时间跨度），实算约250-350年，"a few centuries"准确，未改。
- **SEO字段离群值检查**：`check_seo_field_stats.py --new-slug calligraphy-alphabet`：title 32字符z=0.01、description 172字符z=0.23，均在全站正常分布范围内。
- **独立审核agent**（正常完成，约10.6分钟，68次工具调用，未触发看门狗降级流程）：FAIL，2处阻断项，均已修复并二次验证——① FAQ"calligraphy vs cursive"一条声称Blackletter和Chancery Italic都"从未用于日常快速记录"，与正文Chancery Italic小节自己所写"Cresci的改良让这套字体更适合日常通信和记账"直接矛盾（同一篇文章内自相矛盾，命中教训库L-0805-5同类失效模式）；已重写FAQ答案，把"日常实用"的表述准确归到Chancery Italic、把"缓慢正式"的表述归到Blackletter，消除矛盾。② "How to use this tool"第二段与2026-08-10发布的姊妹页`cursive-alphabet`对应段落逐句同骨架换词（"是替代X的展示字体，不是逐笔指南。不会显示...的方式。实际用途是作为参照：看看..."），命中教训库L-0804-11、且是该细分变体（"操作指引段落"）第2次复发，已达复发≥2次升级门槛；已整段重写，改为聚焦"三款字体均基于真实历史原型数字化而非通用花体字"这一新事实（呼应本次的信源调研），结构和信息顺序与cursive-alphabet完全不同。独立审核agent同时确认：全部历史事实/字体元数据/开源许可证核实通过，742测试全过、build成功、三种字体浏览器实测渲染正确，humanizer/avoid-ai-writing零命中，FAQ可独立提取，无手写内链断链风险。审核agent已按教训库规则追加两条复发记录（L-0804-11第2次复发+L-0805-5新变体，均为追加不改写原文件）。
- **发布**：单次commit `473adc9`（工具本体：`src/lib/calligraphyAlphabet.ts`、`tests/calligraphyAlphabet.test.ts`、`src/components/calculators/CalligraphyAlphabetGenerator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`、`package.json`/`package-lock.json`新增3个字体依赖；已含独立审核后的两处重写）；提交时发现仓库另有`broken-link-outreach-log.md`被并发任务修改，仅`git add`本次相关的8个文件，未一并提交；CF Pages自动部署，curl轮询3次（约30秒）后从404转为200，确认正文含"Calligraphy Alphabet"标题、结构化数据含WebApplication+FAQPage+BreadcrumbList；IndexNow提交`/calligraphy-alphabet/`：Bing 200 / Yandex 200。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第224行`calligraphy alphabet`标`[已用 2026-08-16]`，改动前后总行数断言1637不变（二次wc -l核对）。

## 2026-08-16 — Weight Converter 内容质量审计更新（本条为content-quality-audit审计更新，非新发布，commit `e74748c`）

- **背景**：`trafficsite-content-quality-audit`任务对`weight-converter`（本站从未被审计过，2026-08-04首发，9个已审计工具之后按`tools.ts`文件顺序的下一个候选）做首次深度复审，聚焦公式正确性与组件可用性（工具站审计维度，非长文写作维度）。
- **诊断专属核查重点**：7种质量单位（mcg/mg/g/kg/oz/lb/US ton）换算系数是否与NIST HB44/1959国际码磅协定一致、4张参考表与3条worked example数值是否可独立复算、极大/极小量级下`roundSig()`是否有浮点残留（本站`length-converter`2026-08-13审计已确认同款`roundSig`实现在极端量级下有IEEE-754残留，需要核对`weight.ts`是否有同一未修复的bug）、meta description等SEO字段是否健康。
- **公式/EEAT核实**：直接下载NIST Handbook 44（2026版）Appendix C原始PDF（非WebSearch摘要）逐字核对，确认"453.592 37"（磅→克）、"28.349 523 125"（盎司→克）、"907.184 74"（短吨→千克）、"31.103 476 8"（金衡盎司→克）四组数字均逐字存在于官方PDF表格中，与`weight.ts`/`tools.ts`引用完全一致；金衡盎司比常衡盎司重9.7%（31.1034768/28.349523125-1）独立复算确认。用Python Decimal（30位精度）独立重算全部referenceTables（4张表，共27行）、3条worked example、faq中全部数字，与`tests/weight.test.ts`及正文逐一比对全部吻合。NIST两条sources链接curl均200，无死链。
- **发现并修复3处真实问题（均经独立fresh-context agent复核CONFIRMED后修复）**：
  1. **roundSig()浮点残留复发**（组件可用性/正确性维度，与length-converter 2026-08-13审计发现的同一bug）：`src/lib/weight.ts`的`roundSig()`沿用`Math.round(n*magnitude)/magnitude`（`magnitude`由`Math.pow(10, 负指数)`算出）旧实现，length-converter当时的修复（改用`Number(n.toPrecision(sig))`）未同步传播到这个姊妹文件。独立agent用Node复现：`convert(1e11,'ton','mcg')`原始值`9.0718474e+22`，旧算法产出`9.071850000000001e+22`（第6位有效数字之后残留非零位），`1e12 kg→mcg`更极端，旧算法产出形似20个9的`999999999999999900000`而非干净的`1e+21`；本工具单位跨度达mcg↔US ton共12个数量级，用户可通过UI直接输入触发（`NumberField`无max上限）。**修复**：同步改用`Number(n.toPrecision(sig))`，验证正常量级测试值不受影响，`tests/weight.test.ts`27项及全站742项测试修复后全部通过。
  2. **meta description超长**（SEO维度）：description字段172字符，超出~155-160字符SERP安全区间。**修复**：精简到156字符，保留完整7单位列表与"instantly"关键词，及4个差异化参考表主题中的3个（body weight/cooking/dosing，因搜索查询列表中无明确匹配"newborn"的词，判定为最可舍弃的一项）；同一字段同时驱动meta description/og/twitter description/JSON-LD WebApplication.description/页面首屏可见导语，单处改动已全部同步生效。
  3. **3处用户可见em dash**（AI写作痕迹维度，本工具2026-08-04发布早于8/7规则生效日，属强制回查范围）：`tools.ts`正文1处("Neither...definition — both are derived")+`WeightConverter.tsx`渲染文案2处("can't be negative — enter"/"NIST Handbook 44) — not measured")，均改为句号/逗号消除。`sources[]`引用标签内1处em dash（"NIST — SI Units: Mass"）经核对为站内跨15+工具沿用的"机构 — 标题"引用惯例格式（length-converter 2026-08-13审计已明确判定同类实例为"站内既有惯例格式，非叙事问题，未改动"），本次同样未改动，避免制造单工具例外。
- **三处均为定点修改**，未做大范围重写；`npm run build`（82页）成功后，`git status`确认另有`broken-link-outreach-log.md`被同仓库并发任务修改，仅`git add`本次相关的3个文件（`src/lib/weight.ts`/`src/data/tools.ts`/`src/components/calculators/WeightConverter.tsx`），单独commit `e74748c`并push。
- **部署与验证**：push后curl轮询确认线上description已生效部署（第2次轮询命中）；curl构建产物+线上JS bundle（`WeightConverter.CFk8Nsbv.js`）确认`roundSig`修复与两处em dash修复均已实际部署到客户端渲染路径，非仅静态HTML层面。`node tools/submit-indexnow.mjs /weight-converter/`提交成功（Bing 200/Yandex 200）。
- **未发现问题的维度**：GEO（按`Skill(ai-seo)`引导的Content Extractability Check人工核对，coreSummary/3条worked examples/4张reference tables/7条FAQ+FAQPage schema/时效信号/AI爬虫放行robots.txt等9/10项清晰通过，等效明显高于≥80门槛，唯一软性缺口为无独立作者署名，全站模板级设计非本工具专属问题）、竞品差异化（WebSearch+curl核对unitconverters.net等头部竞品，虽同样含历史/定义类说明文字，但CalcBadger额外提供3条具体worked example+同时显示6个单位换算结果的UX，构成真实增量）、内链健康度（用真实`pickRelatedGuides`+crossCategory轮转逻辑跑全站33个工具的node模拟，33/33覆盖，weight-converter被正常链接；顺带复核发现独立跑`pickRelatedGuides`未含crossCategory环节会误判`square-footage-calculator`为孤儿页，加回完整轮转逻辑后确认33/33无孤儿，该发现与本次weight-converter审计范围无关，未处理，仅记录避免误判）、schema一致性（WebApplication/FAQPage/BreadcrumbList三处JSON-LD线上核对与tools.ts数据一致）、合规（`/terms/`"No professional advice"条款覆盖medical场景）、AdSense（ads.txt/robots.txt均正常）、图片可用性（无正文配图，纯favicon兜底，符合预期）。
- 3处独立审核均由全新fresh-context sub-agent各自CONFIRMED（roundSig用独立Node脚本复现、meta description独立curl+计数复核、em dash逐行读取源码复核），未触发全局CLAUDE.md看门狗规则里的卡死降级流程。无推翻核心结论的情形，未触发Owen邮件升级。

## 2026-08-17 — Asphalt Calculator（第34个工具，commit `9f46582`）

- **选题（逐行交代，按2026-08-12规则）**：发布顺序总表上次已用行为第224行`calligraphy alphabet`（2026-08-16）。第223行`random pokemon generator`此前未标记，本次判定跳过并回词库标注：Pokémon是Nintendo/Game Freak注册商标，广告变现站上生成/罗列具体角色数据的商业性使用商标关联风险偏高，虽不在词库红线清单里明确列出但精神一致（行246`pokemon-type-calculator`同样受影响，已同步记录供以后核对）。第225-226行`female/male symbol`跳过：与前两次运行（random-letter-generator=Games、calligraphy-alphabet=Reference）中的Reference类目仍重复，且二词体量小、逻辑上应合并一页，非放弃，留待后续Reference轮次。第227行`board-foot-calculator`已标`[已用 2026-08-13]`。第228行`wingdings translator`跳过：CPC高($9.44)但Wingdings字体本身是Microsoft专有资产无法直接嵌入，需Unicode PUA映射+自制替代字形方案，实现门槛超出本次运行时间，留待专门实现。第229行`how many bottles of water is a gallon`跳过：C层单值换算词，判定应并入已发布的volume-converter页FAQ而非新开页。第230行`click speed test`跳过：Games类目已在近两次运行中出现过，且与已发布的reaction-time-test机制相近需要更多差异化设计时间。第231行`how many miles is a half marathon`跳过：应并入已发布的length-converter页"Common race distances"参考表（该表已含10K行，见第213行备注）。第232行`approximately symbol`已标`[已用 2026-08-13 并入math-symbols页]`。第233行`graffiti alphabet`跳过：Reference类目本轮已连续两次分散跳过。第234行`section symbol`跳过：CPC $25.80远高于同类符号词（其余多为$0-0.3），疑似close-variant污染（参照本文件红线部分"hex to decimal"同类案例），需先核实真实CPC构成再定优先级。第235行`asphalt calculator`（A层，月搜索量27,100，KD0，CPC $3.68）**选中**——类目Construction，与近两次Games/Reference形成分散，且此前未标注权威壁垒风险。
- **权威壁垒预检**：WebSearch"asphalt calculator"/"asphalt calculator tool site"实测SERP，结果为独立计算器站（thecalculatorsite/gigacalculator/omnicalculator/homeprojectcalculator）、区域铺路承包商（Craig Paving/DRYCO/American Asphalt）、一个州级行业协会（Minnesota Asphalt Pavement Association，非营利贸易组织非企业垄断）混合分布，无单一品牌/机构垄断，判定无权威壁垒风险，未回词库补标。
- **公式研究**：吨数 = 长(ft)×宽(ft)×压实深度(ft)×密度(lb/ft³)/2000。密度取两档：热拌沥青145 lb/ft³（Iowa DOT Standard Specifications第2303节，用于将投标数量换算为吨位基准，落在Asphalt Institute Engineering FAQ给出的142-148 lb/ft³原位密度区间内，两个独立信源互相印证）；再生沥青(RAP)112 lb/ft³（FHWA-RD-97-148《User Guidelines for Waste and Byproduct Materials in Pavement Construction》记录的1,600-2,000 kg/m³=100-125 lb/ft³压实区间，取接近区间中点的数值，因125真实中点为112.5非112，正文措辞用"near the midpoint"避免精度虚高）；另设custom密度输入项覆盖冷补料等未被单一标准机构统一记录密度的材料，不替它们编造具体数字。
- **开发**：`src/lib/asphalt.ts`纯函数模块+`AsphaltCalculator.tsx`（长宽单位切换/压实深度单位切换/材料Segmented三态hotmix-rap-custom/废料损耗率/可选单价估算），复用已验证的NumberField/Segmented UI原语，未写原生表单控件。`tests/asphalt.test.ts`15项，期望值独立手算（24×12ft驱道2in热拌=48ft³=6,960lb=3.48吨，5%损耗后3.654吨；150×40ft场地4in RAP层=2,000ft³=224,000lb=112吨整，两例数字均为干净整数，便于人工复核）。
- **验证**：`npm test`757/757通过（38个测试文件）；`npm run build`84页成功，含`/asphalt-calculator/`与`/embed/asphalt-calculator/`；无人值守会话`preview_start`报"Dev servers can't be started from unattended sessions"，改读`dist/`构建产物核对——静态HTML含WebApplication+FAQPage+BreadcrumbList三类schema、`<title>`/`<h1>`正确、6条FAQ全部渲染、embed页含"Powered by"署名链接、无NaN/undefined泄漏；`python3 独立站/scripts/check_comparatives.py`对正文文本命中0句（体量小、以公式/权威来源为主，属正常）；`python3 独立站/scripts/check_seo_field_stats.py --new-slug asphalt-calculator`：title 51字符z=1.28正常范围，description 158字符z=-0.61正常范围。
- **humanizer+avoid-ai-writing双重检查**：`Skill(humanizer)`扫描发现正文6处叙事性em dash（非sources引用标签内的惯例格式，是真实的句内插入），逐一改写为句号/逗号/冒号后重扫零命中；`Skill(avoid-ai-writing)`detect模式复扫全部sections+FAQ+coreSummary+description，未发现Tier1/2词表命中、rule-of-three滥用、模板化"挑战与展望"段落等P0/P1问题，判定为clean。
- **独立审核agent**（正常完成，约4.4分钟，30次工具调用，未触发看门狗降级流程）：PASS，1项精度措辞建议已采纳，1项未解决的信源核验缺口如实记录——① 两条worked example与参考表6行独立手算复核全部吻合；②Asphalt Institute Engineering FAQ、FHWA-RD-97-148两条来源均被审核agent直接抓取原始页面逐字核对（"142 to 148 pounds per cubic foot"/"1600-2000 kg/m³ (100-125 lb/ft³)"均逐字匹配），Iowa DOT Section 2303因该域名有Cloudflare反爬限制、curl与浏览器均被拦截，未能直接抓取原文验证，仅有WebSearch索引摘要佐证（"unit weight of 145 pounds per cubic foot unless otherwise stated on the plans"），判定为"未解决的验证缺口"而非"来源不实"——已如实记录，供以后有权限的会话补验；③正文与board-foot-calculator/concrete-calculator结构对比确认非模板换词（各自公式/信源/算例均独立差异化）；④测试期望值确认为独立手算而非实现输出回填；⑤指出"the midpoint"措辞对RAP区间(100-125)不够精确（真实中点112.5非112），已采纳并同步改写`tools.ts`/`AsphaltCalculator.tsx`/`asphalt.ts`/`asphalt.test.ts`四处为"near the midpoint"表述；⑥核对`内容通用教训库.md`确认本次未发现的问题、也未产生需要追加的新通用条目。
- **发布**：单次commit `9f46582`（工具本体：`src/lib/asphalt.ts`、`tests/asphalt.test.ts`、`src/components/calculators/AsphaltCalculator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`）；提交时发现仓库另有`broken-link-outreach-log.md`被并发任务修改、及一个indexnow日志的时间戳备份文件，均未纳入本次commit；CF Pages自动部署，curl轮询3次（约30秒）后从404转200，正文含"Asphalt Calculator"标题；`/embed/asphalt-calculator/`同步200；IndexNow提交`/asphalt-calculator/`：Bing 200 / Yandex 200。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第235行`asphalt calculator`标`[已用 2026-08-17]`，同时补齐第223/225/226/228/229/230/231/233/234行的跳过理由（按2026-08-12新规），改动前后总行数断言1637不变（二次wc -l核对）。

## 2026-08-17 — Glitch Text Generator（第35个工具，commit `aaebac0`，本日第二次运行）

- **选题**：上次已用行为第235行`asphalt calculator`（本日第一次运行，Construction类目）。第236行`glitch text generator`（B层，月搜索量27,100，KD4，CPC $0.00）**选中**——类目定为新设的"Text Tools"，与本日第一次运行的Construction形成分散；此前未被词库标记为已用/权威壁垒/跳过。权威壁垒预检：WebSearch"glitch text generator"，SERP前7个结果为Glyphy/Creative Fabrica/LingoJam/PSD-Dude/Pixelied/Coddy/GlitchText.Cool等独立小型工具站，无银行/制药/大型媒体品牌垄断，判定无权威壁垒风险。
- **公式研究**：核心是Unicode Combining Diacritical Marks区块（U+0300–U+036F）的组合标记随机叠加算法（俗称zalgo/glitch text）。直接curl `unicode.org/Public/UCD/latest/ucd/UnicodeData.txt`取官方Unicode Character Database原始数据，用Python按字符名称规则（含"BELOW"/"LOW LINE"/"CEDILLA"/"OGONEK"→下方；含"OVERLAY"/"SOLIDUS"→穿透；其余→上方）程序化分类全部112个码位为MARKS_UP(60)/MARKS_DOWN(46)/MARKS_MID(5)，排除隐形的U+034F（COMBINING GRAPHEME JOINER），三桶互不重叠、合计覆盖111个非排除码位。4档强度（Mild 0-2/Medium 2-5/Heavy 5-10/Extreme 10-18，每字符每方向随机区间）+3种方向（Up only/Down only/Up+down，后者额外叠加约1/3数量的穿透标记）。
- **开发**：`src/lib/glitchText.ts`纯函数模块（`glitchify()`接受可注入RNG便于测试）+`GlitchTextGenerator.tsx`（多行文本输入+Intensity/Marks两个Segmented+Shuffle按钮重新随机化+Copy按钮+Base characters/Marks added/Avg marks per character/Style四项calc-results），复用已验证的Segmented UI原语，自由文本输入用原生`<textarea>`（非本站禁用的number/select类原生控件，不受硬规则约束）配`.glitch-textarea-shell`定制样式。`tests/glitchText.test.ts`19项：mark表覆盖率/无重叠/码位范围断言、空输入/纯空白输入、方向隔离（up-only不产生down标记等）、stripGlitch往返一致性、intensity单调性、RNG确定性可复现（injectable random）。
- **验证**：`npm test`776/776通过（39个测试文件）；`npm run build`87页成功，含`/glitch-text-generator/`与`/embed/glitch-text-generator/`；无人值守会话`preview_start`报"Dev servers can't be started from unattended sessions"，改读`dist/`构建产物核对——静态HTML含WebApplication+FAQPage+BreadcrumbList三类schema、`<title>`/`<h1>`正确、6条FAQ全部渲染、参考表渲染、embed页含"Powered by"署名链接、无NaN/undefined泄漏；Astro islands为isomorphic渲染，静态HTML里已能看到服务端用真实Math.random()算出的"glitch"字符glitch化结果，`stripGlitch()`还原后精确等于原字符串，calc-results区显示Base characters=6/Marks added=64/Avg=10.7（64/6=10.67对得上，且落在Medium+Up+down理论上限72以内），证明逻辑端到端正确而非仅单测通过；`python3 独立站/scripts/check_comparatives.py`对正文文本命中0句（属正常，讲解以技术机制/历史事实为主非比较级论证）；`python3 独立站/scripts/check_seo_field_stats.py --new-slug glitch-text-generator`：title 16字符z=-1.09正常范围，description 180字符z=0.77正常范围。
- **humanizer+avoid-ai-writing双重检查**：`Skill(humanizer)`+`Skill(avoid-ai-writing)`自查发现2处"worth doing"类模糊背书表述（"worth pasting"/"worth remembering"）已改写为直接祈使句；1处正文内`"Up + down"`用了弯引号已改直引号；`sources[].label`内2处em dash（惯例引用标签格式）经独立审核agent复核后判定仍需清零（见下）。narrative字段（coreSummary/sections/faq）最终零em dash、零弯引号、零delve/robust/seamless/testament/underscore等词表命中。
- **独立审核agent**（正常完成，约6.1分钟，42次工具调用，未触发看门狗降级流程）：verdict为PUBLISH WITH MINOR FIXES，两项已采纳修复——①`sources[]`第二条把"pre-iOS 8.4 Messages崩溃bug于2015年年中修复"这一具体细节错误挂到了"Wikipedia — 'Zalgo text'"这个来源标签上（审核agent对该Wikipedia条目全文检索"8.4"/"8.3"/"2015"均查无结果，该条目仅以"参见"形式链接到"effective power" bug、未自行给出版本号/日期），已拆分为独立的第三条sources，新增来源为Wikipedia"SpringBoard"条目"effective power" bug小节（审核agent与本次运行各自独立curl核实该条目原文确有"With the release of iOS 8.4 on June 30, 2015, the 'effective. Power' bug has been fixed."字样，二次交叉验证一致）；②`sources[].label`里2处em dash（"Unicode Consortium — ..."/"Wikipedia — 'Zalgo text' ..."）已改写为逗号分隔消除。其余五项（Unicode码位分类全量核对/测试套件真实性——审核agent故意交换MARKS_UP/MARKS_DOWN验证有2个测试立即失败后已还原/与random-letter-generator及asphalt-calculator做6-gram短语查重零重合/UI逻辑与状态管理/CalculatorIsland.astro注册）均判定PASS。核对`内容通用教训库.md`：本次问题①属于L-0805-21"数字真但归因信源错"模式在结构化`sources[]`字段（而非叙事内联归因句）里的新载体，问题②属于L-0810-4"破折号检查漏扫sources[].label元数据字段"的再次复发（CalcBadger站内第二次），均已追加复发记录到该文件对应条目（只追加未整体重写）。
- **发布**：单次commit `aaebac0`（工具本体：`src/lib/glitchText.ts`、`tests/glitchText.test.ts`、`src/components/calculators/GlitchTextGenerator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`、`src/styles/global.css`）；提交时发现仓库另有`broken-link-outreach-log.md`被并发任务修改、及一个indexnow日志的时间戳备份文件，均未纳入本次commit；CF Pages自动部署，curl轮询4次（约75秒）后从404转200，正文含"Glitch Text Generator"标题；`/embed/glitch-text-generator/`同步200；IndexNow提交`/glitch-text-generator/`：Bing 200 / Yandex 200。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第236行`glitch text generator`标`[已用 2026-08-17]`，改动前后总行数断言1637不变（二次wc -l核对）。

## 2026-08-17 — Random Animal Generator（第36个工具，commit `63b9911`，本日第三次运行）

- **slug** `random-animal-generator`，**category** `Games`（本日已发布Construction/Text Tools，与前两次不同）。
- **选题（逐行交代）**：上次已用行为第236行`glitch text generator`（本日第二次运行，Text Tools类目）。第237行`random animal generator`（B层，月搜索量27,100，KD0，CPC $0.00）**选中**——与已发布的random-letter-generator/coin-flip-simulator同属随机生成器子类但对象不同（动物而非字母/硬币），此前未被词库标记为已用/权威壁垒。第237行与第236行相邻，无需跳过中间行。权威壁垒预检：WebSearch"random animal generator tool"，SERP前9个结果均为独立小工具站（thewordfinder/randomanimalsgenerator/randomlists/randommer等），无动物园/教育机构官方站垄断，判定无权威壁垒风险。
- **公式/数据权威来源**：本工具非公式计算器，是从131个精选动物（含分类class+主要原生地region）里等概率抽取的生成器。分类依据美国联邦ITIS（Integrated Taxonomic Information System）四份Class报告（Mammalia TSN 179913/Aves TSN 174371/Reptilia TSN 173747/Amphibia TSN 173420，均curl核实页面确实显示对应Class名称+"Taxonomic Rank: Class"），"Fish"和"Invertebrate"两个非正式伞状标签交叉核对圣地亚哥动物园Wildlife Explorers的分组方式（amphibians/arthropods/birds/fish/mammals/reptiles六类，与本页六类一致）。
- **抽取算法**：`Unique only`模式用partial Fisher-Yates去除法做无放回抽样（保证同批次不重复）；`Allow repeats`模式做独立有放回抽样（同一动物可连续出现），与站内random-letter-generator/coin-flip-simulator用的机制一致但应用对象不同。
- **测试**：`tests/randomAnimal.test.ts`14个用例，含数据集完整性断言（131条目/6大类计数精确匹配52/26/16/10/12/15/零重名）与抽取逻辑断言（注入固定rng序列验证有放回/无放回的精确抽取顺序，无放回超出池容量时正确封顶而非报错），期望值均独立计算/手推，不是拿实现输出回填。
- **worked example概率复核（发现并修正1处真实数学错误）**：撰写"为什么unique和repeats的概率略有不同"一节时，初稿错误地写"若mammals是多数(占比>50%)方向会反转，unique抽取概率会略高于repeats"——`check_comparatives.py`未命中此表述（触发词表不完整，脚本本身承认），按站规仍手工用Python代数验证（Fraction精确分数）：无放回连续同类抽取概率**在任何多数/少数占比下都恒小于等于**有放回概率（因为移除一个已抽中的成功项，只要该类不是100%，剩余池子里该类占比必然略微下降，这是超几何分布负相关的通用性质，与该类原始占比是否过半无关），用K=52/79/10/121（对131）四组数据数值验证结论一致，初稿的"方向会反转"是编造的错误结论，已改写为正确的通用表述（"无放回抽样恒不高于有放回抽样，与多数/少数无关"）。这是本次运行内自行发现修正，不是审核agent发现的。
- **独立审核agent**（正常完成，约6分钟，39次工具调用，未触发看门狗降级流程）：verdict为PUBLISH WITH MINOR FIXES。**发现并采纳2处地理标注错误**：`randomAnimal.ts`里`Keel-billed Toucan`与`Red-eyed Tree Frog`原标注`region: 'South America'`，审核agent独立WebSearch核实National Geographic/Cincinnati Zoo/Birds of the World等来源后确认两者实际原生地"主要在中美洲，仅少量延伸到南美洲北部"（如墨西哥南部经中美洲到哥伦比亚北部），判定"South America"标注不准确，已改为`'Central America'`；另标记`RandomAnimalGenerator.tsx`一处结果列表用的em dash（"— {a.animalClass} · {a.region}"）虽判定为UI分隔符非叙事正文、不算阻断项，但仍已顺手清零改为纯`·`分隔。审核agent独立复算的worked example数字（无放回0.8733%/有放回0.9855%/mammal占比39.69%）与本次运行的数字完全吻合，且独立brute-force验证了"无放回恒≤有放回"这条通用结论对k=10/52/100/120四组数据均成立，交叉确认了本次运行内自行发现修正的那处数学错误确实已修好。审核agent额外自行核实了20+动物的分类/原生地（含Turkey Vulture/Brown Pelican/Common Vampire Bat/Nine-banded Armadillo/Jaguar/Common Kingfisher等宽分布物种），均判定合理，taxonomic class（Mammal/Bird/Reptile/Amphibian/Fish/Invertebrate）全部核对无误。修复后本次运行额外自查了Scarlet Macaw/Green Iguana两个同样"墨西哥到南美洲"分布模式的物种，WebSearch确认二者在南美洲的分布规模（亚马逊盆地"最常见"/南美洲被认为是演化起源地）远比toucan/tree frog更均衡，判定维持"South America"标注合理，未额外修改。
- **humanizer+avoid-ai-writing双重检查**：撰写阶段直接规避已知AI写作特征清单（Tier1/2/3词表零命中、零em dash、零弯引号、无rule-of-three堆砌、标题均sentence case），审核agent复核narrative字段（coreSummary/sections/faq/sources labels）同样零命中，仅发现上述1处组件UI层的em dash（非叙事正文）已顺手修复。
- **验证**：`npm test`790/790通过（40个测试文件，较修复前776/39净增14个测试1个文件）；`npm run build`89页成功，含`/random-animal-generator/`与`/embed/random-animal-generator/`；无人值守会话`preview_start`报"Dev servers can't be started from unattended sessions"，改读`dist/`构建产物核对——静态HTML含WebApplication+FAQPage+BreadcrumbList三类schema、`<title>`/`<h1>`正确、6条FAQ全部渲染、参考表渲染、Select初始态显示"All animals (131)"与"Animals in this pool"=131/"Chance per draw"=0.8%（1/131四舍五入）、无NaN/undefined泄漏；`python3 独立站/scripts/check_comparatives.py`对正文文本命中0句（脚本本身提示触发词表不完整，已按上述流程手工核实"a little higher"/"slightly less likely"等比较级表述，发现并修正1处真实错误，见上）；`python3 独立站/scripts/check_seo_field_stats.py --new-slug random-animal-generator`：title 19字符z=-0.90正常范围，description 172字符z=0.26正常范围。
- **发布**：单次commit `63b9911`（工具本体：`src/lib/randomAnimal.ts`、`tests/randomAnimal.test.ts`、`src/components/calculators/RandomAnimalGenerator.tsx`、`src/components/CalculatorIsland.astro`、`src/data/tools.ts`）；提交时发现仓库另有`broken-link-outreach-log.md`被并发任务修改、及一个indexnow日志时间戳备份文件，均未纳入本次commit；CF Pages自动部署，curl轮询3次（约50秒）后从404转200，正文含"Random Animal Generator"标题；`/embed/random-animal-generator/`同步200；IndexNow提交`/random-animal-generator/`：Bing 200 / Yandex 202。
- **词库标记**：`独立站/流量站词库/CalcBadger-关键词库.md`第237行`random animal generator`标`[已用 2026-08-17]`，改动前后总行数断言1637不变（二次wc -l核对）。

## 2026-08-17 — Mortgage Calculator 内容质量审计更新（本条为content-quality-audit审计更新，非新发布，commit `67624d8`）

- **背景**：`trafficsite-content-quality-audit`任务对`mortgage-calculator`（本站26个从未被审计过的工具中，用git commit时间戳打破并列后选中的最早一个，2026-08-05首发）做深度复审，聚焦公式正确性与组件可用性（工具站审计维度）。
- **诊断专属核查重点**：级payment amortization公式M=P[r(1+r)^n]/[(1+r)^n-1]是否与Regulation Z Appendix J actuarial method一致；worked example全部具体数字（$320,000贷款/$2,022.62月供/年度余额表/15v30年对比）是否可独立复算；PMI 0.5-1.5%、HPA 78%/80%终止阈值等监管断言是否权威；组件.tsx硬编码文案是否也有AI写作特征。
- **公式/数据核实**：用独立Node脚本（非复制实现代码）重新实现公式，逐一验证tools.ts全部具体数字（$320,000贷款额、$2,022.62月供、$728,142.36总还款、$408,142.36总利息、7行年度余额表、15v30年对比表全部数字）精确匹配到分；4组不同本金/利率/期限组合验证余额单调递减且到期归零；仓库自带`tests/mortgage.test.ts`（15项，作者注明用Python独立算出期望值）与本次独立复现完全吻合，双重独立验证。发现一处3美分级的舍入现象（正文"$24,271.44 has been paid"用12×四舍五入后月供而非12×精确月供，相差$0.03）——判定为标准金融文案惯例、不具实质性，不构成问题，未修改。Reg Z Appendix J、CFPB两个mortgage说明页、HPA examination procedures页均curl返回403（consumerfinance.gov对curl的机器人防护），改WebSearch核实三条链接均在线且标题内容一致。PMI 0.5-1.5%区间、HPA 78%/80%阈值均WebSearch独立核实为准确行业/法规惯例。
- **发现2处真实问题（均经独立fresh-context agent复核CONFIRMED后修复）**：
  1. **meta description超长**（SEO维度）：description字段187字符，超出150-160字符SEO惯例上限，同一字段复用进WebApplication schema description/meta/og/twitter description/页面首段导语共3+处曝光点。**修复**：精简到150字符，保留全部6类费用组成（principal/interest/taxes/insurance/PMI/HOA）+总利息+3类输入变量（home price/down payment/rate）。
  2. **组件硬编码em dash**（AI写作特征维度，本工具2026-08-05发布早于2026-08-07规则生效日，触发全量humanizer检查）：仅扫`tools.ts`数据字段零命中，抓build产物逐字符扫描才发现`src/components/calculators/MortgageCalculator.tsx`第126行calc-note提示文案含1处真实em dash（"not amortized — they change the total payment"）——与本站2026-08-11 temperature-converter审计记录的失效模式完全一致。**修复**：改为冒号消除。
- **另发现2处候选，经独立agent复核判定NOT CONFIRMED、未修改**：`sources[]`的3条label字段用站内既有"信源名 — 一句话说明"格式（全站83处同款写法），独立复核agent援引length-converter 2026-08-13先例（结构化引用标签惯例，非叙事问题）判定不构成问题；正文1处"0.5–1.5%"为合法数字区间排版惯例（en dash非叙事em dash），同样判定不构成问题。但同一天CalcBadger的glitch-text-generator审计对完全同款的sources[].label格式给出了相反裁决（已修复）——这个口径分歧已追加记录到`独立站/待Owen处理事项.md`既有条目（`imageCredit/sources元数据字段的破折号是否要按硬规则清零`），未擅自统一处理，也未回溯清理本工具或其他工具的历史sources[]格式。
- **两处修复均为定点修改**，未做大范围重写；`npm test -- mortgage`15/15通过、全站`npm test`790/790通过（40个测试文件）；`npm run build`89页成功；`git status`确认`broken-link-outreach-log.md`被同仓库并发任务修改且有一个indexnow日志备份文件，均未纳入本次commit，仅`git add`本次相关2个文件，单独commit `67624d8`并push。
- **部署与验证**：push后curl轮询6次（约2分钟）确认线上description已生效部署；同时确认线上JSON-LD WebApplication.description与build产物一致、组件em dash已从线上HTML消失、"not amortized:"新文本已生效。`node tools/submit-indexnow.mjs /mortgage-calculator/`提交成功（Bing 200/Yandex 200）——提交前一次误用`--help`当作URL参数提交了`https://calcbadger.com/--help`垃圾条目到`indexnow-submit-log.json`，已发现并在下一次commit前手工清除该条目，重新以正确URL提交。
- **未发现问题的维度**：EEAT（公式权威性，Reg Z Appendix J为教科书级标准公式）、时效性（Reg Z与HPA均无近期修订）、竞品差异化（WebSearch核对"mortgage calculator"目前SERP由calculator.net/NerdWallet/Bankrate/各大银行官方计算器占据，属极高竞争度头部词，本页相比纯计算器类竞品有监管依据引用+worked example+具名署名等真实增量）、GEO（按ai-seo skill的Content Extractability Check人工核对9-10/10项通过，description精简未影响正文GEO结构）、内链健康度（Node独立复现pickRelatedGuides+crossCategory完整逻辑，36/36工具零孤儿覆盖，mortgage-calculator本身relatedFinal输出与线上实际渲染完全一致）、Schema一致性（WebApplication/FAQPage/BreadcrumbList三处均与tools.ts逐字段核对一致）、合规（terms页"No professional advice"条款明确覆盖financial场景，footer可达）、图片（不适用，纯UI计算器无正文配图）、AdSense（ads.txt/about/privacy/terms均正常，无敏感类目）。
- 两条独立审核agent均正常完成（第一条约31秒6次工具调用，第二条约34秒6次工具调用），均在15-20分钟等待上限内返回，未触发全局CLAUDE.md看门狗规则里的卡死降级流程。无推翻核心结论的情形，未触发Owen邮件升级。
- **教训库回写**：L-0805-2（SEO字段长度离群检测）追加一条毕业后复发记录，说明该检查目前只覆盖首发写作流程，已发布未审计的旧文章仍可能带着离群值挂在线上；L-0810-4（humanizer破折号检查易漏组件/元数据字段）追加一条复发+一次相反裁决记录，并同步更新`独立站/待Owen处理事项.md`既有条目补充CalcBadger站内自相矛盾的新证据。
